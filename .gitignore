# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# SDD Pilot tooling (kept locally, not tracked)
#/.claude/
#.claudeignore
#/.github/agents/
#/.github/instructions/
#/.github/skills/
#/.github/sddp-config.md

# SDD agent-context / governance docs
#/AGENTS.md
#/CLAUDE.md
#/project-instructions.md

# SDD specs / artifacts
#/specs/

# Local backup of pre-rewrite git history (created during history scrub)
/.git.backup-pre-scrub/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
node_modules

# Local dev secrets & demo artifacts (never commit)
/secrets/
# Native (no-Docker) environment file — holds DATABASE_URL with the DB password inline.
# `.env.native.example` IS committed; the populated `.env.native` must never be.
/.env.native
/cookies.txt
/examples/license-demo/.out/

# Browser demo app (generated wasm build + snapshot; node deps/build)
/examples/license-demo-app/src/wasm/
/examples/license-demo-app/public/demo-bundle.json
/examples/license-demo-app/node_modules/
/examples/license-demo-app/dist/
