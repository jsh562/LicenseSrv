{
  "name": "license-demo-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "setup": "bash ../../scripts/build-wasm-web.sh && node ../../scripts/prepare-demo-app.mjs",
    "demo-app": "npm run setup && npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
