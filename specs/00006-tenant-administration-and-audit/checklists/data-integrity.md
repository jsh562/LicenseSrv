# Data Integrity Requirements Quality Checklist

> Purpose: A unit test for the SPEC/data-model of tenant administration & audit (E005) — checks whether the requirements specify the new `admin_session` schema, the `app_user` credential/lockout extension, forced-RLS tenant isolation, expand-only migration 0005, the two credential lookups, the entity state machines, the race-safe last-owner invariant, secret hygiene, and the reused append-only audit completely and consistently; it does not test running code.

**Created**: 2026-07-02 | **Feature**: [spec.md](../spec.md) | **Domain**: Data Integrity | **Audience**: PR reviewer

## admin_session Schema Completeness

- [X] CHK201 Are all `admin_session` columns enumerated with type, nullability, and constraint — `id`, `tenant_id`, `user_id`, `token_hash`, `expires_at`, `revoked_at`, `created_at`, `last_seen_at` — with the PK specified as composite `(tenant_id, id)` and `id` as an application-generated UUID v7? [Completeness, data-model §3] — data-model §3 table enumerates all 8 columns with type/nullability/constraint and PK `(tenant_id, id)`; §Conventions defines `id` as UUID v7, application-generated, time-ordered; §3 `id` note "UUID v7; logical session id".
- [X] CHK202 Are the constraint specifics stated precisely — `token_hash` NOT NULL and **globally UNIQUE**, `expires_at` NOT NULL, `revoked_at` nullable, `created_at` NOT NULL DEFAULT `now()` — and is `last_seen_at` defined as best-effort activity telemetry that is NOT authoritative for expiry, so it cannot be mistaken for a validity gate? [Completeness, data-model §3 / §5] — data-model §3: token_hash NOT NULL + UNIQUE ("Globally UNIQUE → enables the pre-tenant resolution lookup"), expires_at NOT NULL, revoked_at nullable, created_at NOT NULL DEFAULT now(); last_seen_at = "Best-effort activity telemetry, bumped on use... Not authoritative for expiry"; §5 lists the global token lookup.
- [X] CHK203 Is `user_id` specified as an intra-tenant composite FK `(tenant_id, user_id) → app_user(tenant_id, id)` (backed by `idx_admin_session_user`), so a session can never bind to a user in another tenant? [Consistency, data-model §3 / §5] — data-model §3 user_id row + §5 FK row: `FOREIGN KEY (tenant_id, user_id) REFERENCES app_user (tenant_id, id)`, backed by `idx_admin_session_user (tenant_id, user_id)`; "cross-tenant binding is structurally impossible".

## app_user Credential & Lockout Extension

- [X] CHK204 Are the four added `app_user` columns enumerated with type/nullability/default/constraint — `password_hash` text (null), `status` text NOT NULL DEFAULT `'active'` CHECK IN(invited,active,deactivated), `failed_login_count` int NOT NULL DEFAULT `0`, `locked_until` timestamptz (null) — and is each stated to be additive and backward-compatible (nullable or defaulted, expand-only)? [Completeness, data-model §2] — RESOLVED: data-model §2 fully enumerates all four columns (type/nullability/default/constraint) and states each is additive/expand-only (each nullable or defaulted). Reconciled the `status` CHECK enumeration here to `IN(invited,active,deactivated)` to match the data-model/contract fix (see CHK206); DEFAULT remains `'active'`.
- [X] CHK205 Is `password_hash` nullability justified by concrete cases (invited-but-not-activated user, P2 SSO-only user), and is a null `password_hash` ("cannot sign in via password") explicitly distinguished from `status='deactivated'`, so the two "cannot sign in" states are not conflated? [Clarity, data-model §2] — data-model §2 password_hash row: nullable "because a principal may legitimately have no password: an invited-but-not-yet-activated user (`status='invited'`...), or a P2 SSO-only user (FR-016)"; null = "cannot sign in via password — distinct from `status='deactivated'` (the two ... states are not conflated)".
- [X] CHK206 Is the `status` domain specified as a closed set `{invited, active, deactivated}` via CHECK, with the E002 data-model's aspirational `active/disabled/deleted` naming explicitly declared superseded here (and GDPR erase kept as a separate E002 job, not a `status` value), so there is no cross-artifact drift on user-state values? [Consistency, data-model §2 / §11] — RESOLVED: found a genuine cross-artifact drift — the API contract `UserStatus` enum is `[invited, active, deactivated]` (invited = created, credential not yet set), but the data-model CHECK was `{active, deactivated}`. Added `invited` to the data-model status domain — §1 entity table, §2 CHECK `IN('invited','active','deactivated')` (DEFAULT `active`; invite-first inserts `invited`; `invited → active` on first password set), §11 state machine, §14 summary, and the ER diagram — so data-model and contract agree. The E002 aspirational `active/disabled/deleted` naming is still explicitly superseded and GDPR erase remains a separate E002 job (§2/§11). Checklist expectation updated to the reconciled closed set.

## Forced RLS, Grants & Non-Owner Isolation

- [X] CHK207 For `admin_session`, are both `ENABLE` and `FORCE ROW LEVEL SECURITY` required (with FORCE's purpose — subjecting the owner too so no owner-owned view bypasses isolation — stated), the `tenant_isolation` predicate specified identically on `USING` and `WITH CHECK`, and the grants to the non-owner `licensesrv_app` enumerated (`SELECT, INSERT, UPDATE, DELETE`) with `UPDATE` (last_seen_at / revoked_at), `DELETE` (prune), and `INSERT` (sign-in) each justified? [Coverage, data-model §6] — data-model §6 SQL block: `ENABLE` + `FORCE ROW LEVEL SECURITY`, identical USING/WITH CHECK `tenant_isolation` predicate, `GRANT SELECT,INSERT,UPDATE,DELETE ... TO licensesrv_app` (the non-owner NOBYPASSRLS role, §Conventions); FORCE "subjects the table owner too, so no owner-owned view/function silently bypasses isolation"; UPDATE (bump last_seen_at / set revoked_at), DELETE (prune expired/revoked), INSERT (create session at sign-in) each justified.
- [X] CHK208 Is the unset-GUC behavior specified — `NULLIF(current_setting('app.current_tenant', true), '')::uuid` → NULL → predicate matches **zero rows** — so any tenant-scoped session query without a resolved tenant is refused rather than run unscoped? [Clarity, data-model §6] — data-model §6: "Unset GUC → NULLIF(...) → NULL → predicate matches zero rows, so any tenant-scoped session query without a resolved tenant is refused, never run unscoped (FR-002, SC-002)"; also §Conventions restates the same rule.
- [X] CHK209 Is it specified that the `app_user` `ALTER` adds columns **only** and **inherits** the existing E002 RLS enable/force, `tenant_isolation` policy, and grants unchanged (the migration adds no RLS/grant statement for `app_user`), so the credential columns cannot be read outside tenant scope? [Consistency, data-model §2 / §14] — data-model §2: "app_user already has RLS ENABLE+FORCE, the tenant_isolation policy, and SELECT/INSERT/UPDATE/DELETE grants from E002 0002. The ALTER adds columns only and inherits that isolation and those grants unchanged — the migration adds no RLS/grant statement for app_user"; §14 restates "app_user ALTER inherits its existing E002 RLS/policy/grants (migration adds none)".

## Migration & Index Integrity (0005 expand-only)

- [X] CHK210 Is `migrations/0005_admin_sessions.sql` specified as expand-only and sequential (after `0004`) — additive `ALTER app_user` columns + `CREATE TABLE admin_session` + indexes + RLS/policy/grants only, with no changes to existing tables and no new audit/role table — so it cannot conflict with `0000`–`0004`? [Completeness, data-model §Scope / §14] — data-model header (Scope + "New migration") and §14: `0005_admin_sessions.sql` "expand-only, sequential (after 0004): ALTER TABLE app_user ADD COLUMN ...; CREATE TABLE admin_session (...) + composite FK; CREATE UNIQUE INDEX/CREATE INDEX; ENABLE/FORCE RLS + tenant_isolation policy + grants. No new audit/role table; role, api_key, audit_log reused unchanged."
- [X] CHK211 Is it specified that every new index is `tenant_id`-leading (PK `(tenant_id, id)`, `idx_admin_session_user (tenant_id, user_id)`, and `app_user` login reusing existing `UNIQUE (tenant_id, email_hash)` — no new index), while `UNIQUE (token_hash)` is the deliberate **global, non-tenant-scoped** index, and is that asymmetry justified as the pre-tenant resumption lookup (mirroring `api_key.key_hash`) rather than left as an unexplained deviation? [Consistency, data-model §5] — data-model §5 constraints/indexes table + closing note: PK `(tenant_id, id)` and `idx_admin_session_user (tenant_id, user_id)` are tenant-leading, login reuses existing `UNIQUE (tenant_id, email_hash)` (no new index), and `UNIQUE (token_hash)` is called out as the deliberate global pre-tenant resumption lookup "Mirrors api_key.key_hash"; "All new indexes are tenant_id-leading".

## Two Credential Lookups (privileged pre-tenant vs RLS-scoped)

- [X] CHK212 Are the two credential lookups distinguished unambiguously — interactive login keyed by `email_hash` (unique **per tenant**, requiring a slug/subdomain selector, run RLS-scoped via `withTenant`) versus session resumption keyed by `token_hash` (**globally** UNIQUE, a **privileged pre-tenant** lookup on the RLS-bypassing connection like `resolveApiKey`) — with the privileged read specified to return only `(tenant_id, user_id, validity)`, never `token_hash`, and then drop to `withTenant` scope for all further work? [Completeness, data-model §4] — data-model §4 two-lookup table + notes: interactive sign-in keyed by per-tenant `email_hash` (slug/subdomain selector, RLS-scoped `withTenant`) vs session resumption keyed by global `token_hash` ("Privileged pre-tenant lookup, exactly like resolveApiKey"); privileged read selects only `tenant_id, user_id, expires_at, revoked_at`, "never returns token_hash and does nothing tenant-scoped", "then set app.current_tenant and drop to licensesrv_app for all further work".

## Entity State Machines

- [X] CHK213 Is a session's validity defined by a concrete predicate — `revoked_at IS NULL AND now() < expires_at AND owner.status='active'` — with `active → revoked` (sign-out/forced, terminal) and `active → expired` (implicit at `now() ≥ expires_at`, no column change) enumerated, and the owner-status gate specified so deactivating/erasing the owner invalidates their sessions on the next request? [Completeness, data-model §11 / §3] — data-model §3 ("valid iff revoked_at IS NULL AND now() < expires_at (and the owning app_user.status = 'active')") + §11: `active → revoked` (sign-out/forced, terminal), `active → expired` (implicit at now() ≥ expires_at, no column change), and "Deactivating/erasing the owner invalidates all their sessions on the next request (owner-status gate)".
- [X] CHK214 Are the reused entities' state machines each enumerated with no undefined transition — `app_user` `active ↔ deactivated` (reversible) versus `api_key` `active → revoked` (terminal, explicitly no `revoked → active`, rotate = revoke old + create new) — so integrity-invalid transitions are disallowed rather than left open? [Edge-Case, data-model §11] — data-model §11: `app_user` transitions now fully enumerated (`invited → active`, `invited → deactivated`, `active → deactivated`, `deactivated → active`; "No other transitions are defined") — reversible active↔deactivated preserved and the new `invited` state's transitions closed off; `api_key` `active → revoked` terminal, "No `revoked → active`", rotate = revoke + create new. No undefined transition left open.

## Last-Owner Safeguard & Role Integrity

- [X] CHK215 Is the last-owner safeguard specified as an **app-layer invariant (NOT a DB constraint)**, with a concrete race-safe mechanism (`SELECT … FOR UPDATE` over the tenant's owner grants, or a `SERIALIZABLE` transaction, in the same transaction as the mutation), covering both demoting/removing and **deactivating** the last active-user owner, and required to fail-closed and record a `security_event` — given it is cross-row logic no single per-row constraint can express? [Consistency, data-model §7 / plan AD-008] — data-model §7: "Last-owner safeguard = app-layer invariant, NOT a DB constraint", "enforced in the repository, in the same transaction as the mutation", assert "≥ 1 other active-user owner remains ... otherwise refuse (fail-closed) and record a security_event", race-safe via "SELECT … FOR UPDATE over the tenant's owner grants or a SERIALIZABLE transaction", and "treat deactivating the last active owner as covered by the same guard"; plan AD-008 mirrors (FOR-UPDATE guard → 409 last_owner).
- [X] CHK216 Is role grant/change integrity specified precisely — grant = `INSERT` a `role` row guarded by `UNIQUE (tenant_id, user_id, role)`; change = insert-new + delete-old (a swap) within one transaction; the role domain a closed set `{owner, admin, viewer}`; and the last-owner guard's count defined as "≥ 1 other `active`-user owner remains" — so integrity-relevant role operations are bounded and unambiguous? [Completeness, data-model §7 / §1] — data-model §7: grant = "INSERT a role row ... UNIQUE (tenant_id, user_id, role) prevents duplicate grants", change = "insert the new grant and delete the old (a swap), within one transaction", guard asserts "≥ 1 other active-user owner remains"; §1 role row: `role: text CHECK IN(owner,admin,viewer)`, `UNIQUE (tenant_id, user_id, role)`.

## Secret Hygiene & Brute-Force Lockout

- [X] CHK217 Is the invariant stated as a hard requirement that **no column ever stores a plaintext password or a raw session token** — `password_hash` a slow-KDF one-way hash, `token_hash` a fast hash of a high-entropy token, `key_hash` unchanged — and that none of these is ever returned by any API projection or written to any log (with the user/session projections that ARE exposed enumerated, and audit `before/after` snapshots secret-minimized at write time)? [Completeness, data-model §10 / Spec FR-017] — data-model §10 + §Conventions + §2/§3: "no hashed-credential column is ever returned by any API projection or written to any log"; password_hash slow-KDF (§2), token_hash fast hash of high-entropy token (§3), key_hash unchanged; exposed projections enumerated — users expose `id, status, masked/email_hash-derived id`; session listings expose `id, created_at, last_seen_at, expires_at, revoked_at` (never token_hash); "audit_log.before/after snapshots MUST be PII/secret-minimized at write time"; raw password/token never stored (§Conventions). Traces Spec FR-017.
- [X] CHK218 Are the lockout semantics specified quantitatively enough to verify — `failed_login_count` incremented on each failed verify and reset to `0` on success; crossing threshold `N` sets `locked_until = now() + backoff`; while `now() < locked_until` sign-in is refused **before** password verification regardless of a correct password; `locked_until` independent of `status`; and a single generic failure returned to avoid user/tenant enumeration — with the distinction living only in the audit trail? [Measurability, data-model §9 / Spec FR-018] — data-model §9: increment on each failed verify, "crosses the threshold N, set locked_until = now() + backoff"; reset to 0 + locked_until NULL on success; "While locked ... sign-in is refused before password verification, regardless of a correct password"; "Independent from status"; "the same generic failure is returned ... the distinction lives only in the audit trail". Traces Spec FR-018.

## Append-Only Audit Reuse & Traceability

- [X] CHK219 Is the reused `audit_log` specified as append-only and tamper-evident — `licensesrv_app` holding only `INSERT`/`SELECT` (no `UPDATE`/`DELETE` for any role, including owner) — with exactly one row appended per administrative mutation and per denial in the **same transaction** (audit-write failure rolls back the mutation), an enumerated event/action catalog, correct `security_event` flags, and no secret material ever in `before`/`after`? [Coverage, data-model §12 / Spec FR-013] — data-model §12: "Every administrative mutation and every denial appends one audit_log row in the same transaction (atomic — an audit-write failure rolls back the mutation)"; enumerated event/action/target/security_event catalog table; "No secret material is ever written to before/after"; "licensesrv_app holds only INSERT/SELECT on audit_log (no UPDATE/DELETE), so no console action by any role — including owner — can edit or delete an entry". Traces Spec FR-013.
- [X] CHK220 Does every data-integrity-relevant requirement (FR-001/002/003/006/007/008/009/010/013/017/018) trace to at least one Success Criterion (SC-001..SC-010) and a plan Requirement-Coverage-Map component, so no session, isolation, credential, role, or audit rule is left unverifiable or unowned? [Traceability, Spec §Success Criteria / plan §Requirement Coverage Map] — every listed FR traces to ≥1 SC and a plan Requirement Coverage Map row: FR-001→SC-001 (auth.ts/session.ts); FR-002→SC-002 (rbac-middleware.ts); FR-003→SC-001 (session.ts); FR-006→SC-004 (users.ts); FR-007→SC-004 (users.ts/session.ts); FR-008→SC-005 (users.ts); FR-009→SC-006 (apikeys.ts); FR-010→SC-006 (apikeys.ts); FR-013→SC-007 (audit.ts + E002 grants); FR-017→SC-009 (password.ts/users.ts); FR-018→SC-009 (auth.ts + migration 0005). No integrity rule left unverifiable/unowned.
