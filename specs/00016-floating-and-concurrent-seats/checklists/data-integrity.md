# Data Integrity Checklist: Floating & Concurrent Seats
**Created**: 2026-07-23 | **Feature**: [spec.md](../spec.md)

## Race-Safe Concurrency Accounting & Effective Cap

- [X] CHK001 Is the invariant "count of live leases per license never exceeds the effective cap" stated as a testable requirement independent of any locking mechanism? [Measurability, FR-003 / SC-002 / INV-1] <!-- Evaluator: Covered by spec.md FR-003/SC-002 + data-model INV-1 — cap invariant stated independent of the locking mechanism -->
- [X] CHK002 Is the concurrent-race acceptance stated measurably — for C free seats, exactly C of any number of concurrent acquisitions succeed and the live count never exceeds the cap? [Measurability, SC-002] <!-- Evaluator: Covered by spec.md SC-002 — exactly-C-of-N measurable acceptance; effective-cap target now the assertion boundary -->
- [X] CHK003 Is the "effective cap" defined unambiguously as `max_concurrent + concurrency_overage`, and is that SAME definition used by both the accounting/race requirement and the overage requirement (no second definition)? [Consistency, FR-012 / data-model §5] <!-- Evaluator: Resolved — aligned FR-003 & SC-002 to the single effective-cap definition (max_concurrent + concurrency_overage) matching FR-012/INV-1 -->
- [X] CHK004 Is the source of truth for live-seat usage unambiguously a derived COUNT of live leases (not a stored counter), with a single definition across spec and data-model? [Clarity, data-model §1/§5 / Key Entities] <!-- Evaluator: Covered by data-model §1 (Seat usage = COUNT(*) of LIVE leases) + Key Entities + INV-1 -->
- [X] CHK005 Is "no over-allocation / no partial lease" defined measurably — a refused acquire records no lease row and there is no observable state where the live count exceeds the effective cap? [Measurability, FR-004 / SC-003] <!-- Evaluator: Covered by spec.md FR-004/SC-003 + data-model race-safe acquire (else refuse, no partial row) -->
- [X] CHK006 Is the serialization boundary (a per-license lock covering count+insert) captured as a stated design constraint rather than left implicit, so the race requirement is traceable to a mechanism? [Traceability, AD-001 / data-model §2 race-safe acquire] <!-- Evaluator: Covered by plan AD-001 + HINT-001 + data-model §2 race-safe acquire (per-license pg_advisory_xact_lock count+insert) -->
- [X] CHK007 Is the aggregate-cap guard (advisory-lock count+insert) distinguished from the same-holder guard (partial-unique index) so the two are not treated as redundant or contradictory? [Consistency, data-model §2/§5 / INV-1 / INV-2] <!-- Evaluator: Covered by data-model §2 (aggregate advisory-lock cap vs same-holder lease_one_live index explicitly distinguished) + INV-1 vs INV-2 -->

## Reclaim <-> Renew Mutual Exclusion (Generation Fence)

- [X] CHK008 Is reclaim/renew mutual exclusion stated as a requirement — a renew arriving after its lease was reclaimed or expired is rejected with a distinct reason? [Completeness, FR-011 / SC-008] <!-- Evaluator: Covered by spec.md FR-011/SC-008 -->
- [X] CHK009 Is the fencing mechanism specified unambiguously — a monotonic `generation` (initialized at acquire, bumped on each successful renew) matched under `status='live' AND expires_at>now()`, so a stale renew touches zero rows? [Clarity, AD-003 / data-model §2 guarded renew / INV-3] <!-- Evaluator: Covered by plan AD-003 + data-model §2 guarded-renew SQL + INV-3 -->
- [X] CHK010 Is "no seat double-count" defined measurably — a reclaimed seat can never be revived by a late renew and the live count is never inflated? [Measurability, FR-011 / SC-008] <!-- Evaluator: Covered by spec.md FR-011/SC-008 (no revive of a reclaimed seat, no double-count) -->
- [X] CHK011 Is the distinct stale/late-renew rejection reason (`lease_not_renewable`) required consistently across spec, the state machine, and the error model? [Consistency, FR-011 / plan Error Handling / state machine §4] <!-- Evaluator: Covered by FR-018 (lease-not-renewable enumerated) + plan Error Handling (409 lease_not_renewable) + data-model §2/§4 — consistent code -->

## Holder-Key Uniqueness & Concurrency Scope

- [X] CHK012 Is "at most one live lease per (license, holder-key)" stated as a data invariant in the requirements, not only as a partial-unique index definition? [Traceability, FR-023 / Key Entities / INV-2] <!-- Evaluator: Covered by FR-023 (enforces the invariant) + Lease Key Entity invariant + INV-2 -->
- [X] CHK013 Is the holder-key derivation defined deterministically (salted hash of a client-supplied opaque reference, scoped per `concurrency_scope`) so the same holder reproduces the same key across restarts? [Clarity, FR-001 / FR-023] <!-- Evaluator: Covered by FR-001 (stable client ref, salted+hashed) + FR-026 (server-held salt) + Clarifications (dedupes across restarts) -->
- [X] CHK014 Is re-acquire from the same holder-key required to be idempotent (return/renew the existing lease, consume no second seat) and reconciled with the single-use acquire idempotency token? [Consistency, FR-023 / FR-007 / FR-014 / SC-016] <!-- Evaluator: Covered by FR-023 (→FR-007/FR-014) + Edge Cases + data-model §2 lease_one_live — same-holder idempotency reconciled with nonce replay (both return original lease, no second seat) -->
- [X] CHK015 Are the three concurrency scopes (session/machine/user) enumerated with a defined default and a defined effect on the count (machine-scope instances share one seat vs session-scope two)? [Coverage, FR-023 / SC-016] <!-- Evaluator: Covered by FR-023 (session default / machine / user, per-scope effect) + SC-016 (machine shares one seat vs session two) -->
- [X] CHK016 Is the `concurrency_scope` snapshotted on the lease row (scope in force at acquire) distinguished from the plan/license scope, so a later scope change cannot retroactively re-key live leases? [Consistency, data-model §1 / AD-006] <!-- Evaluator: Covered by data-model §1 + lease.concurrency_scope column ("scope snapshot in force when acquired") + AD-006 license snapshot — holder_key computed at acquire, never re-keyed -->

## Idempotency, Anti-Replay & Overage Metering

- [X] CHK017 Is the acquire idempotency token defined as single-use (`UNIQUE (tenant_id, nonce)`), and is the replay outcome (returns the ORIGINAL lease, consumes no second seat) stated measurably? [Measurability, FR-014 / SC-011 / INV-12] <!-- Evaluator: Covered by FR-014 + SC-011 + INV-12 + data-model lease_nonce_uniq -->
- [X] CHK018 Is the nonce uniqueness scope defined (per-tenant) and justified against cross-tenant collision, so the constraint boundary is unambiguous? [Clarity, data-model §1 lease_nonce_uniq] <!-- Evaluator: Covered by FR-014 (unique per tenant) + data-model lease_nonce_uniq UNIQUE (tenant_id, nonce) — per-tenant boundary + forced RLS make cross-tenant collision moot -->
- [X] CHK019 Is idempotent release specified to never drive the live-lease count below the true value (never negative) for both an already-released AND an unknown lease? [Measurability, FR-008 / SC-006 / INV-11] <!-- Evaluator: Covered by FR-008 (already-released or unknown, never below zero) + SC-006 + INV-11 -->
- [X] CHK020 Is idempotent renew specified to keep exactly one seat and never corrupt lease state under repeated heartbeats? [Completeness, FR-007 / SC-005] <!-- Evaluator: Covered by FR-007 (repeated renewals keep exactly one seat, never corrupt state) + SC-005 -->
- [X] CHK021 Is the authoritative record of a soft-cap overage acquisition defined as the append-only audit entry (with the concurrency level reached), and is the lease-row `overage` boolean consistently described as a non-authoritative flag rather than a second source of truth? [Consistency, FR-013 / INV-10 / data-model §1 overage] <!-- Evaluator: Covered by FR-013 + INV-10 + data-model lease.overage comment ("AUTHORITATIVE meter is the append-only audit entry"; flag non-authoritative) -->
- [X] CHK022 Is the nonce retention/replay window bounded so store-and-replay idempotency holds for the full window a retry could occur before a nonce may be purged? [Completeness, FR-014 / data-model §1 nonce — GAP: no bounded replay window stated] <!-- Evaluator: Covered by FR-014 (Security amendment) — nonce retained for the row's retention lifetime; replay rejected for the full retention window; post-retention reuse is a fresh cap-checked acquire that cannot resurrect/double-count. Prior GAP now closed -->

## Snapshot Immutability & Fail-Closed Entitlement

- [X] CHK023 Is the snapshot-at-issuance of `max_concurrent`, scope, overage, timings, and per-reason policy onto the license required to immunize live leases from later plan edits, stated as a requirement (not only a schema note)? [Completeness, AD-006 / License entity / data-model §1 License] <!-- Evaluator: Covered by FR-005 (max_concurrent snapshotted) + License Key Entity (scope/overage/timings/policy snapshotted at issuance) + AD-006 + data-model §1 (snapshot immunizes live leases) -->
- [X] CHK024 Is the distinction between snapshotted enforcement values (on the license) and live-read plan toggles (`concurrency_require_activation`, `lease_signed_handle`) defined, so a reviewer knows which value governs each check? [Clarity, data-model §1 License/Plan / AD-006] <!-- Evaluator: Covered by data-model §1 Plan/License — gating + handle toggles read live at acquire; only enforcement-governing values snapshotted -->
- [X] CHK025 Is absent `max_concurrent` required to fail closed with a distinct "no concurrency entitlement" reason — never treated as unlimited and never falling back to `max_activations`? [Completeness, FR-005 / SC-019 / INV-6] <!-- Evaluator: Covered by FR-005 + SC-019 + INV-6 (fail-closed, never unlimited, never falls back to max_activations) -->
- [X] CHK026 Is the independence of `max_concurrent` from `max_activations` stated as a requirement (each counted separately; a plan may carry both), so the two seat dimensions cannot be conflated in accounting? [Consistency, FR-005 / FR-025] <!-- Evaluator: Covered by FR-005 (independent, each counted separately, may carry both) + FR-025 (floating not gated on activation by default) -->

## Lease State Machine & License-State Coupling

- [X] CHK027 Are the lease state transitions fully specified — live→released, live→reclaimed (timer or revoke) — with released/reclaimed terminal and no return to live? [Completeness, FR-024 / state machine §4] <!-- Evaluator: Covered by data-model §4 state machine (all transitions + terminal states, no return to live) + FR-024 -->
- [X] CHK028 Is the `lease_ended_shape` invariant stated (a live lease has NULL `ended_at`; a terminal lease records `ended_at`), so no half-populated row is valid? [Completeness, data-model §2 lease_ended_shape] <!-- Evaluator: Covered by data-model §2 lease_ended_shape CHECK (live⇒ended_at NULL; terminal⇒ended_at NOT NULL) -->
- [X] CHK029 Is the per-reason license-state policy defined unambiguously (revoke ⇒ reclaim within the sweep interval; suspend/expire ⇒ lapse-on-timer default) with its configurability stated? [Clarity, FR-024 / SC-017 / data-model §4] <!-- Evaluator: Covered by FR-024 + SC-017 + data-model §4 + configurable lease_policy_on_{revoke,suspend,expire} columns/CHECKs -->
- [X] CHK030 Is renew required to re-check live license status so a renew against a revoked (or suspended/expired per policy) license is refused? [Completeness, FR-024 / state machine §4] <!-- Evaluator: Covered by FR-024 (renew MUST re-check live license state) + data-model §4 (renew refused against revoked license) -->
- [X] CHK031 Is the boundary between E015 server-side revoke-reclaim of its own leases and E013 revocation propagation to clients stated explicitly, so the two are not conflated? [Consistency, STF-001 / Scope Excluded / FR-024] <!-- Evaluator: Covered by Scope/Excluded (E015 reclaims own leases vs E013 client/token propagation) + STF-001 [RESOLVED] + FR-024 -->

## Timing, Expiry & Sweeper Determinism

- [X] CHK032 Is lease expiry required to be server/monotonic-time computed (`last_renewed_at + lease_ttl_seconds`), with an explicit prohibition on trusting a client wall clock? [Clarity, FR-009 / INV-4] <!-- Evaluator: Covered by FR-009 (server/monotonic time, MUST NOT trust client wall clock) + INV-4 + data-model §1 (server-computed expires_at) -->
- [X] CHK033 Is the TTL ≥ 3× heartbeat invariant stated as an enforced CHECK on both `plan` and `license`, with its rationale (a single missed heartbeat never reclaims a live seat)? [Measurability, FR-009 / INV-5 / data-model §2 CHECKs] <!-- Evaluator: Covered by FR-009 (TTL ≥ 3× heartbeat rationale) + INV-5 + data-model CHECK plan_lease_ttl_ge_3x_hb & license_lease_ttl_ge_3x_hb -->
- [X] CHK034 Is the reclaim trigger boundary quantified (a live lease with `expires_at + grace < now` is reclaimed), so reclaim timing is measurable rather than "eventually"? [Measurability, FR-010 / SC-007 / data-model §4] <!-- Evaluator: Covered by FR-010 + SC-007 + data-model §4 (flips live rows with expires_at + grace < now) -->
- [X] CHK035 Is the sweeper's fail-open behavior stated as a requirement — an error on one license or tenant never blocks acquire/renew/release elsewhere? [Completeness, FR-010 / SC-008 / AD-002] <!-- Evaluator: Covered by FR-010 (fail-open; error never blocks any license/tenant) + SC-008 + AD-002 -->
- [X] CHK036 Is the sweeper batch size / scan ordering / per-run bound specified so its behavior on a large lapsed set is deterministic and bounded? [Completeness, FR-010 / Risk Mitigation — GAP: "sweeper batch bound" noted but no batch/ordering requirement stated] <!-- Evaluator: Resolved — added bounded batch (configurable max/run, default 1000), oldest-expired-first (ascending expires_at), idempotent-across-runs sweep bound to FR-010 + data-model §4. Prior GAP now closed -->
- [X] CHK037 Is a clock-skew / grace boundary at the expiry instant stated for the network-partition case (client alive but unable to heartbeat), so the reclaim-vs-renew edge is unambiguous? [Clarity, Edge Cases (network partition, clock skew) / FR-009] <!-- Evaluator: Resolved — clarified Edge Cases (clock skew): grace absorbs skew; reclaim boundary strictly expires_at + grace < server_now; at the exact expiry instant a not-yet-past-grace lease stays live and renew still succeeds (consistent with data-model §4) -->

## Referential Integrity, Isolation & Retention

- [X] CHK038 Is the composite FK `(tenant_id, license_id) → license` with `ON DELETE NO ACTION` required, together with the no-hard-delete rule (a license with any lease cannot be hard-deleted; leases end by soft transition)? [Completeness, FR-021 / INV-9 / data-model §2] <!-- Evaluator: Covered by FR-021 + INV-9 + data-model lease_license_fk ON DELETE NO ACTION (no hard-delete; soft transitions) -->
- [X] CHK039 Is tenant isolation stated as a fail-closed data-layer requirement (forced RLS; unset GUC ⇒ zero rows; a cross-tenant lease reference resolves to not found), not only an API-layer check? [Completeness, FR-019 / SC-012 / INV-7] <!-- Evaluator: Covered by FR-019 + SC-012 + INV-7 + data-model forced RLS policy (unset GUC → zero rows; cross-tenant → not found) -->
- [X] CHK040 Is GDPR erasability / retention of pseudonymous lease rows specified — no raw hardware identifier or signing key stored, terminal rows purged by the platform retention path (no app-role DELETE)? [Completeness, FR-020 / SC-015 / INV-8 / data-model §1 Retention] <!-- Evaluator: Covered by FR-020 + SC-015 + INV-8 + data-model §1 Retention + grants (no app-role DELETE; platform retention purge) -->
