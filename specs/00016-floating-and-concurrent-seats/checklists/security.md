# Security Requirements Checklist: Floating & Concurrent Seats

**Created**: 2026-07-23 | **Feature**: [spec.md](../spec.md)

## Authentication & Runtime API-Key Scope

- [X] CHK001 Is the exact scope the runtime lease surface (acquire/renew/release) requires named as a requirement, so fail-closed rejection can be confirmed against the right scope rather than an unspecified one? [Completeness, FR-002] <!-- Evaluator: Covered by contracts/lease-api.openapi.yaml (apiKey securityScheme + x-rbac.requiredScope: lease) and plan.md API Surface ("lease" scope named) -->
- [X] CHK002 Does the requirement set distinguish the outcome for a missing/invalid API key (unauthenticated) from an authenticated key lacking the lease scope (unauthorized)? [Clarity, FR-002] <!-- Evaluator: Covered by contracts info block (401 unauthorized vs 403 forbidden on missing lease scope) + plan.md Error-Handling (401/403) -->
- [X] CHK003 Is there a measurable success criterion verifying fail-closed rejection of an unscoped or wrong-scope runtime call, given FR-002 has no dedicated SC? [Coverage, FR-002] <!-- Evaluator: Resolved — added SC-020 (fail-closed 401/403 scope rejection, no seat consumed) to spec.md -->

## Console RBAC & CSRF

- [X] CHK004 Are viewer vs admin capabilities on the registry and force-release stated consistently between FR-015 ("admin force-releases") and FR-016 ("operator/admin force-release")? [Consistency, FR-015/FR-016] <!-- Evaluator: Resolved — tightened FR-016 to "an operator with the admin role (admin or higher — consistent with FR-015)" removing the loose "operator/admin" ambiguity -->
- [X] CHK005 Does a requirement state the admin force-release route requires an authenticated console session in addition to RBAC and CSRF? [Completeness, FR-016] <!-- Evaluator: Covered by spec.md Assumptions (console session + RBAC + CSRF) and contracts sessionCookie; reinforced in amended FR-016 ("over an authenticated console session") -->
- [X] CHK006 Is the double-submit CSRF mechanism specified precisely enough (token source and match rule) to confirm the fail-closed 403 on a missing/mismatched token? [Measurability, FR-016/SC-013] <!-- Evaluator: Covered by contracts CsrfToken parameter (X-CSRF-Token MUST equal the non-HttpOnly CSRF cookie issued at login; missing/mismatched → 403 fail-closed) -->
- [X] CHK007 Is the CSRF posture specified for the read surface (GET registry) versus the state-changing force-release, so only mutating calls require the token? [Coverage, FR-015/FR-016] <!-- Evaluator: Covered by contracts (CSRF required on state-changing /admin calls; "GET operations do not require it") -->

## Tenant Isolation & Cross-Tenant Non-Disclosure

- [X] CHK008 Does a requirement make forced-RLS default-deny explicit (unset tenant GUC ⇒ zero rows) as a control independent of application-layer checks? [Completeness, FR-019] <!-- Evaluator: Covered by data-model INV-7 and migration 0011 RLS policy ("Unset GUC -> NULL -> zero rows (refuse unscoped access)") -->
- [X] CHK009 Is the cross-tenant → not-found rule stated consistently with the release-idempotency success-on-unknown behavior, so the two rules do not contradict? [Consistency, FR-008/FR-019] <!-- Evaluator: Covered by contracts TENANT SCOPING (release is the single deliberate 200-no-op exception; not an oracle) + plan.md release-carveout note -->
- [X] CHK010 Does the requirement set make explicit that the release no-op on an unknown/cross-tenant lease id is RLS-safe (touches zero rows of another tenant) and is not an enumeration oracle? [Coverage, FR-008/FR-019] <!-- Evaluator: Covered by contracts (release "neither reads nor mutates any cross-tenant state and is not an enumeration oracle") -->
- [X] CHK011 Are the routes that retain cross-tenant → 404 (renew, admin registry, force-release) enumerated distinctly from the release carve-out in a requirement? [Clarity, FR-019] <!-- Evaluator: Covered by plan.md ("renew + admin routes keep cross-tenant → 404") + contracts per-route responses -->

## Signing-Key & Lease-Handle Secret Non-Exposure

- [X] CHK012 Is "never exposed" for the signing private key / lease secret specified across every channel a reviewer must check — API response, log, and audit entry? [Completeness, FR-022/SC-015] <!-- Evaluator: Covered by FR-022 ("MUST NEVER appear in any response, log, or audit entry") + SC-015 -->
- [X] CHK013 Does a requirement mandate E004 domain separation (a distinct lease-handle domain) so the handle cannot be confused with the LIC1 token or CRL domains? [Traceability, AD-004] <!-- Evaluator: Covered by plan.md AD-004 + HINT-003 (domain LICSRV-LEASE-v1, distinct from LIC1/CRL) + contracts SIGNED LEASE HANDLE -->
- [X] CHK014 Is it specified that only the public handle artifact and the opaque signing-key id are exposed, and that the opaque key id itself carries no secret material? [Clarity, FR-022/SC-015] <!-- Evaluator: Covered by FR-022 ("only the public handle and the opaque key id are exposed") + contracts keyId ("OPAQUE identifier only, never key material") + data-model -->
- [X] CHK015 Is the residual validity of a signed handle after an early release/reclaim addressed — a locally verifiable handle may stay valid until handle TTL while the server has already freed the seat? [Coverage, FR-022] <!-- Evaluator: Resolved — added Edge Case bounding the handle's short TTL to the heartbeat interval so the residual offline-verifiable window after an early release/reclaim is at most one heartbeat cycle, server stays authoritative -->
- [X] CHK016 Does a requirement bound the signed handle's TTL to the lease TTL so its offline-verifiable reuse window is explicitly limited? [Measurability, FR-022/SC-018] <!-- Evaluator: Covered by FR-022 ("whose validity is bounded by the lease TTL") + SC-018 -->

## PII Minimization & Holder-Key Derivation

- [X] CHK017 Is holder-key derivation (salted hash of a client-supplied reference) specified for all three scopes, including that machine scope hashes the E009 fingerprint and never ingests a raw hardware id? [Completeness, FR-020/FR-023] <!-- Evaluator: Covered by FR-023 (session/machine/user) + FR-001 (salt+hash) + contracts Fingerprint/SignalHash ("Raw hardware identifiers are never transmitted") -->
- [X] CHK018 Is salt management for the holder-key specified — its scope (per-tenant vs global) and rotation policy — to bound cross-tenant/holder correlation? [Completeness, FR-020] <!-- Evaluator: Resolved — added FR-026 (per-tenant/per-product server-held holder-key salt, rotatable; live leases unaffected; no auto-migration across rotation — mirrors E009's activation-salt model) -->
- [X] CHK019 Does a requirement make the GDPR-erasure path for lease records concrete (erasing authority and retention bound) rather than only asserting erasability? [Measurability, FR-020] <!-- Evaluator: Covered by FR-020 + data-model (terminal leases purged by the platform retention path/owner role; app role has NO DELETE; mirrors E009/E013/E014 retention model) -->
- [X] CHK020 Is it specified that no raw hardware identifier appears in any lease row, response, log, or audit entry across every path (acquire/renew/release/reclaim/registry)? [Coverage, FR-018/FR-020/SC-015] <!-- Evaluator: Covered by SC-015 + FR-018 + FR-020 + contracts ("The server NEVER receives, stores, logs, or returns a RAW hardware identifier") -->

## Anti-Replay & Stale-Renew Fencing

- [X] CHK021 Is the single-use acquire token's uniqueness scope and lifetime specified, including whether token reuse after retention purge reopens a replay window? [Completeness, FR-014] <!-- Evaluator: Resolved — amended FR-014 to specify per-tenant uniqueness (UNIQUE (tenant_id, nonce)), retention-lifetime on the lease row (mirrors E009 nonce), and that any post-purge reuse is a fresh cap-checked acquire that cannot resurrect/double-count a seat -->
- [X] CHK022 Does a requirement state the replayed-acquire outcome (returns the original lease, consumes no second seat) distinctly from a first-time acquire? [Clarity, FR-014/SC-011] <!-- Evaluator: Covered by FR-014 + SC-011 + contracts (201 fresh vs 200 idempotent replay) -->
- [X] CHK023 Is the generation-fence stale-renew rejection specified as a security control (a late renew racing a reclaim is refused) with a distinct, non-oracle reason? [Traceability, FR-011/SC-008] <!-- Evaluator: Covered by FR-011 + SC-008 + contracts (uniform 409 lease_not_renewable; generation fence) -->
- [X] CHK024 Are renew and release idempotency guarantees stated so a repeated or replayed call cannot corrupt or double-count the live-seat total? [Coverage, FR-007/FR-008] <!-- Evaluator: Covered by FR-007 (idempotent renew, exactly one seat) + FR-008 (idempotent release, never below zero) -->

## Rate Limiting

- [X] CHK025 Is the rate-limit threshold specified measurably (a defined multiple of heartbeat cadence) rather than the qualitative "sized to admit legitimate heartbeat cadence"? [Measurability, FR-017] <!-- Evaluator: Resolved — amended FR-017 to define the threshold measurably as a per-plan-configurable burst multiple of the key's expected aggregate heartbeat rate (default ≥ 2×), fleet-safe against near-lockstep heartbeats -->
- [X] CHK026 Is the rate-limit key specified as per-API-key so one tenant or key cannot exhaust another's request budget? [Completeness, FR-017] <!-- Evaluator: Covered by FR-017 ("keyed per API key") + contracts (rate-limited per API key) -->
- [X] CHK027 Does a requirement mandate the 429 response carry a Retry-After header and that each limit-exceeded event is audited as a security event? [Traceability, FR-017/SC-014] <!-- Evaluator: Covered by FR-017 (429 rate_limited + Retry-After + audit as security event) + SC-014 -->

## Audit Coverage & Attribution

- [X] CHK028 Does the append-only audit requirement enumerate every operation and every denial reason (including no-entitlement, license-not-active, activation-required, and signer faults), not only the happy path? [Coverage, FR-018] <!-- Evaluator: Resolved — amended FR-018 to enumerate every distinct denial reason and to include a transient signer-fault (503, no seat consumed) on the acquire/renew path -->
- [X] CHK029 Is the audit actor attribution specified for the synthetic sweeper / revoke-reclaim worker (a system actor plus the lease/license id) so automatic reclamations are attributable? [Completeness, FR-018] <!-- Evaluator: Resolved — amended FR-018 to attribute automatic reclamations (sweeper + revoke-reclaim) to a synthetic system/worker actor + lease/license id (mirrors E013/E014 synthetic-actor workers) -->
- [X] CHK030 Is "security event" defined and distinguished from a routine audit entry consistently across the requirements that emit one? [Consistency, FR-016/FR-017/SC-010/SC-013] <!-- Evaluator: Resolved — added a Glossary "Security event" definition (security_event marker on auth/RBAC/CSRF denials, rate-limit breaches, denied force-release) distinct from routine audit entries; referenced by FR-016/FR-017, verified by SC-010/SC-013 -->
- [X] CHK031 Does a requirement state the audit log is append-only/tamper-evident against the application role (no update or delete), not merely additive? [Traceability, FR-018] <!-- Evaluator: Covered by data-model INV-10 + audit_log notes ("Append-only (SELECT+INSERT)"; app role has no UPDATE/DELETE) -->
- [X] CHK032 Is the overage-metering audit entry's content specified (the concurrency level reached) with an explicit exclusion of card data and raw hardware identifiers? [Completeness, FR-013] <!-- Evaluator: Covered by FR-013 ("capturing the concurrency level reached ... without card data or raw hardware identifiers") -->

## Threat Model & Failure Posture

- [X] CHK033 Is the honest-client threat-model boundary (hostile local user out of scope; server authoritative for the seat count) stated in a requirement/assumption a reviewer can rely on to scope residual risk? [Completeness, Assumptions] <!-- Evaluator: Covered by spec.md Assumptions ("honest-client threat model ... a hostile local user is out of scope; the server ... is authoritative for the seat count") -->
- [X] CHK034 Is fail-closed vs fail-open posture assigned per surface (runtime acquire/renew/release fail-closed; reclaim sweeper fail-open) with no surface left unspecified? [Consistency, FR-002/FR-010] <!-- Evaluator: Covered by FR-002 (runtime fail-closed) + FR-010 (sweeper fail-open) + plan.md Error-Handling posture table -->
- [X] CHK035 Is the signer-fault outcome on the acquire/renew path specified as a requirement (fail-closed, no seat consumed) rather than only in the plan's error-handling table? [Coverage, FR-022] <!-- Evaluator: Resolved — amended FR-022 (signer unavailable ⇒ acquire fail-closed no seat/no lease; renew leaves lease+seat unchanged) + added measurable SC-021 -->
- [X] CHK036 Does a requirement specify that activated-devices-only gating fails closed when no valid current activation exists, so an absent activation cannot bypass the gate? [Clarity, FR-025] <!-- Evaluator: Resolved — amended FR-025 (gating on ⇒ acquire fail-closed when no valid current activation resolves; absent/expired/deactivated cannot bypass) + added measurable SC-022 -->
- [X] CHK037 Are denial reasons specified to avoid leaking cross-tenant or license-existence information (uniform, non-enumerable not-found)? [Coverage, FR-019] <!-- Evaluator: Covered by contracts (cross-tenant → uniform 404, never 403; Error schema "no field discloses another tenant's existence") -->
