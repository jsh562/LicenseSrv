# Security Requirements Quality Checklist: Observability and SLOs
**Created**: 2026-07-17 | **Feature**: [spec.md](../spec.md)

## Tenant-Isolation Paged Invariant

- [X] CHK001 Is a requirement defined for what precisely constitutes a "cross-tenant access violation" — the exact principal-vs-resource comparison (authenticated tenant vs the tenant GUC being set vs the target row's tenant) that triggers the page? [Clarity, Spec OR-011/OR-012] <!-- Evaluator: Resolved — OR-011 now defines a violation as any mismatch between the authenticated principal's tenant (auth-seam req.tenant) and the app.current_tenant GUC set at withTenant() -->
- [X] CHK002 Is the assertion point unambiguously specified as a single location, given the spec alternates between "the DB tenant choke point", "`withTenant()`", and "auth seam" — is it one seam or two? [Ambiguity, Spec OR-011 / IP-001] <!-- Evaluator: Resolved — OR-011 now names a single assertion point (the withTenant() choke point) that consumes identity from the auth seam; not two seams -->
- [X] CHK003 Is the constraint that the isolation assertion itself performs no cross-tenant read stated as a testable requirement rather than only an implementation hint? [Completeness, Spec §Edge Cases / HINT-003] <!-- Evaluator: Resolved — OR-011 now normatively requires the assertion MUST NOT perform any cross-tenant read (compares in-memory identities only) -->
- [X] CHK004 Is "immediate page" defined with a measurable bound (detection-to-page latency, evaluation interval) that distinguishes it from burn-rate alerting? [Measurability, Spec OR-012] <!-- Evaluator: Resolved — OR-012 now bounds "immediate" as no burn-rate/for: window, fires on first evaluation cycle, detection-to-page ≤ ~1 min -->
- [X] CHK005 Is the "dedicated security signal" specified concretely enough to build against — its type (counter vs event vs log), name, and the fields it must carry? [Completeness, Spec OR-011] <!-- Evaluator: Resolved — OR-011 now specifies the signal: Prometheus counter tenant_isolation_violation_total (no high-cardinality labels) + structured security log event with request_id, authenticated tenant, attempted/GUC tenant, assertion location, outcome -->
- [X] CHK006 Is there a requirement guaranteeing the isolation security event is never dropped by trace/metric sampling or export batching? [Completeness, Spec §Edge Cases / OR-014] <!-- Evaluator: Covered by spec.md OR-014 — isolation signal reaches Alertmanager via a path independent of trace sampling and batched OTLP export -->
- [X] CHK007 Is the "no false positives on same-tenant traffic" property stated as a requirement, not only as a verification criterion (OBJ3 VC3)? [Coverage, Spec OBJ3] <!-- Evaluator: Resolved — OR-012 now normatively requires no page on same-tenant (matching) access for both live traffic and the canary; OR-011 emits only on mismatch -->
- [X] CHK008 Is the division of responsibility between "RLS blocks" and "the assertion signals" specified — which component is authoritative for the "blocked" outcome asserted in OBJ3 VC1? [Consistency, Spec OBJ3 / HINT-003] <!-- Evaluator: Resolved — OR-011 now states RLS is the authoritative enforcement that blocks; the assertion is the detection/signal path only -->
- [X] CHK009 Are the synthetic canary's parameters specified — run cadence, tenant fixtures used, what outcome counts as a pageable "success", and whether it runs against production? [Completeness, Spec OR-012 / OBJ3] <!-- Evaluator: Resolved — OR-012 now specifies cadence (~60s), dedicated reserved synthetic tenant fixtures, runs against the live control plane, pageable = cross-tenant attempt NOT blocked -->
- [X] CHK010 Is a requirement defined for the canary's own failure mode (canary cannot run) as distinct from a detected breach, so an infra failure is not misread as isolation? [Ambiguity, Spec OR-012] <!-- Evaluator: Resolved — OR-012 now classifies canary execution failure distinctly from a breach: it MUST NOT trigger the isolation page and SHOULD raise a separate lower-severity dead-man alert -->
- [X] CHK011 Is the isolation page's severity/routing relative to the SEV1/SEV2 taxonomy specified, or is it left ambiguous against the burn-rate severities in OR-016? [Consistency, Spec OR-012 / OR-016] <!-- Evaluator: Covered by spec.md OR-016 — tenant-isolation page is SEV1 with defined routing and escalation (auto-escalate if unacknowledged within 10 min) -->

## Secrets and PII Redaction (Logs/Metrics/Traces)

- [X] CHK012 Is the set of "secrets and raw PII" enumerated exhaustively, or does "API keys and license keys" read as non-exhaustive examples leaving other fields (auth headers, DB DSNs, tokens) unspecified? [Ambiguity, Spec OR-004] <!-- Evaluator: Resolved — OR-004 now enumerates a minimum redaction set: API keys, Authorization/x-api-key headers, bearer/session tokens, license keys/token payloads, DB DSNs, OTLP/exporter secrets -->
- [X] CHK013 Does a redaction requirement cover all three signal types consistently, given OR-004 names only logs while the Edge Cases clause requires logs, metrics, AND traces to redact? [Consistency, Spec OR-004 / §Edge Cases] <!-- Evaluator: Covered by spec.md OR-004 — redaction rule set applies consistently across logs, metrics, traces, and the /metrics surface -->
- [X] CHK014 Is "machine fingerprints hashed" specified with the required properties (algorithm, salting, determinism for queryability, irreversibility)? [Clarity, Spec OR-004] <!-- Evaluator: Resolved — OR-004 now requires a deterministic, one-way keyed hash (HMAC-SHA-256 with server-held salt/pepper): correlatable across signals, irreversible, enumeration-resistant -->
- [X] CHK015 Is there a requirement covering leakage via trace span attributes such as pg-instrumentation query text or bound parameters that can carry license keys? [Coverage, Spec OR-013 / §Edge Cases] <!-- Evaluator: Resolved — OR-013 now requires pg auto-instrumentation db.statement/bound-parameter capture to be disabled or redacted; span attributes subject to the OR-004 rule set -->
- [X] CHK016 Is there a requirement covering secrets/PII leaking through error messages, stack traces, or exception payloads captured in logs/traces? [Completeness, Spec OR-004] <!-- Evaluator: Resolved — OR-004 now applies the redaction rule set to error messages, stack traces, and exception payloads in logs and span/exception events -->
- [X] CHK017 Is the behavior on redaction failure defined (drop the field / drop the line vs emit unredacted) so redaction fails closed? [Completeness, Spec OR-004] <!-- Evaluator: Resolved — OR-004 now requires redaction to fail closed: a field that cannot be confidently redacted MUST be dropped rather than emitted in the clear -->
- [X] CHK018 Is there a requirement that the client-supplied correlation header is sanitized before being recorded, so it cannot smuggle secrets/PII or injected content into logs? [Coverage, Spec OR-002] <!-- Evaluator: Resolved — OR-002 now requires the client-supplied tag be validated/sanitized (bounded length, restricted charset, no control/newline chars) before recording -->

## Metrics-Endpoint Exposure Control

- [X] CHK019 Is "access-controlled or bound to a non-public port" an unambiguous single mandate, given AD-001 selects a dedicated port with auth "none" — do OR-005 and AD-001 agree? [Consistency, Spec OR-005 / Plan AD-001] <!-- Evaluator: Covered by spec.md OR-005 + Plan AD-001 — both mandate a single dedicated internal non-public port as the access-control mechanism (not an either/or) -->
- [X] CHK020 Is "non-public port" defined measurably (bind address, network policy, firewall expectation) so a reviewer can judge conformance? [Measurability, Spec OR-005] <!-- Evaluator: Resolved — OR-005 now gives measurable criteria: configurable dedicated port not published on public ingress, internal/private bind address and/or network policy/firewall, never shares the public /v1 listener -->
- [X] CHK021 Is the authentication/authorization model for the metrics port stated as a requirement, or is "none" left as an implicit plan assumption? [Clarity, Spec OR-005 / Plan AD-001] <!-- Evaluator: Resolved — OR-005 now states the model normatively: NO application-level auth; network isolation is the sole access control, so the surface must carry no tenant data or secrets -->
- [X] CHK022 Is the "carries no tenant data or secrets" property of the metrics surface independently verifiable and distinct from the label-cardinality constraint (OR-008)? [Completeness, Spec OR-005] <!-- Evaluator: Covered by spec.md OR-005 (content property: no tenant data/secrets in the exposition) — distinct from OR-008 (label-cardinality), verifiable via plan Security testing tier ("no secrets/PII in signals") + OR-004 /metrics scope -->
- [X] CHK023 Does the fail-open requirement for a metrics-port bind failure acknowledge the security implication of silent metrics unavailability (blindness to breaches)? [Consistency, Spec OR-014 / Plan Error Handling] <!-- Evaluator: Resolved — OR-014 now requires metrics unavailability to raise its own operational alert (Prometheus up==0 / dead-man's switch) so fail-open never becomes a silent blind spot -->

## Exporter-Secret Handling (<VAR>_FILE)

- [X] CHK024 Are the specific exporter secrets that must use `<VAR>_FILE` enumerated (OTLP endpoint token, Collector credentials), or does OR-018 remain generic ("any exporter secret")? [Completeness, Spec OR-018] <!-- Evaluator: Resolved — OR-018 now enumerates the OTLP exporter auth token/header and Collector transport credentials (basic-auth password / mTLS client key) -->
- [X] CHK025 Is the precedence/conflict behavior specified when both the inline var and its `<VAR>_FILE` counterpart are provided? [Ambiguity, Spec OR-018] <!-- Evaluator: Resolved — OR-018 now states <VAR>_FILE takes precedence over inline <VAR> per the established readSecret convention (src/server/config/secrets.ts): readable non-empty file wins; empty/unreadable required secret fails fast -->
- [X] CHK026 Is there a requirement that exporter secrets never appear in the logged config summary or startup output? [Coverage, Spec OR-018 / OR-004] <!-- Evaluator: Resolved — OR-018 now requires exporter/<VAR>_FILE secrets to be absent from the startup/config summary; configSummary remains secret-free (OR-004) -->

## Signing-Key Material Excluded from Telemetry

- [X] CHK027 Is there an explicit, testable requirement that no signing-key material appears in any telemetry, or is this asserted only in the Compliance Check narrative? [Completeness, Spec §Compliance / OR-007] <!-- Evaluator: Resolved — added OR-020: signing-key material MUST be excluded from all telemetry (logs/metrics/traces//metrics), making normative the Principle I guarantee previously only in the Compliance narrative -->
- [X] CHK028 Is the signer span/metric attribute set constrained by a requirement to availability/latency only, excluding key id, key bytes, or signing payload? [Clarity, Spec OBJ4 / OR-007] <!-- Evaluator: Resolved — OR-020 limits signer instrumentation to availability/latency/outcome and forbids key identifiers, key bytes, and signing payload/input -->
- [X] CHK029 Is there a requirement that signer error/exception telemetry cannot leak key material? [Coverage, Spec OR-007 / OBJ4] <!-- Evaluator: Resolved — OR-020 extends the exclusion to signer error, exception, and stack-trace telemetry (no key material or signing input in any captured payload) -->

## Fail-Open vs Security Posture

- [X] CHK030 Is there a requirement clarifying that fail-open applies to telemetry export only and never relaxes the fail-closed tenant-isolation enforcement? [Consistency, Spec OR-014 / OR-011] <!-- Evaluator: Resolved — OR-014 now scopes fail-open strictly to telemetry export/backends and states it NEVER relaxes request-path enforcement (auth, RLS, tenant-isolation), which stays fail-closed -->
- [X] CHK031 Is the delivery guarantee for the isolation page specified when the OTLP/Collector export path is down — does the page still fire independently of fail-open export? [Completeness, Spec OR-012 / OR-014] <!-- Evaluator: Covered by spec.md OR-014 — the isolation page fires even when the Collector/OTLP export is unavailable (independent path) -->
- [X] CHK032 Is there a requirement that fail-open behavior must never suppress, mask, or delay a security-violation signal? [Ambiguity, Spec OR-014 / §Edge Cases] <!-- Evaluator: Resolved — OR-014 now states fail-open MUST NOT suppress, mask, delay, or drop the tenant-isolation security signal -->

## Client-Supplied request_id Trust Boundary

- [X] CHK033 Is "never trusted for security decisions" defined by enumerating the excluded uses (authz, tenant selection, audit identity, rate-limit keys), or left open to interpretation? [Clarity, Spec OR-002 / §Edge Cases] <!-- Evaluator: Resolved — OR-002 now enumerates excluded uses: authorization, tenant selection/resolution, audit identity, and rate-limit keys -->
- [X] CHK034 Is there a requirement to validate/bound the inbound correlation header (length, charset) to prevent log injection or cardinality abuse via the tag? [Completeness, Spec OR-002] <!-- Evaluator: Resolved — OR-002 now bounds the tag (≤128 chars, restricted printable-ASCII charset, no control/newline chars) and forbids its use as a metric label -->
- [X] CHK035 Are the authoritative server-generated `request_id` and the non-authoritative client tag given distinct, specified field names so they cannot be confused downstream? [Clarity, Spec OR-002 / Plan AD-002] <!-- Evaluator: Resolved — OR-002 now names the authoritative field request_id and the non-authoritative client tag client_request_id, which never overwrites or is conflated with request_id -->
- [X] CHK036 Is there a requirement that the non-authoritative client-supplied tag is never used as a metric label (cardinality/injection risk)? [Coverage, Spec OR-002 / OR-008] <!-- Evaluator: Resolved — OR-002 now states the client-supplied tag MUST NEVER be used as a metric label (cross-refs OR-008 cardinality policy) -->

## Cross-Cutting Traceability

- [X] CHK037 Do all security clauses in "Operational Constraints" map to a corresponding OR requirement, with no security constraint left unbacked? [Traceability, Spec §Operational Constraints] <!-- Evaluator: Covered — every security Operational Constraint maps to an OR: no secrets/PII → OR-004, metrics non-public → OR-005, label constraint → OR-008, fail-open → OR-014, isolation invariant/immediate page → OR-011/OR-012/OR-019, cloud-agnostic → OR-017 -->
- [X] CHK038 Is each security-relevant "Edge Cases & Boundaries" clause traceable to a normative OR requirement rather than living only as prose? [Traceability, Spec §Edge Cases] <!-- Evaluator: Covered after amendments — all security edge cases now trace to ORs, incl. the no-cross-tenant-read clause (now OR-011) and the client-tag trust boundary (now OR-002); /metrics → OR-005, labels → OR-008, fail-open → OR-014, redaction → OR-004 -->
- [X] CHK039 Is the tenant-isolation runbook (RR-001) consistent with OBJ3's page semantics and signal definition, so responders act on the same signal the system emits? [Consistency, Spec RR-001 / OBJ3] <!-- Evaluator: Resolved — RR-001 now references the concrete OR-011 signal (tenant_isolation_violation_total-driven SEV1 page) and its log-event fields, so responders and the emitting system share one signal definition -->
