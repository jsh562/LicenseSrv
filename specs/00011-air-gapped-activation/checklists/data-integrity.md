# Requirements Quality Checklist: Data Integrity — Air-Gapped Activation (E010)
**Created**: 2026-07-15 | **Feature**: [spec.md](../spec.md)

## Seat Integrity Through the File Transport

- [X] CHK001 Is "consumes exactly one seat via the shared accounting" defined against a measurable data invariant (seat count delta of exactly +1 on a fresh bind, 0 otherwise), or only asserted qualitatively? [Measurability, SC-003/FR-003] <!-- Evaluator: Covered by spec.md §Success Criteria SC-003/SC-005 + contract AirGapActivateResult `created` (fresh bind +1, replay 0) -->
- [X] CHK002 Is "indistinguishable from an online activation in the seat count and the registry" specified precisely enough to be checkable — which activation-row fields MUST be identical to an online bind and which MAY differ? [Clarity, FR-003/SC-003] <!-- Evaluator: Covered by plan AD-008 + spec.md Key Entities (SAME E009 row, no origin field differs) + FR-026 -->
- [X] CHK003 Do the requirements state that seat-cap enforcement is race-safe across concurrent air-gap submissions AND concurrent online activations competing for the same license's seats? [Completeness, FR-003/FR-004] <!-- Evaluator: Covered by spec.md FR-003 + contract (shared E009 seat lock, race-safe for any transport) -->
- [X] CHK004 Is "distinct machine" defined for seat purposes on the file path (fingerprint-derived identity) so two request files for the same machine cannot consume two seats? [Completeness, FR-003] <!-- Evaluator: Covered by spec.md FR-003 + contract SignalHash/machineId (fingerprint-derived K-of-N identity) -->
- [X] CHK005 Is the moment the `maxActivations` seat snapshot is taken (at file-processing time, server-side) stated, so the seat check cannot rely on any client-supplied claim in the request file? [Clarity, contract seat_limit_reached/FR-003] <!-- Evaluator: Covered by spec.md FR-018 + contract (server-side snapshotted maxActivations at processing) -->
- [X] CHK006 Does a requirement tie the `created` flag to a data invariant (seat count unchanged when `created:false`), rather than leaving it as a transport-only field description? [Measurability, FR-005/contract created] <!-- Evaluator: Covered by spec.md FR-005 + contract created (created:false ⇒ no additional seat) -->

## Idempotency & Replay Correctness

- [X] CHK007 Is "returns the original response file" defined as byte-identical bytes, or only an equivalent credential — is the exact replay obligation stated? [Clarity, FR-005/SC-005] <!-- Evaluator: Covered by contract AirGapActivateResult (BYTE-IDENTICAL original response) + FR-005 -->
- [X] CHK008 Is the idempotency key explicitly the request nonce (and only the nonce), with its required entropy and single-use semantics stated? [Completeness, FR-005/FR-001] <!-- Evaluator: Covered by spec.md FR-005 (keyed on nonce) + FR-020 (≥128-bit, single-use) + FR-001 -->
- [X] CHK009 Is the lifetime/retention of the stored nonce→response mapping defined (how long a replay keeps returning the original), or is it left unbounded? [Completeness, FR-005] <!-- Evaluator: Covered by spec.md FR-021 (nonce retained via activation record for the life of the activation) -->
- [X] CHK010 Is the equivalence rule that separates an idempotent replay (`created:false`) from a forged reuse (`nonce_replayed`) specified — which request fields must match the original for it to count as the same activation? [Clarity, FR-005/contract nonce_replayed] <!-- Evaluator: Covered by spec.md FR-005 + contract nonce_replayed (same request file ⇒ replay; nonce reused for a DIFFERENT activation ⇒ refused) -->
- [X] CHK011 Are cross-transport nonce collisions addressed — what happens if a nonce already consumed by an online E009 activation is presented in an air-gap request file, and vice versa? [Coverage, FR-005] <!-- Evaluator: Resolved — added FR-024 (nonce unique per tenant across ALL transports; shared E009 nonce store, no separate namespace) to spec.md -->
- [X] CHK012 Is the interaction between the freshness window and the nonce lifetime defined — can a request be stale-refused yet its nonce still eligible for idempotent replay, or does staleness gate replay? [Consistency, FR-005/FR-008] <!-- Evaluator: Covered by spec.md FR-021 + contract stale_request (freshness gates only the FIRST processing; an already-processed file always replays regardless of age) -->
- [X] CHK013 If the original credential has since expired or its signing key rotated, does the spec state whether a replay re-returns the ORIGINAL response bytes or a freshly re-signed credential? [Completeness, FR-005/FR-006] <!-- Evaluator: Covered by contract AirGapActivateResult (byte-identical original bytes, not re-signed) + FR-016 (overlapping keyring keeps rotated-key credential valid) -->
- [X] CHK014 Is the double-submit race for the SAME request file specified to yield exactly one seat and one activation row (nonce serialization), never a partial or duplicated bind? [Coverage, FR-005] <!-- Evaluator: Covered by spec.md FR-005 (nonce store-and-replay serialization) + FR-003 (race-safe seat lock) -->

## K-of-N Binding & Drift Through the File

- [X] CHK015 Does the request-file format carry the fingerprint in the same salted-signal-hash shape E009 online activation consumes, so the binding is computed identically? [Consistency, FR-001/FR-003] <!-- Evaluator: Covered by spec.md FR-001 + contract SignalHash + plan Integration Points (request signals map into activate()) -->
- [X] CHK016 Are the minimum-signals threshold and the K-of-N drift-tolerance re-match rule stated as requirements, or only referenced as inherited from E009 without a stated value source? [Completeness, FR-009/contract insufficient_signals] <!-- Evaluator: Covered by spec.md FR-020 (min signals = E009 K-of-N threshold, default 3) + FR-009 -->
- [X] CHK017 Does the spec state that a returning machine whose fingerprint drifted re-matches the SAME seat (no new seat) identically through the file transport as online? [Completeness, FR-003] <!-- Evaluator: Resolved — added FR-025 (drift re-match reuses the SAME seat, no new seat, on the air-gap transport) to spec.md -->
- [X] CHK018 If an offline machine re-imports after a drift re-match, is it specified whether a new seat is consumed and which response (fresh vs original) the operator receives? [Coverage, FR-003/FR-005] <!-- Evaluator: Resolved — added FR-025 (no new seat; returns created:false with a REFRESHED response, distinct from the byte-identical same-nonce replay) to spec.md -->
- [X] CHK019 Is the refusal precedence between `insufficient_signals` and other outcomes (seat, license status, stale) defined so the data outcome for a borderline file is deterministic? [Clarity, contract status-precedence/FR-009] <!-- Evaluator: Covered by contract STATUS PRECEDENCE + plan HINT-003 (strictly ordered file-layer then business rules) -->

## No-New-Entity & Registry Consistency

- [X] CHK020 Is "no air-gap column / no migration" (plan AD-008) stated consistently with FR-003 — is it a requirement that NOTHING persisted in the activation row distinguishes air-gap from online? [Consistency, plan AD-008/FR-003] <!-- Evaluator: Covered by plan AD-008 + spec.md FR-003 + Key Entities (no new persisted entity; SAME activation row) -->
- [X] CHK021 If nothing in the row distinguishes origin, is "appears in the same registry" (SC-003) satisfiable without a provenance marker — is the absence of an origin field an intentional, stated decision? [Consistency, SC-003/FR-003] <!-- Evaluator: Resolved — added FR-026 (absence of an origin field is intentional; registry parity holds because the row is the same shape as online) to spec.md -->
- [X] CHK022 Is the audit log (`airgap.activated`) specified as the single authoritative place air-gap provenance is recorded, consistent with the no-new-entity decision? [Consistency, FR-012/plan AD-008] <!-- Evaluator: Resolved — added FR-026 (provenance recorded SOLELY in the airgap.activated audit entry, not the data model) to spec.md -->
- [X] CHK023 Are the response-file metadata fields (activationId, expiresAt, keyId, machineId) defined as derived from the E009 activation row, so there is one source of truth rather than a parallel copy? [Traceability, contract ResponseFileEnvelope/FR-006] <!-- Evaluator: Covered by contract ResponseFileEnvelope (activationId = E009 UUID; machineBoundKey = SAME artifact online returns) + spec.md FR-006/FR-022 -->

## Fail-Closed & No-Partial-State

- [X] CHK024 For each refusal class (seat full, non-active, stale, malformed, unknown-version, too-few-signals, signer-unavailable), does a requirement assert NO activation row and NO partial seat is left? [Completeness, FR-004/FR-007/FR-008/FR-009] <!-- Evaluator: Resolved — added FR-028 (uniform fail-closed: NO activation row and NO partial seat on ANY refusal class, incl. oversize) to spec.md -->
- [X] CHK025 Is the atomicity boundary defined — is seat reservation + credential signing + row insert one transaction that rolls back fully on signer failure (503)? [Clarity, contract signer_unavailable/FR-006] <!-- Evaluator: Covered by spec.md FR-023 + contract SignerUnavailable (single transaction, rolls back on 503) -->
- [X] CHK026 Do the requirements mandate file-layer validation (decode, version, freshness) BEFORE any seat or row mutation, so a rejected file can never leave state? [Consistency, plan HINT-003/FR-007] <!-- Evaluator: Resolved — added FR-028 (file-layer validation runs BEFORE the E009 activate() transaction) to spec.md -->
- [X] CHK027 Is it stated that a truncated or partially-decodable request file cannot produce a partially-populated activation record? [Completeness, FR-007] <!-- Evaluator: Covered by spec.md FR-007 (truncated ⇒ processing nothing, no partial activation) + FR-028 -->

## PII Minimization & Retention at the Data Layer

- [X] CHK028 Does a requirement bound what is persisted from the files to only salted hashes / pseudonymous machineId, prohibiting raw hardware identifiers in storage, logs, AND audit? [Completeness, FR-010/SC-009] <!-- Evaluator: Covered by spec.md FR-010 (files/storage/logs) + FR-012 (audit) + SC-009 -->
- [X] CHK029 Is it stated that the nonce and raw fingerprint signal values are never persisted in the audit log or error details (only that a request was processed)? [Clarity, FR-012/contract audit] <!-- Evaluator: Covered by contract AUDIT + Error + nonce (never the nonce or fingerprint values in audit/error) + spec.md FR-012 -->
- [X] CHK030 Is GDPR retention/erasure of air-gap-originated activation rows and their salted hashes explicitly inherited from E009, or left as an unstated assumption? [Coverage, FR-010/plan follow-up] <!-- Evaluator: Resolved — added FR-027 (air-gap rows are SAME E009 records under the E009 retention/erasure path, no separate handling) to spec.md -->
- [X] CHK031 Is the response file's `machineId` required to be derived deterministically from the fingerprint so identity/erasure semantics match the E009 record it references? [Traceability, contract machineId/FR-010] <!-- Evaluator: Covered by contract machineId (salted hash derived from the fingerprint) + plan AD-008 (same E009 record) + spec.md FR-027 -->

## Tenant Isolation & Audit Integrity

- [X] CHK032 Is forced-RLS tenant scoping on every air-gap read/write path — including the audit write — stated as a requirement rather than only a plan HOW? [Completeness, FR-011/plan] <!-- Evaluator: Covered by spec.md FR-011 (isolate air-gap processing by tenant, fail-closed) + FR-012 (per-request audit); RLS is the correctly-deferred HOW -->
- [X] CHK033 Is the cross-tenant outcome defined at the data layer as not-found with no row and no disclosure, and is this consistent between FR-011, SC-010, and the contract's `license_not_found`? [Consistency, FR-011/SC-010/contract license_not_found] <!-- Evaluator: Covered by spec.md FR-011 + SC-010 + contract license_not_found/Error (cross-tenant ⇒ 404, never 403, no disclosure) -->
- [X] CHK034 Does a requirement state the audit log is append-only for all three outcomes (activation, idempotent replay, each refusal) with no update or delete path? [Completeness, FR-012/SC-011] <!-- Evaluator: Covered by spec.md FR-012 + SC-011 (append-only entry for activation, replay, and each refusal) -->
- [X] CHK035 Is the required audit-entry content (actor, action, reason) enumerated and explicitly bounded to exclude raw identifiers, secrets, the nonce, and fingerprint values? [Clarity, FR-012/SC-011] <!-- Evaluator: Covered by spec.md FR-012 (actor/action/reason, no raw ids/secrets) + contract AUDIT (never the nonce or fingerprint values) -->
- [X] CHK036 Is the rate-limited (429) throttled attempt required to be audited even though no file was parsed, and is the audit data captured for it defined? [Coverage, FR-013/SC-012] <!-- Evaluator: Covered by spec.md SC-012 + FR-013 + FR-012 + contract RateLimited/AUDIT (throttled attempt audited pre-parse with actor/action/reason) -->
