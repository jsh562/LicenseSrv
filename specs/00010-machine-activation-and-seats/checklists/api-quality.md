# API Quality: Machine Activation & Seat Enforcement

**Created**: 2026-07-13 | **Feature**: [spec.md](../spec.md) | **Domain**: API Quality | **Depth**: Standard | **Audience**: Reviewer (PR)

## Auth Plane Specification

- [X] CHK001 Is the runtime-plane credential (`X-API-Key` header, explicitly not a cookie) specified as the sole authenticator for every `/v1` operation? [Clarity, Contract §securitySchemes.apiKey / FR-002] <!-- Evaluator: Covered by Contract §securitySchemes.apiKey + info AUTH PLANES (X-API-Key header, not cookie); both /v1 ops declare `security: apiKey`. FR-002. -->
- [X] CHK002 Is the `activate` scope stated as a distinct, testable precondition for BOTH `POST /v1/activations` and `DELETE /v1/activations/{activationId}`? [Completeness, Contract §x-rbac.requiredScope / FR-002] <!-- Evaluator: Covered by Contract — both /v1 operations carry `x-rbac: { requiredScope: activate }`. FR-002. -->
- [X] CHK003 Is the split between "no resolvable tenant context → 401 unauthorized" and "resolvable key lacking `activate` scope → 403 forbidden" defined so the two runtime failure modes cannot be conflated? [Clarity, Contract §securitySchemes.apiKey / Plan §Error Handling] <!-- Evaluator: Covered by Contract §securitySchemes.apiKey + info AUTH PLANES (no resolvable tenant → 401; resolvable key lacking activate → 403). -->
- [X] CHK004 For the admin plane, are the required credential (`admin_session` cookie), the minimum RBAC role per operation (viewer for GET, admin for deactivate), and the CSRF requirement each specified without overlap or gap? [Completeness, Contract §sessionCookie / §x-rbac / FR-012] <!-- Evaluator: Covered by Contract §sessionCookie + x-rbac.minRole (viewer on GET, admin on deactivate) + §parameters.CsrfToken. FR-012. -->
- [X] CHK005 Is the CSRF (`X-CSRF-Token`) requirement scoped unambiguously to state-changing `/admin` operations only — required on the operator deactivate, not on GET, and never on the `/v1` plane? [Clarity, Contract §parameters.CsrfToken] <!-- Evaluator: Covered by Contract §parameters.CsrfToken — required on state-changing /admin, not on GET, not on /v1 (header-credential plane). -->
- [X] CHK006 Is it specified that the `unauthorized` code and 401 status apply uniformly across BOTH auth planes, per the plan's cross-plane decision? [Consistency, Plan §HINT-005 / Contract §responses.Unauthorized] <!-- Evaluator: Covered by Plan HINT-005 (401 unauthorized across both planes) + Contract §responses.Unauthorized (noApiKey + noSession examples). -->
- [X] CHK007 Is the precedence order among 401 / 403 / 404 defined for cases where more than one could apply (e.g., an unauthenticated caller referencing a cross-tenant id)? [Clarity, Contract §responses.NotFound / §Error] <!-- Evaluator: Resolved — added STATUS PRECEDENCE note to Contract info (auth 401 → authz 403 → resolution 404; unauthenticated + cross-tenant id → 401). -->

## Error Model Completeness & Distinctness

- [X] CHK008 Does every refusal reason named in the spec edge cases (seat limit, replay, non-active license, not found, insufficient signals, unauthorized, forbidden) map to exactly one enumerated `code`? [Completeness, Contract §Error.code enum / Spec §Edge Cases] <!-- Evaluator: Covered by Contract §Error.code enum — each named refusal maps to exactly one code (seat_limit_reached, nonce_replayed, license_not_active, not_found/license_not_found, insufficient_signals, unauthorized, forbidden). -->
- [X] CHK009 Is each error `code` distinct and testable, with a defined `details` shape that distinguishes it from every other refusal? [Measurability, Contract §Error.details] <!-- Evaluator: Covered by Contract §Error.details — per-code details shape enumerated for every code. -->
- [X] CHK010 Are the two 404 codes (`license_not_found` on activate vs `not_found` on deactivate/registry) distinguished by an explicit rule stating when each applies? [Clarity, Contract §Error.code enum] <!-- Evaluator: Covered by Contract §Error.code enum comments — license_not_found only on activate (unknown/foreign license); not_found on admin plane + runtime deactivate (unknown activation). -->
- [X] CHK011 Is the per-`code` `details` payload specified AND constrained never to echo a raw hardware identifier, signing-key material, signed token, or nonce? [Completeness, Contract §Error.details / FR-006] <!-- Evaluator: Covered by Contract §Error + §Error.details — "never carries a raw identifier, key material, token, or nonce"; nonce_replayed details never echo the nonce. FR-006. -->
- [X] CHK012 Is a rate-limit refusal response (status code and error `code`) defined for the runtime surface FR-013 mandates, including whether a `Retry-After` header is returned? [Coverage, FR-013 / Contract §/v1/activations] <!-- Evaluator: Covered by Contract §responses.RateLimited — 429 rate_limited + Retry-After header on both runtime routes (added by Security pass). FR-013/FR-020. -->
- [X] CHK013 Is the response for a syntactically invalid `licenseKey` (a malformed LIC1 token) mapped to a specific code (`validation_error` vs `license_not_found`) rather than left ambiguous? [Clarity, Contract §schemas.LicenseKeyToken] <!-- Evaluator: Resolved — added VALIDATION MAPPING to Contract §schemas.LicenseKeyToken (structurally invalid → 400 validation_error; well-formed but unknown/foreign → 404 license_not_found). -->

## Status Code Correctness

- [X] CHK014 Is the 201-vs-200 distinction for `POST /v1/activations` (fresh bind vs idempotent re-use) defined by an unambiguous, testable condition? [Measurability, Contract §/v1/activations responses] <!-- Evaluator: Covered by Contract §/v1/activations — 201 = new machine bound (seat consumed); 200 = K-of-N drift match or same-nonce replay (no new seat). -->
- [X] CHK015 Is each 409 sub-case (`seat_limit_reached`, `nonce_replayed`, `license_not_active`) bound to 409, `insufficient_signals` to 400, and the not-found cases to 404 — with no code appearing under two statuses? [Consistency, Contract §Error.code enum / Plan §Error Handling Strategy] <!-- Evaluator: Covered by Contract §Error.code enum status comments — each code carries exactly one HTTP status; matches Plan §Error Handling Strategy. -->
- [X] CHK016 Is the deactivate success status specified distinctly per plane (runtime DELETE → 204 no body; admin POST → 200 `DeactivationResult`), and is that divergence justified? [Consistency, Plan §API Surface Summary / Contract §deactivate operations] <!-- Evaluator: Resolved — added STATUS DIVERGENCE justification to Contract §deactivateLicenseActivation (204 RESTful delete vs 200 console action returning DeactivationResult; both idempotent). -->
- [X] CHK017 Is the `503 signer_unavailable` failure from the plan's error strategy represented in the contract's response set (and `code` enum) for activation, or is its omission intentional and documented? [Completeness, Plan §Error Handling Strategy / Contract §Error.code enum] <!-- Evaluator: Resolved — added signer_unavailable (503) to Contract §Error.code enum, POST /v1/activations responses + refusals, and new §responses.SignerUnavailable (fail-closed, no seat). -->

## Idempotency Semantics

- [X] CHK018 Is the idempotent-replay (200) body of `POST /v1/activations` fully specified — returning the ORIGINAL activation's fields plus a refreshed `machineBoundKey`? [Completeness, Contract §/v1/activations 200 / FR-009] <!-- Evaluator: Covered by Contract §/v1/activations 200 (ActivationResult schema, "reused" example, refreshed machineBoundKey); reinforced by the CHK019 machineId clarification. FR-009. -->
- [X] CHK019 Is the K-of-N drift re-match (200) response's `machineId` defined as the originally bound identity rather than the drifted request's identity? [Clarity, Contract §/v1/activations 200 example / FR-005] <!-- Evaluator: Resolved — clarified in Contract §/v1/activations 200 that on drift re-match id/machineId/activatedAt are the ORIGINAL bound activation's (stored machine_id), not the drifted request's. FR-005. -->
- [X] CHK020 Is deactivate idempotency specified to yield the same success outcome whether the activation was active or already deactivated (204 runtime / 200 admin), never driving the seat count below zero? [Measurability, FR-011 / Contract §deactivate operations] <!-- Evaluator: Covered by Contract — DELETE returns 204 and admin POST returns 200 "whether active or already deactivated, never below zero". FR-011. -->
- [X] CHK021 Is the condition distinguishing a same-nonce idempotent replay (200) from a nonce replayed to forge a different activation (409 `nonce_replayed`) stated testably? [Clarity, FR-009 / Contract §/v1/activations] <!-- Evaluator: Covered by Contract §/v1/activations + info ANTI-REPLAY NONCE — same nonce for same (license, machine) → 200; nonce replayed to forge a different activation → 409 nonce_replayed. FR-009. -->
- [X] CHK022 Is the nonce uniqueness scope stated consistently (data-model `UNIQUE (tenant_id, nonce)` per-tenant vs the contract's "same (license, machine)" phrasing) so the replay-vs-idempotent boundary is unambiguous? [Consistency, Data-model §6 / Contract §schemas.ActivateRequest.nonce] <!-- Evaluator: Resolved — added UNIQUENESS SCOPE note to Contract §ActivateRequest.nonce reconciling per-tenant DB uniqueness with the (license, machine) replay-vs-forge boundary (data-model §6). -->

## Response-Shape Invariants

- [X] CHK023 Is it stated as a requirement that `machineBoundKey` is returned ONLY by `POST /v1/activations` (both 201 and 200) and NEVER by the admin registry? [Completeness, Contract §schemas.MachineBoundKey / §schemas.Activation] <!-- Evaluator: Covered by Contract §schemas.MachineBoundKey ("Returned ONLY by POST /v1/activations; never in the admin registry") + info SECRECY invariants; registry Activation omits it. FR-018. -->
- [X] CHK024 Is it specified that no read response (registry or otherwise) ever exposes raw signal hashes or the signing key? [Coverage, Contract §schemas.Activation / SC-011] <!-- Evaluator: Covered by Contract §schemas.Activation ("NO machineBoundKey, NO raw signal hashes") + info SECRECY & PII invariants (signing key never exposed). SC-011. -->
- [X] CHK025 Are the `required` field sets of `ActivationResult` vs registry `Activation` enumerated so a reviewer can confirm the registry omits the credential while the activation result includes it? [Clarity, Contract §schemas.ActivationResult / §schemas.Activation] <!-- Evaluator: Covered by Contract — ActivationResult.required includes machineBoundKey; Activation.required omits it (no machineBoundKey property at all). -->
- [X] CHK026 Is the registry list bound (hard cap 1000, no pagination) specified, together with the defined behavior when a license exceeds 1000 activations? [Completeness, Contract §schemas.ActivationRegistry / Plan AD-009] <!-- Evaluator: Resolved — added >1000 behavior to Contract §ActivationRegistry.activations (return most-recent 1000 by activatedAt desc; seatsUsed/seatLimit still reflect true totals). Plan AD-009. -->
- [X] CHK027 Is `seatsUsed` defined with consistent point-in-time semantics ("after this call" in `ActivationResult` vs "active count" in the registry) so the two cannot disagree for the same state? [Consistency, Contract §schemas.ActivationResult / §schemas.ActivationRegistry] <!-- Evaluator: Covered by Contract — both define seatsUsed as the count of the license's ACTIVE activations (never exceeds seatLimit); "after this call" is the same active count at the mutation's commit point. -->
- [X] CHK028 Is the `keyId` field in `ActivationResult` traceable to a spec/plan requirement, or is it an unmapped contract-only addition? [Traceability, Contract §schemas.ActivationResult.keyId] <!-- Evaluator: Resolved — traced keyId to FR-018 (the opaque signing-key id returned alongside the credential) in Contract §ActivationResult.keyId; informational, never key material. -->

## Request Validation & Parameters

- [X] CHK029 Is the "exactly one of `licenseKey` or `licenseId`" rule specified unambiguously and testably (rejecting both-present and neither-present), not left to interpretation? [Measurability, Contract §schemas.ActivateRequest.oneOf] <!-- Evaluator: Covered by Contract §ActivateRequest.oneOf (required licenseKey XOR required licenseId) + additionalProperties:false — both-present fails oneOf (two matches), neither-present fails (zero matches). -->
- [X] CHK030 Are the fingerprint `signals` array bounds (minItems, maxItems, uniqueItems) specified, and is the relationship between the schema minimum and the server-configured minimum-N (`insufficient_signals`) delineated as schema-vs-business validation? [Clarity, Contract §schemas.Fingerprint / FR-016] <!-- Evaluator: Covered by Contract §schemas.Fingerprint (minItems:1, maxItems:32, uniqueItems:true) + description delineating schema minimum vs the server-configured minimum-N business rule (insufficient_signals). FR-016. -->
- [X] CHK031 Is the `nonce` format constraint (length range, character set, and the >=128-bit entropy expectation) specified? [Completeness, Contract §schemas.ActivateRequest.nonce / FR-009] <!-- Evaluator: Covered by Contract §ActivateRequest.nonce (minLength:16, maxLength:200, pattern ^[A-Za-z0-9_-]+$) + >=128-bit single-use expectation (FR-021, now MUST). -->
- [X] CHK032 Are the path parameters `licenseId` and `activationId` constrained to `format: uuid`, with the response for a malformed UUID path value defined (`validation_error` / 400)? [Completeness, Contract §parameters / §responses.BadRequest] <!-- Evaluator: Resolved — params are format:uuid; added 400 BadRequest (validation_error) to both admin operations so a malformed UUID path has a defined response on every path operation (runtime DELETE already had it). -->
- [X] CHK033 Is the `Location` header required on the 201 creation response, and is its absence on the 200 idempotent-replay response intentional and specified? [Clarity, Contract §/v1/activations 201 / 200] <!-- Evaluator: Resolved — marked the 201 Location header `required: true` and documented its deliberate absence on the 200 idempotent-replay response (existing activation, not newly created). -->
- [X] CHK034 Is the `SignalHash` value format (pattern and fixed length) specified so a reviewer can confirm raw hardware identifiers cannot be submitted as signals? [Measurability, Contract §schemas.SignalHash / FR-006] <!-- Evaluator: Covered by Contract §schemas.SignalHash (pattern ^[A-Za-z0-9_-]{43}$ = base64url of a 32-byte digest, fixed length); raw identifiers never transmitted. FR-006. -->

## Versioning, Content Type & Cross-Artifact Traceability

- [X] CHK035 Is the request/response media type (`application/json`) and any content-negotiation or charset expectation stated as a requirement across all operations? [Completeness, Contract §paths content] <!-- Evaluator: Resolved — added a MEDIA TYPE note to Contract info (application/json UTF-8 on every operation, no content negotiation; non-JSON → 400 validation_error). -->
- [X] CHK036 Is an API versioning strategy specified consistently (versioned `/v1` runtime prefix vs unversioned `/admin`), including how breaking changes are introduced? [Consistency, Contract §paths] <!-- Evaluator: Resolved — added a VERSIONING note to Contract info (runtime versioned /v1, admin/console unversioned by platform convention; breaking runtime changes via a new /v2 prefix). -->
- [X] CHK037 Does every functional requirement FR-001..FR-016 map to at least one endpoint, response, or documented behavior in the contract? [Traceability, Contract §paths vs Spec §Requirements] <!-- Evaluator: Covered by Contract — FR-001..016 each referenced in a path/response/behavior (activate, activate scope, seat lock/refuse, K-of-N, hashes-only, offline cred, license_not_active, nonce, deactivate, idempotent deactivate, registry, rate limit, audit, 404 tenant scope, insufficient_signals). -->
- [X] CHK038 Does the contract's API surface match the plan's API Surface Summary (methods, paths, auth, request/response types) with no divergence? [Consistency, Plan §API Surface Summary / Contract §paths] <!-- Evaluator: Covered by Contract §paths vs Plan §API Surface Summary — same four operations, auth planes, and req/res types; the added 503/429 codes align with Plan §Error Handling Strategy. -->
- [X] CHK039 Is each success criterion SC-001..SC-012 that implies an API-observable outcome traceable to a specific response, code, or field in the contract? [Traceability, Spec §Success Criteria / Contract §schemas] <!-- Evaluator: Covered by Contract — SC-001 (machineBoundKey), SC-002/003 (seat_limit_reached + seatsUsed), SC-004 (license_not_active), SC-005/006 (204/200 idempotent deactivate), SC-007/008 (200 drift vs 201 new), SC-009 (403 forbidden security event), SC-010 (409/200 nonce), SC-011 (pseudonymous machineId), SC-012 (404 cross-tenant). -->
- [X] CHK040 Is the credential field name consistent across artifacts (API `machineBoundKey` vs DB/plan `machine_bound_token` vs spec "machine-bound credential"), or is the divergence documented as an intentional API-vs-DB naming convention? [Consistency, Contract §schemas.MachineBoundKey / Data-model §2 / Plan §API Surface Summary] <!-- Evaluator: Resolved — added a CREDENTIAL NAMING note to Contract info mapping machineBoundKey (API camelCase) = machine_bound_token (DB snake_case, data-model §2) = "machine-bound credential" (prose) as one intentional artifact, not a rename. -->
