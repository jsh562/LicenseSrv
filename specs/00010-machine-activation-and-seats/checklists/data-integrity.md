# Data Integrity Checklist: Machine Activation & Seat Enforcement
**Created**: 2026-07-13 | **Feature**: [spec.md](../spec.md)

## Seat Counting & Race Safety

- [X] CHK001 Is the invariant "active activations per license never exceed max_activations" stated as a testable requirement independent of any locking mechanism? [Measurability, FR-003 / SC-002] <!-- Evaluator: Covered by spec.md FR-003 + SC-002 (invariant stated without reference to a locking mechanism) -->
- [X] CHK002 Is the source of truth for seat usage unambiguously defined (a derived count of active activations vs. a stored counter), with no second definition elsewhere? [Clarity, data-model §4] <!-- Evaluator: Covered by data-model §4 + §1 note + spec Key Entities (seat usage = COUNT(*) of active activations under the FOR UPDATE lock; no stored counter, single definition) -->
- [X] CHK003 Do the requirements specify the concurrency outcome precisely — for S free seats, exactly S of any number of concurrent attempts succeed? [Measurability, SC-002] <!-- Evaluator: Covered by spec.md SC-002 -->
- [X] CHK004 Is the serialization boundary (the per-license lock scope covering count+insert) captured as a requirement or design constraint, not left implicit? [Traceability, plan AD-002 / data-model §4] <!-- Evaluator: Covered by plan AD-002 + HINT-003 + data-model §4 (SELECT ... FOR UPDATE on the license row across count+insert) -->
- [X] CHK005 Do the requirements distinguish the partial-unique active-seat index as a backstop from the lock as the primary guard, so the two are not treated as redundant or contradictory? [Consistency, data-model §4 / §8] <!-- Evaluator: Covered by data-model §4 ("second line of defence") + §8 App-layer invariants ("backstop, not the primary guard") -->
- [X] CHK006 Is "over-allocation" / "no partial activation" defined measurably (no observable state where active count exceeds the cap, and a refused attempt records no row)? [Measurability, FR-004 / SC-003] <!-- Evaluator: Covered by spec.md FR-004 + SC-003 (refused with no record) and FR-003 (count never exceeds cap) -->

## Seat Consistency & Deactivation Lifecycle

- [X] CHK007 Is the deactivation state machine (active↔deactivated) fully specified for every transition, including idempotent self-loops in both directions? [Completeness, data-model §7 / FR-010 / FR-011] <!-- Evaluator: Covered by data-model §7 state table (all transitions incl. both idempotent self-loops + reactivation branch) -->
- [X] CHK008 Do the requirements state that deactivating an already-deactivated or unknown activation succeeds without error? [Completeness, FR-011 / SC-006] <!-- Evaluator: Covered by spec.md FR-011 + SC-006 (idempotent success) -->
- [X] CHK009 Is the "never drives the active-seat count below the true active count (never negative)" property expressed as a measurable invariant? [Measurability, FR-011 / SC-006] <!-- Evaluator: Covered by spec.md FR-011 ("never below zero") + SC-006 ("never below the true active count") -->
- [X] CHK010 Are reactivation-after-deactivation semantics defined unambiguously (a fresh active row vs. reuse of the retained deactivated row)? [Clarity, data-model §7] <!-- Evaluator: Covered by data-model §7 + §2 deactivated_at note (reactivation records a fresh active row; prior deactivated row retained as history) -->
- [X] CHK011 Is the immediate reusability of a freed seat specified with a defined ordering guarantee relative to a subsequent activation? [Clarity, SC-005] <!-- Evaluator: Covered by spec.md SC-005 + data-model §7 ordering note (deactivate takes the shared per-license lock; a subsequent activation re-counts the freed seat under the lock) -->
- [X] CHK012 Are concurrent deactivate + activate interleavings for the same license defined (e.g., can a deactivation racing an at-limit activation free a seat the activation may then claim)? [Completeness, GAP — not stated in spec/data-model §7] <!-- Evaluator: Resolved — added a serialization note to data-model §7: deactivate acquires the same per-license SELECT ... FOR UPDATE lock as activate, so the two serialize deterministically (a committed deactivate frees a seat the queued activate then re-counts and may claim) -->

## Fingerprint Match Determinism

- [X] CHK013 Is the K-of-N match rule stated deterministically (cardinality of signal-hash intersection ≥ K) so identical inputs always yield the same match outcome? [Clarity, FR-005 / data-model §5] <!-- Evaluator: Covered by data-model §5 (cardinality(intersection) ≥ K) + spec FR-005 -->
- [X] CHK014 Is the outcome defined when a request matches more than one existing active activation by ≥K signals (tie-break / precedence)? [Completeness, GAP — data-model §5 assumes a single candidate] <!-- Evaluator: Resolved — added a deterministic multi-match tie-break to data-model §5: exact machine_id wins first, else the highest-overlap active row, ties broken by most-recently-active (updated_at, then activated_at, then id) -->
- [X] CHK015 Is the machine_id derivation specified deterministically (salted hash of the full sorted signal set) so a returning machine reproduces the same canonical identity? [Clarity, data-model §2 machine_id] <!-- Evaluator: Covered by data-model §2 machine_id ("salted hash of the full, sorted signal set") + DDL comment -->
- [X] CHK016 Is the minimum-signal threshold and its relationship to N/K defined unambiguously (server config default vs. a per-request client choice)? [Consistency, FR-016 / Assumptions] <!-- Evaluator: Covered by spec FR-016 + Assumptions (server-config default, not per-request) + data-model §5/§8 invariant (4) -->
- [X] CHK017 Is the default K-of-N (3-of-5) stated as a server-config default and disambiguated from the DB-enforced fp_min ∈ [1,N] bound? [Consistency, FR-005 / data-model §8] <!-- Evaluator: Covered by spec FR-005 (default 3-of-5) + data-model §2 fp_min ("DB floors K ≥ 1, caps K ≤ N; default is server config, not per-request") -->
- [X] CHK018 Is a machine sharing fewer than K signals unambiguously required to be treated as a new activation (consuming a seat only if one is free)? [Clarity, FR-005 / SC-008] <!-- Evaluator: Covered by spec SC-008 + Edge Cases + US3 scenario 3 + data-model §5 "New machine" row -->
- [X] CHK019 Is the ordering between the exact machine_id fast path and the K-of-N overlap match defined so the resolution is deterministic? [Completeness, data-model §5] <!-- Evaluator: Covered by data-model §5 (exact machine_id fast path precedes K-of-N overlap; precedence made explicit by the new tie-break note) -->

## Constraint Completeness

- [X] CHK020 Is "at most one active activation per (tenant, license, machine)" stated as a data invariant in the requirements, not only as an index definition? [Traceability, Key Entities / data-model §8] <!-- Evaluator: Covered by spec Key Entities invariant ("at most one active activation per (license, machine identity)") + data-model §1/§8 -->
- [X] CHK021 Is the nonce uniqueness scope defined (per-tenant `(tenant_id, nonce)`) and justified against cross-tenant collision risk? [Clarity, data-model §6 / §8] <!-- Evaluator: Covered by data-model §6 ("per-tenant uniqueness matches RLS scope; cross-tenant collision of a 128-bit nonce is negligible") + §8 UNIQUE (tenant_id, nonce) -->
- [X] CHK022 Is the composite FK to license (including tenant_id) required so an activation can never bind another tenant's license? [Completeness, data-model §2 / §8] <!-- Evaluator: Covered by data-model §2 license_id (composite FK (tenant_id, license_id)) + Conventions + §8 + DDL -->
- [X] CHK023 Is the fp_min ∈ [1, cardinality(signal_hashes)] constraint specified with defined behavior at both bounds (K ≥ 1 floor, K ≤ N cap)? [Completeness, data-model §8] <!-- Evaluator: Covered by data-model §8 CHECK (fp_min > 0 AND fp_min <= cardinality(signal_hashes)) + §2 ("floors K ≥ 1, caps K ≤ N") -->
- [X] CHK024 Is the snapshot-vs-live distinction defined for both max_activations (seat cap) and license status (activation gate) — which value governs each check? [Clarity, FR-003 / FR-008 / data-model §10] <!-- Evaluator: Covered by spec FR-003 (max_activations snapshotted at issuance) + FR-008 (live status gate) + data-model §4/§8/§10 -->
- [X] CHK025 Are the nullability and lifecycle of machine_bound_token defined (transient null between seat-claim insert and sign, refreshed on re-activation)? [Completeness, data-model §2] <!-- Evaluator: Covered by data-model §2 machine_bound_token ("null only transiently between seat-claim INSERT and sign UPDATE ...; refreshed on re-activation") -->

## Nonce & Idempotency Integrity

- [X] CHK026 Is the same-nonce retry outcome defined precisely (returns the original activation result, consumes no second seat)? [Measurability, FR-009 / SC-010] <!-- Evaluator: Covered by spec FR-009 + SC-010 + data-model §6 (same-request retry replays the original result, no second seat) -->
- [X] CHK027 Is a replay-to-forge-a-different-activation distinguished from a benign same-request retry by an unambiguous key (the (license, machine) tuple)? [Clarity, FR-009 / data-model §6] <!-- Evaluator: Covered by spec FR-009 + data-model §6 + plan HINT-004 (same (license, machine) → replay 200; else → 409 nonce_replayed) -->
- [X] CHK028 Is the nonce TTL / retention window specified so store-and-replay behavior is bounded and testable? [Completeness, NEW-CONFIG / research §3] <!-- Evaluator: Covered by spec FR-021 + NEW-CONFIG + data-model §6 (bounded replay-rejection window, default 24h) -->
- [X] CHK029 Is the interaction between nonce uniqueness and K-of-N idempotent re-activation defined (a genuine re-activation carries a fresh nonce and refreshes in place)? [Consistency, data-model §6] <!-- Evaluator: Covered by data-model §6 row 3 (genuine re-activation carries a new nonce; K-of-N + activation_one_active refresh the existing active row in place, no new seat) -->

## Tenant Isolation, Privacy & Audit

- [X] CHK030 Is tenant isolation stated as a fail-closed data-layer requirement (forced RLS; unset scope resolves to zero rows), not only an API-layer check? [Completeness, FR-015 / data-model §11] <!-- Evaluator: Covered by spec FR-015 + data-model §11 (FORCE RLS; unset GUC → NULL → zero rows) -->
- [X] CHK031 Is a cross-tenant activation/license reference required to resolve to not-found (never forbidden) consistently across the runtime and admin planes? [Consistency, SC-012 / contract Error model] <!-- Evaluator: Covered by spec SC-012 + contract (cross-tenant licenseId/activationId → 404 never 403, both planes) + Error model -->
- [X] CHK032 Is the no-raw-hardware-identifier rule stated for every store — the row, the machine-bound token, the audit log, and application logs — without exception? [Completeness, FR-006 / SC-011] <!-- Evaluator: Covered by spec FR-006 (store/log only salted hashes) + FR-014 (audit no raw ids) + SC-011 + data-model §3 (token fp = salted signal_hashes) / §9 -->
- [X] CHK033 Is GDPR erasability of pseudonymous activation rows specified — which fields are nulled and what the retention/erase path is? [Completeness, data-model §9 / Key Entities] <!-- Evaluator: Covered by data-model §9 (label nulled on erasure; salted hashes remain pseudonymous; platform retention/erase path) + Key Entities -->
- [X] CHK034 Is a retention window for deactivated (historical) activation rows defined, or is it left unbounded to the platform retention path? [Completeness, GAP — data-model §9 defers without a bound] <!-- Evaluator: Covered — the Security pass bounded it to a configurable window (default 90 days after deactivation), consistent across data-model §9, spec Key Entities, and NEW-CONFIG (no conflicting number) -->
- [X] CHK035 Is the append-only audit requirement specified to cover every activation, deactivation, and denied/limit-exceeded attempt, with denied RBAC flagged as a security event? [Coverage, FR-014 / SC-009] <!-- Evaluator: Covered by spec FR-014 (append-only, every activate/deactivate/denied) + FR-017 (CSRF failure as security event) + SC-009 + data-model Conventions/Audit + §10 (append-only audit_log) -->

## License-State Coupling & Lifecycle Gaps

- [X] CHK036 Do the requirements define what happens to existing active activations (and the seat count) when the parent license is later revoked, suspended, or expired? [Completeness, GAP — spec Risks note offline lag but the server-side state effect is unspecified] <!-- Evaluator: Resolved — added FR-023 (existing activation rows are NOT auto-deactivated/expired on license suspend/revoke/expire; live status gates only NEW activations per FR-008; seat count unchanged; operators reclaim via explicit deactivation) + data-model §10 note -->
- [X] CHK037 Is the behavior on hard-delete of a license that still has activations specified as a requirement (the ON DELETE NO ACTION rationale surfaced), not only as a schema default? [Completeness, data-model §2 / §8] <!-- Evaluator: Resolved — added FR-024 (a license MUST NOT be hard-deleted while any activation references it; composite FK ON DELETE NO ACTION/RESTRICT) and promoted the data-model §8 FK note from schema default to a stated requirement -->
- [X] CHK038 Is the license-active gate consistent across spec (FR-008), contract error code (`license_not_active`), and data-model app-layer invariants? [Consistency, FR-008 / contract / data-model §8] <!-- Evaluator: Covered — FR-008 + contract 409 license_not_active + data-model §8 invariant (3) are consistent -->
- [X] CHK039 Are the fields exposed by the registry (pseudonymous identity only; never credential or raw signal hashes) required consistently between spec, contract, and data-model? [Consistency, FR-012 / contract / data-model §2] <!-- Evaluator: Covered — FR-012 + contract Activation schema (no machineBoundKey, no raw signals) + data-model §2 are consistent -->
- [X] CHK040 Is every data-integrity success criterion (SC-002/006/007/008/010/011/012) traceable to a functional requirement and to an entity, constraint, or index in the data model? [Traceability, SC-### / data-model §8] <!-- Evaluator: Covered — data-model §8/§14 map each SC to an FR + constraint/index (SC-002→FR-003/seat lock+partial-unique; SC-006→FR-011/status flip; SC-007/008→FR-005/K-of-N; SC-010→FR-009/nonce UNIQUE; SC-011→FR-006/hashes-only; SC-012→FR-015/forced RLS) -->
