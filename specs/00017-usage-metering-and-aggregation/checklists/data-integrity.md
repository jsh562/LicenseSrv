# Data Integrity Checklist: Usage Metering & Aggregation (E016)
**Created**: 2026-08-02 | **Feature**: [spec.md](../spec.md)

## Completeness

- [X] CHK001 Are the requirements explicit that exactly-once accrual holds under a concurrent-producer race (parallel producers minting the same key), not only under serial retries? [Completeness, FR-002/SC-015/AD-001] <!-- Evaluator: Covered by spec.md §FR-002 + SC-015 + Edge Cases (concurrent parallel producers → accrued exactly once via unique constraint) + plan AD-001 -->
- [X] CHK002 Do the requirements state that the rollup's exactly-once/idempotent recompute survives rollup-worker restarts, overlapping sweeps, or re-processing the same events (recompute-not-increment)? [Completeness, FR-010/AD-002/INV-5] <!-- Evaluator: Resolved — amended FR-010 to require rollup idempotency under worker restarts/overlapping sweeps/re-processing (recompute-from-raw keyed by watermark → identical aggregate, no double-count) -->
- [X] CHK003 Is the full set of permitted removal paths for a raw usage event enumerated (retention prune, GDPR erasure) with all other mutation/deletion prohibited in normal operation? [Completeness, FR-003/INV-2] <!-- Evaluator: Covered by spec.md §FR-003 (removal only via retention prune FR-015 or GDPR erasure FR-016; no mutate/delete in normal operation) + data-model INV-2 -->
- [X] CHK004 Is the negative/reversal-event behavior for a COUNT meter defined (does a negative event decrement the count, and by how much)? [Completeness, FR-013/HINT-002] <!-- Evaluator: Resolved — promoted COUNT reversal semantics from HINT-002 to normative FR-013 (a signed reversal decrements the event count by its integer reversal count, e.g. -1 removes one counted event) -->
- [X] CHK005 Is the negative/reversal-event behavior for a UNIQUE_COUNT meter defined (a distinct value stays counted / a reversal does not retract it, since the set is monotonic within a bucket)? [Completeness, FR-013/HINT-002/data-model §usage_unique_value] <!-- Evaluator: Resolved — promoted UNIQUE_COUNT monotonicity to normative FR-013 (distinct set monotonic within a bucket; a reversal never retracts a distinct value; negative/zero/non-integer UNIQUE_COUNT quantity → validation_error) -->
- [X] CHK006 Are the quantity semantics for COUNT and UNIQUE_COUNT meters specified (e.g. whether a non-1 or non-integer quantity is rejected at ingest)? [Completeness, FR-008/HINT-002] <!-- Evaluator: Covered by plan HINT-002 (PINNED: COUNT quantity MUST be a non-zero integer, UNIQUE_COUNT a positive integer; else per-event validation_error at ingest) + contract PerEventRejectionCode; reinforced by amended FR-013 -->
- [X] CHK007 Is UNIQUE_COUNT reproducibility after raw-event pruning explicitly guaranteed by the durable side table (so a pruned raw event cannot silently under-count distinct values)? [Completeness, HINT-002/INV-6/SC-010] <!-- Evaluator: Resolved — added SC-020 asserting UNIQUE_COUNT remains exact/reproducible after prune via the durable usage_unique_value side table (a pruned raw event cannot under-count distinct values) -->
- [X] CHK008 Is the behavior defined for a late event whose event_time falls beyond the retention window but within an as-yet-unrolled bucket (accept, reject, or bounded by watermark)? [Completeness, FR-004/FR-012/FR-015] <!-- Evaluator: Resolved — amended FR-012 to make the retention window the single governing bound: a too-old event is rejected stale_event even if its target bucket is unrolled (an unrolled bucket does not extend acceptance) -->
- [X] CHK009 Are the clock-skew rejection bounds fully specified as configurable thresholds (too-old = older than retention window; future = configured allowance)? [Completeness, FR-004/SC-007] <!-- Evaluator: Covered by spec.md §FR-004 (older than retention window / more than a configured future allowance → distinct reason) + NEW-CONFIG (clock-skew future allowance) + SC-007 -->
- [X] CHK010 Do the requirements define the freeze-on-usage trigger precisely (any single usage_event existing freezes both aggregation type and unit)? [Completeness, FR-009/SC-006/INV-8] <!-- Evaluator: Covered by spec.md §FR-009 (freeze aggregation type AND unit once any usage event exists) + SC-006 + data-model INV-8 -->
- [X] CHK011 Is the over-quota crossing condition defined against which value (the true signed net vs the floored display) and stated to be signal-only without blocking ingest? [Completeness, FR-014/SC-009] <!-- Evaluator: Resolved — amended FR-014 to state the crossing is evaluated against the stored TRUE signed net (not the floor-at-zero display), signal-only, never blocking ingest -->
- [X] CHK012 Do the requirements specify the outcome of deleting a referenced license or entitlement given the composite `ON DELETE NO ACTION` FKs (a referenced row cannot be hard-deleted)? [Completeness, data-model §usage_event/§usage_rollup FKs] <!-- Evaluator: Covered by data-model §usage_event/§usage_rollup/§usage_unique_value composite FKs ON DELETE NO ACTION (a license/entitlement with any usage row cannot be hard-deleted; removal only via owner-role prune/erase, never cascade) -->
- [X] CHK013 Is the unset-tenant-GUC → zero-rows behavior stated as a requirement for every usage table (event, rollup, unique_value)? [Completeness, FR-017/SC-012/INV-3] <!-- Evaluator: Covered by spec.md §SC-012 (unset tenant GUC → zero rows on the usage tables) + data-model INV-3 + RLS section (all three tables FORCE RLS on app.current_tenant) -->
- [X] CHK014 Do the requirements specify that GDPR erasure removes a tenant's raw events AND all durable aggregates (usage_rollup + usage_unique_value), not only the raw events? [Completeness, FR-016/data-model §Retention & GDPR] <!-- Evaluator: Covered by spec.md §FR-016 (tenant's usage events AND aggregates erasable) + data-model §Retention & GDPR (erasure removes usage_event, usage_rollup, AND usage_unique_value) + INV-9 -->
- [X] CHK015 Is the dimension minimization requirement complete (server-side allow-list schema, bounded keys, scalar values, no PII) rather than advisory? [Completeness, FR-016/HINT-005/INV-9] <!-- Evaluator: Covered by spec.md §FR-016 (normative MUST: server-side allow-list schema, bounded keys, scalar-only, size caps; violating dimensions REJECTED per-event validation_error, never silently dropped/truncated/stored) + HINT-005 + INV-9 -->
- [X] CHK016 Are watermark semantics (watermark_ingested_at = MAX ingested_at folded in) and their role in idempotent bucket re-open specified as requirements? [Completeness, FR-012/AD-002/INV-5] <!-- Evaluator: Covered by amended FR-010 (watermark = MAX ingested_at folded in; idempotent recompute) + FR-012 (bucket re-open) + data-model INV-5 / usage_rollup.watermark_ingested_at -->


## Clarity

- [X] CHK017 Is "reproducible aggregate" defined unambiguously (an identical query over an unchanged window returns byte-identical totals), distinct from eventual consistency of an open bucket? [Clarity, FR-011/SC-004] <!-- Evaluator: Covered by spec.md §FR-011 (identical query over unchanged window → identical totals) + SC-004; the open-bucket eventual-consistency path (FR-005 on-read fallback, data-model Rollup step 6) explicitly never changes stored totals -->
- [X] CHK018 Is it unambiguous which value downstream billing true-up (E014) consumes — the TRUE signed net, not the operator-facing floor-at-zero display? [Clarity, FR-020/SC-017/STF-001] <!-- Evaluator: Covered by spec.md §FR-020 (true-up consumes the TRUE signed net stored value, NOT the floor-at-zero display) + SC-017 + STF-001 resolution -->
- [X] CHK019 Is the distinction between the fixed hourly storage grain and the caller-supplied "per period" query window clearly stated so neither is read as the other? [Clarity, FR-010/FR-011/AD-003] <!-- Evaluator: Covered by spec.md §FR-010 (FIXED hourly storage grain; "per period" queries roll up hourly buckets over a caller window; billing alignment at E014 read) + AD-003 -->
- [X] CHK020 Is the idempotency scope `(tenant, source, event_id)` and the role of `source` in preventing cross-producer key collisions clearly defined? [Clarity, FR-002/Clarifications] <!-- Evaluator: Covered by spec.md §FR-002 (unique (tenant, source, idempotency_key); source scopes the key so independent producers never collide) + Clarifications 2026-07-24 + data-model §usage_event -->
- [X] CHK021 Is it clear that the stored aggregate value is never hard-floored and that flooring is exclusively a display/query concern? [Clarity, FR-013/data-model §Reversal-Net] <!-- Evaluator: Covered by spec.md §FR-013 (stored true signed net; floor is display-only) + data-model §Reversal-Net Note (value NOT hard-floored in storage; max(0,value) is a display-only floor) + HINT-003 -->
- [X] CHK022 For over-quota, is it clear whether the flag or the audit entry is the authoritative record, and how the flag behaves once usage is already above the allowance? [Clarity, FR-014/SC-009/data-model §usage_rollup] <!-- Evaluator: Resolved — amended FR-014: the append-only audit entry is authoritative/durable; over_quota is a DERIVED flag (recomputable from rollup); once over the allowance the flag stays set (no additional block) and a later below-allowance reversal clears the derived flag while the crossing audit entry is retained -->
- [X] CHK023 Is the meaning of "dedupe guaranteed only within the retention window" clear, including that a re-report of a pruned key is a fresh accrual (not a resurrection)? [Clarity, FR-002/FR-015/INV-6] <!-- Evaluator: Covered by spec.md §FR-002 (dedupe guaranteed only within retention window) + FR-015 (re-report after prune is a fresh accrual, MUST NOT resurrect) + data-model INV-6 -->


## Consistency

- [X] CHK024 Do FR-002 and the Key Entities section agree on the idempotency uniqueness scope after the clarification (`(tenant, source, event_id)`)? [Consistency, FR-002/Key Entities/Clarifications] <!-- Evaluator: Covered — FR-002 ((tenant, source, idempotency_key)), Key Entities (unique per (tenant, source, idempotency key)), and Clarifications 2026-07-24 all agree on the (tenant, source, event_id) scope -->
- [X] CHK025 Is the FR-013 display floor consistent with FR-011/FR-020 such that no reading implies E014 receives the zero-floored value (STF-001 resolution)? [Consistency, FR-013/FR-020/SC-017/STF-001] <!-- Evaluator: Covered — FR-020 explicitly routes the TRUE signed net (not the floor) to E014; FR-013 floor is display-only; SC-017 asserts both halves; STF-001 resolved inline -->
- [X] CHK026 Does the fail-open on-read fallback for the open bucket stay consistent with the reproducibility requirement (never changes stored totals)? [Consistency, FR-005/SC-004/data-model §Rollup step 6] <!-- Evaluator: Covered by data-model §Rollup step 6 (on-read fallback computes the open bucket on the fly but never changes stored totals, so reproducibility holds) + FR-005 fail-open + SC-004 -->
- [X] CHK027 Is the self-describing `agg_type` snapshot on usage_rollup consistent with the entitlement's frozen `aggregation` so a query needs no join and cannot disagree? [Consistency, SC-005/FR-009/data-model §usage_rollup] <!-- Evaluator: Covered by data-model §usage_rollup (agg_type is a self-describing snapshot so a read needs no join) reconciled with FR-009 freeze-on-usage (aggregation frozen once usage exists → snapshot cannot diverge) + SC-005 -->
- [X] CHK028 Are the retention/dedupe window value and the clock-skew "too-old" bound consistent (both anchored to the same retention window)? [Consistency, FR-004/FR-015/NEW-CONFIG] <!-- Evaluator: Covered — FR-004 too-old bound = "older than the retention window", FR-015 retention/dedupe window, NEW-CONFIG single ~35d window; amended FR-012 makes the retention window the single governing acceptance bound -->
- [X] CHK029 Is the append-only guarantee consistent across FR-003, the SELECT/INSERT-only grants, and the composite-FK NO ACTION rules (no path mutates or cascades a raw event)? [Consistency, FR-003/INV-2/data-model §Grants] <!-- Evaluator: Covered — FR-003 (append-only, no mutate/delete), data-model grants SELECT/INSERT only on usage_event, and FK ON DELETE NO ACTION (no cascade); INV-2 -->
- [X] CHK030 Are the hourly-bucket grain and its UTC truncation stated consistently across usage_rollup and usage_unique_value (same bucket definition)? [Consistency, AD-003/INV-4/data-model CHECKs] <!-- Evaluator: Covered — both usage_rollup and usage_unique_value carry CHECK (bucket = date_trunc('hour', bucket, 'UTC')); data-model INV-4 + AD-003 fixed hourly grain -->


## Testability

- [X] CHK031 Are the distinct rejection reasons enumerated (unknown/archived/non-metered/cross-tenant, stale_event, future_event, batch_too_large, missing scope) so each is independently testable? [Testability, FR-006/FR-004/FR-005/FR-001] <!-- Evaluator: Covered by contract PerEventRejectionCode enum (not_found/not_metered/archived/license_inactive/stale_event/future_event/validation_error) + Error.code enum (validation_error/batch_too_large/unauthorized/forbidden/not_found/rate_limited) + plan Error Handling Strategy -->
- [X] CHK032 Is the mixed-batch outcome specified per event (new accrues, duplicate no-op, invalid reported) so a single bad event demonstrably does not fail the batch? [Testability, FR-007/AD-008] <!-- Evaluator: Covered by spec.md §FR-007 + AD-008 + contract IngestSummary { accepted, duplicate, rejected[] } (a single bad event never fails the batch) -->
- [X] CHK033 Is the over-cap batch requirement testable as "rejected before ANY accrual" (an observable pre-accrual boundary)? [Testability, FR-005/AD-008] <!-- Evaluator: Covered by spec.md §FR-005 (over-cap batch rejected before any accrual) + contract batch_too_large (rejected BEFORE any accrual, details {max,size}) -->
- [X] CHK034 Is the reversal outcome testable as two independent assertions (stored true signed net vs floored display) per SC-017? [Testability, FR-013/SC-017] <!-- Evaluator: Covered by spec.md §SC-017 (two halves: stored true signed net identical on re-query vs floored display) + FR-013 -->
- [X] CHK035 Is the prune outcome testable as "raw + keys gone AND rollup unchanged AND re-report accrues fresh" with observable pre/post states? [Testability, FR-015/SC-010] <!-- Evaluator: Covered by spec.md §SC-010 (raw + keys pruned, rollup unchanged, re-report is a fresh accrual) + FR-015; reinforced by SC-020 for UNIQUE_COUNT prune-safety -->
- [X] CHK036 Are the SUM vs COUNT vs UNIQUE_COUNT accrual results specified distinctly enough that the same event stream yields verifiably different, deterministic totals per type? [Testability, FR-008/SC-005] <!-- Evaluator: Covered by spec.md §SC-005 (SUM sums quantities, COUNT counts events, UNIQUE_COUNT counts distinct values) + FR-008 + HINT-002 per-aggregation rollup math -->
- [X] CHK037 Is the late/out-of-order acceptance criterion measurable (the prior period's aggregate observably updates while no prior event is mutated)? [Testability, FR-012/SC-007/SC-008] <!-- Evaluator: Covered by spec.md §SC-007 (late event within window updates the correct period) + SC-008 (reversal adjusts without mutating/deleting any prior event) + FR-012 -->


## Edge Case Coverage

- [X] CHK038 Do the requirements cover a bucket whose net goes negative after a reversal (stored net negative, display floored to zero, true-up sees the negative)? [Coverage, FR-013/FR-020/SC-017] <!-- Evaluator: Covered by spec.md §FR-013 (stored true signed net, display floored) + FR-020 (true-up sees the true signed net) + SC-017 + STF-001 -->
- [X] CHK039 Do the requirements cover concurrent late events targeting the same already-rolled bucket resolving to a single reproducible recompute (no lost-update)? [Coverage, FR-012/AD-002/INV-5] <!-- Evaluator: Resolved — amended FR-010 to state concurrent late events resolving the same already-rolled bucket converge to a single reproducible recompute (recompute-from-raw, no lost update); FR-012 bucket re-open + INV-5 -->
- [X] CHK040 Is a duplicate distinct value under redelivery / late arrival covered as an idempotent no-op that does not inflate UNIQUE_COUNT? [Coverage, HINT-002/data-model §usage_unique_value] <!-- Evaluator: Covered by data-model §usage_unique_value (UNIQUE (…, value_hash) + INSERT ON CONFLICT DO NOTHING → an already-seen distinct value is a no-op; UNIQUE_COUNT = COUNT(*)) + HINT-002 -->

