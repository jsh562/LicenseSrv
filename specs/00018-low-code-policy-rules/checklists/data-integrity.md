# Checklist: Data Integrity — Low-Code Policy Rules (E017)
**Created**: 2026-08-12 | **Feature**: [spec.md](../spec.md)

## Completeness

- [X] CHK001 Is the exact set of "content" fields whose edit MUST create a new immutable version enumerated (condition, effect, priority, entitlement_id, plan_id) distinctly from lifecycle fields? [Completeness, INV-2/FR-011] <!-- Evaluator: Covered by data-model.md INV-2 (content set enumerated: condition, effect, priority, entitlement_id, plan_id, rule_key, version, author, created_at) vs mutable status/updated_at -->
- [X] CHK002 Are the requirements explicit that a content edit INSERTs a new (rule_key, version+1) row while the prior version row is retained and never mutated? [Completeness, FR-011/SC-008/INV-2] <!-- Evaluator: Covered by data-model.md §"Content axis" + INV-2 and spec FR-011/SC-008 -->
- [X] CHK003 Is it specified that `status` (with `updated_at`) is the ONLY mutable column and that every content column is immutable after insert? [Completeness, INV-2/AD-006] <!-- Evaluator: Covered by data-model.md INV-2 (status/updated_at only mutable) and now spec FR-011 (elevated to requirement) -->
- [X] CHK004 Does the spec require the audit to pin the EXACT (rule_key, version) that fired so an in-flight or prior decision is explainable after the rule is re-versioned? [Completeness, SC-008/SC-009/FR-014] <!-- Evaluator: Covered by spec FR-014/SC-009 (fired rule id+version) + data-model fired_rule {rule_id, rule_key, version} -->
- [X] CHK005 Is the storage location for the "considered-but-not-applied" rule ids+versions defined, given FR-014/SC-009 require capturing them but the `policy_evaluation` schema exposes only a single `fired_rule` field? [Completeness, FR-014/SC-009/INV-8] <!-- Evaluator: Resolved — added considered_rules jsonb column (+CHECK, ER, INV-8) to policy_evaluation reconciling FR-014/SC-009 with the DDL -->
- [X] CHK006 Is the canonicalization of the decision context defined so that `input_hash` (and `input_snapshot`) reproduces the same value on identical re-evaluation? [Completeness, FR-005/FR-014/SC-003] <!-- Evaluator: Resolved — added canonical-serialization clause to FR-005 and data-model INV-12 -->
- [X] CHK007 Is a concrete `policy_evaluation` retention / prune policy (threshold, owner-role mechanism) specified rather than left as "retention keys off created_at"? [Completeness, FR-014/INV-8] <!-- Evaluator: Resolved — added bounded config-sourced retention window (owner-role prune) to NEW-CONFIG, FR-014, and INV-8 -->
- [X] CHK008 Are the required audit fields for a fail-closed evaluation (base decision stood, `fired_rule = NULL`) fully specified so a skipped-rule outcome is still explainable? [Completeness, FR-010/INV-7/SC-006] <!-- Evaluator: Covered by data-model INV-7 (fired_rule=NULL, base decision, appended row) + INV-8; spec FR-010/SC-006 -->
- [X] CHK009 Does the spec state what happens on the NEXT issuance when an entitlement's authored `rule_max` is lowered below a previously-applied effect value? [Completeness, FR-007/SC-004/INV-4] <!-- Evaluator: Resolved — added Edge Case (re-clamp to the new lower maximum at next (re-)issuance, evaluated fresh) -->
- [X] CHK010 Is the "≥ base plan value" requirement for the authored maximum specified as a service-layer invariant (author-time + evaluation), since a single-table CHECK cannot join `plan_entitlement.int_value`? [Completeness, FR-007/HINT-003/INV-4] <!-- Evaluator: Covered by spec FR-007/FR-021 + data-model INV-4 and the DDL service-layer NOTE (HINT-003) -->
- [X] CHK011 Are the expand-only `entitlement` extension columns (`rule_max`, `rule_eligible`, `rule_tiers`) and their default/backfill semantics for existing rows fully specified? [Completeness, FR-003/FR-007] <!-- Evaluator: Covered by data-model §3 + DDL (rule_max NULL, rule_eligible NOT NULL DEFAULT false, rule_tiers NULL; existing rows safe defaults) -->


## Clarity

- [X] CHK012 Is "immutable version" defined unambiguously enough to distinguish a content edit (new version) from a status transition (in-place update)? [Clarity, FR-011/AD-006] <!-- Evaluator: Covered by data-model §"Two orthogonal axes" (content axis immutable vs status axis mutable) + INV-2 -->
- [X] CHK013 Is the meaning of "at most one live (active|preview) version per rule_key" stated clearly, including how promotion demotes the prior live version? [Clarity, INV-3/SC-008] <!-- Evaluator: Covered by data-model INV-3 + policy_rule_one_live and §"Status axis" (promotion sets new version live, prior live disabled in one tx) -->
- [X] CHK014 Is "exactly ONE effect applies per entitlement" unambiguous about whether "one per entitlement" means per target entitlement key vs per rule? [Clarity, FR-006/INV-5] <!-- Evaluator: Covered by FR-006 + INV-5 ("per target entitlement") -->
- [X] CHK015 Is the deterministic tiebreak defined precisely (stable `(rule_key, version)` ordering) so two same-priority rules always resolve to one reproducible outcome? [Clarity, FR-006/SC-010/INV-5] <!-- Evaluator: Covered by FR-006/SC-010 + INV-5 (stable (rule_key, version) tiebreak) -->
- [X] CHK016 Is "fail closed" defined precisely for each trigger (error, timeout, resource-bound breach, unguarded absent-field) so the required base-decision behavior is unambiguous? [Clarity, FR-010/INV-7] <!-- Evaluator: Covered by FR-010 (four triggers enumerated) + INV-7 -->
- [X] CHK017 Are the three effect kinds and their bound semantics (adjust_limit ≤ rule_max, toggle only if rule_eligible, select_tier ∈ rule_tiers) each stated with unambiguous accept/refuse conditions? [Clarity, FR-003/FR-007/INV-4] <!-- Evaluator: Covered by FR-003/FR-007 + INV-4 (per-kind accept/refuse) -->
- [X] CHK018 Is the `dry_run`-only nullability of `license_id`/`plan_id` clearly distinguished from enforced/preview modes that require a real license reference? [Clarity, FR-013/FR-014/INV-9] <!-- Evaluator: Covered by DDL policy_evaluation_license_shape CHECK + INV-9 (null only for dry_run) -->
- [X] CHK019 Is "append-only" defined at the data layer (SELECT/INSERT only, no app UPDATE/DELETE) so no requirement implies mutating a prior audit row? [Clarity, FR-014/INV-8] <!-- Evaluator: Covered by INV-8 + grant SELECT,INSERT only -->


## Consistency

- [X] CHK020 Do FR-014/SC-009 (audit records considered-but-not-applied ids+versions) and the `policy_evaluation` DDL (single `fired_rule`, no considered column) agree, or is there an unreconciled gap? [Consistency, FR-014/SC-009] <!-- Evaluator: Resolved — reconciled the gap by adding considered_rules jsonb (array of {rule_id, rule_key, version}) to the policy_evaluation DDL, ER, and INV-8 -->
- [X] CHK021 Is the effect-bound guarantee consistent across author-time refusal (FR-002) and evaluation-time clamp (FR-007) so the same over-bound effect is never applied by either path? [Consistency, FR-002/FR-007/INV-4] <!-- Evaluator: Covered by FR-007 (refuse at author, clamp at eval) + INV-4 -->
- [X] CHK022 Is the "grant UPDATE on policy_rule" consistent with the immutability requirement, i.e. is the repo-level restriction of UPDATE to `status`/`updated_at` stated as a requirement, not only a code note? [Consistency, INV-2/AD-006] <!-- Evaluator: Resolved — elevated the UPDATE-restricted-to-status/updated_at restriction to a stated requirement in FR-011 (was code note + INV-2) -->
- [X] CHK023 Does the highest-priority-wins "no effect chaining" rule stay consistent with the audit requirement to record other matching rules as considered? [Consistency, FR-006/FR-014/INV-5] <!-- Evaluator: Covered by FR-006/FR-014 + INV-5/INV-8 (now with considered_rules storage) -->
- [X] CHK024 Is the "≥ base plan value" ceiling semantic applied consistently across FR-007, SC-015, Key Entities, and the data-model note (no residual "plan ceiling" wording)? [Consistency, FR-007/STF-001/SC-015] <!-- Evaluator: Covered by STF-001 [RESOLVED] — all "plan ceiling" wording replaced with authored-maximum (≥ base) across FR-007/SC-015/Key Entities/data-model -->
- [X] CHK025 Are the forced-RLS + composite-FK `ON DELETE NO ACTION` conventions applied consistently to both `policy_rule` and `policy_evaluation`? [Consistency, FR-015/INV-1] <!-- Evaluator: Covered by DDL (both tables ENABLE+FORCE RLS, composite FKs ON DELETE NO ACTION) + INV-1 -->
- [X] CHK026 Is the minimization requirement (no secret/PII in condition, effect, input_snapshot, decision) applied consistently to every persisted column and audit projection? [Consistency, FR-017/INV-10/SC-013] <!-- Evaluator: Covered by FR-017 + INV-10 (condition, effect, input_snapshot, decision, considered_rules all minimized, no secret/PII) -->
- [X] CHK027 Is determinism consistent with the audit's `input_snapshot`/`input_hash` — i.e. does the recorded context match exactly the context that produced the decision? [Consistency, FR-005/FR-014/INV-6] <!-- Evaluator: Covered by INV-6 + new INV-12 (canonical serialization ties the recorded hash to the deciding context) -->
- [X] CHK028 Is the deterministic priority index `(tenant_id, entitlement_id, status, priority DESC)` consistent with the requirement that only live (active|preview) versions participate in enforcement? [Consistency, FR-006/FR-011/INV-3] <!-- Evaluator: Covered by DDL policy_rule_eval index (status in scan) + policy_rule_one_live + INV-3/INV-5 -->


## Testability

- [X] CHK029 Can "prior version retained and never mutated" be verified via an observable assertion (the version=N row is byte-identical after a version=N+1 edit)? [Testability, FR-011/SC-008/INV-2] <!-- Evaluator: Covered by SC-008 + INV-2 (immutable content) and Testing Strategy (versioning integration test) -->
- [X] CHK030 Is the one-live-version invariant expressed so a duplicate live version is a detectable failure (partial-unique violation)? [Testability, INV-3/SC-008] <!-- Evaluator: Covered by policy_rule_one_live partial-unique index + INV-3 -->
- [X] CHK031 Is deterministic reproducibility stated with measurable criteria (same context evaluated twice → identical `decision` AND `fired_rule` AND `input_hash`)? [Testability, FR-005/SC-003/INV-6] <!-- Evaluator: Resolved — extended SC-003 to require identical decision, fired rule, AND input_hash; backed by INV-6/INV-12 -->
- [X] CHK032 Is the effect clamp testable via an authored value above `rule_max` yielding a decision at exactly `rule_max` (and a rule-ineligible boolean refusing the toggle)? [Testability, FR-007/SC-004/SC-015/INV-4] <!-- Evaluator: Covered by SC-004/SC-015 + INV-4 (clamp to rule_max; toggle only if rule_eligible) -->
- [X] CHK033 Is fail-closed observable as a distinct outcome (base value returned, path not blocked, audit row appended with `fired_rule = NULL`) for each trigger? [Testability, FR-010/SC-006/INV-7] <!-- Evaluator: Covered by SC-006 + INV-7 (base decision, no crash/block, fired_rule=NULL appended) -->
- [X] CHK034 Is the requirement that an audit-write failure ALSO fails closed WITHOUT blocking issuance stated with an observable acceptance criterion? [Testability, FR-010/FR-014] <!-- Evaluator: Resolved — added audit-write-failure clause to FR-014/INV-8 and new SC-020 (observable: base decision stands, token issued, failure to ops logging) -->
- [X] CHK035 Is the unset-tenant-GUC → zero-rows behavior stated as a testable outcome on BOTH policy tables? [Testability, FR-015/SC-012/INV-1] <!-- Evaluator: Covered by SC-012 + INV-1 (forced RLS on both tables, unset GUC → zero rows) -->
- [X] CHK036 Is the `dry_run`-only nullable-license CHECK testable (enforced/preview with null license_id is rejected; dry_run permitted)? [Testability, FR-013/FR-014/INV-9] <!-- Evaluator: Covered by DDL policy_evaluation_license_shape CHECK + INV-9 -->
- [X] CHK037 Is each mode-marked audit outcome (enforced|preview|dry_run) verifiable as exactly one appended row per evaluation with the specified fields? [Testability, FR-014/SC-009/INV-8] <!-- Evaluator: Covered by INV-8 ("exactly one mode-marked row") + SC-009 -->
- [X] CHK038 Is "no effect chaining / exactly one fired rule per entitlement" verifiable via overlapping rules producing a single applied effect and the rest recorded as considered? [Testability, FR-006/SC-010/INV-5] <!-- Evaluator: Covered by FR-006/SC-010 + INV-5 and the reconciled considered_rules storage (INV-8) -->

