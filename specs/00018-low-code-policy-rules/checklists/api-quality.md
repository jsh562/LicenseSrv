# API Quality: Low-Code Policy Rules (E017)
**Created**: 2026-08-12 | **Feature**: [spec.md](../spec.md)

## Completeness

- [X] CHK001 Are all six admin endpoints (create/list/get/edit/status/dry-run) documented with a fully specified request schema, success response schema, and status codes? [Completeness, plan §API Surface Summary] <!-- Evaluator: Covered by policy-api.openapi.yaml §paths (all six operations) + plan §API Surface Summary -->
- [X] CHK002 Is each of the four author-time validation codes (`invalid_condition` / `unsafe_operator` / `effect_out_of_bounds` / `condition_too_large`) defined with a distinct, non-overlapping trigger condition? [Completeness, FR-002] <!-- Evaluator: Covered by policy-api.openapi.yaml §Error.code enum comments + RuleValidationError examples -->
- [X] CHK003 Is "reject-before-persist" stated as a requirement for every validation refusal — i.e. an invalid/unsafe/out-of-bounds rule is never stored or evaluated? [Completeness, FR-002] <!-- Evaluator: Covered by policy-api.openapi.yaml §AUTHOR-TIME VALIDATION block + FR-002 -->
- [X] CHK004 Is the `condition_too_large` cap expressed with a stated limit and dimension (bytes / depth / nodes) rather than an unquantified "too large"? [Completeness, FR-009] <!-- Evaluator: Covered by policy-api.openapi.yaml §Error.details condition_too_large → { limit, actual, dimension: bytes|depth|nodes } -->
- [X] CHK005 Is the `PolicyEffect` union documented as CLOSED with exactly three kinds and no open/extensible fallback? [Completeness, FR-003] <!-- Evaluator: Covered by policy-api.openapi.yaml §PolicyEffect oneOf + discriminator (three mappings) -->
- [X] CHK006 Is the `value` type fixed per effect kind (adjust_limit→number, toggle_boolean→boolean, select_tier→string) in the contract? [Completeness, FR-003] <!-- Evaluator: Covered by AdjustLimitEffect (number) / ToggleBooleanEffect (boolean) / SelectTierEffect (string) -->
- [X] CHK007 Is the `PolicyCondition` required to be structured JSON (JSONLogic-style) with the allow-listed operator set enumerated, and explicitly not a free-text expression? [Completeness, FR-001] <!-- Evaluator: Covered by policy-api.openapi.yaml §PolicyCondition (operator allow-list enumerated; "NOT a free-text expression") -->
- [X] CHK008 Is the dry-run supplied `DecisionContext` request schema bounded and allow-listed (field-level constraints, `additionalProperties: false`) rather than an open object? [Completeness, FR-004] <!-- Evaluator: Resolved — DecisionContext already additionalProperties:false/allow-listed; added FR-020 size/AST-depth/field-count bounds + oversizedContext validation_error example to policy-api.openapi.yaml -->
- [X] CHK009 Is the dry-run context-source rule documented — exactly one of `context` or `licenseId`, with the both/neither case defined as a refusal? [Completeness, FR-013] <!-- Evaluator: Covered by DryRunRequest oneOf(required context/licenseId) + DryRunBadRequest contextSource example -->
- [X] CHK010 Is the full version-history response shape (`PolicyRuleDetail.versions`, ordering, minItems) defined for the GET-detail endpoint? [Completeness, FR-011] <!-- Evaluator: Covered by PolicyRuleDetail.versions (version DESC, minItems:1, items PolicyRuleVersion carrying version/status/priority/createdAt/author) -->
- [X] CHK011 Is the `consideredNotApplied` array shape and its `reason` enum (lower_priority / not_matched / failed_closed / disabled) specified in the dry-run result? [Completeness, FR-006] <!-- Evaluator: Covered by ConsideredRule.reason enum + DryRunResult.consideredNotApplied -->
- [X] CHK012 Does the requirement set specify that the audit/`policy_evaluation` record captures the same fired-rule + considered-but-not-applied ids/versions the dry-run response exposes? [Completeness, FR-014] <!-- Evaluator: Resolved — FR-014/SC-009 + data-model considered_rules column already specify it; aligned contract §AUDIT prose to state the audit records the same consideredNotApplied ids+versions -->
- [X] CHK013 Is the status-transition endpoint's behavior for an invalid or unsupported transition (e.g. missing/unknown `status`) documented? [Completeness, FR-011] <!-- Evaluator: Resolved — missing/unknown status was 400 validation_error; added 409 invalid_state_transition (code+response+details) for a valid status not permitted from current state (FR-011/012) -->
- [X] CHK014 Is the fail-closed dry-run outcome (rule errors/times out → base decision, `source: base`, `reason: failed_closed`) documented with an example? [Completeness, FR-010] <!-- Evaluator: Covered by dry-run failedClosed example (source: base, firedRule: null, reason: failed_closed) -->
- [X] CHK015 Is the list-endpoint result-bounding contract (deterministic ordering, hard cap, `truncated` signal, not-paginated) specified? [Completeness, FR-011] <!-- Evaluator: Covered by PolicyRuleList (priority DESC + ruleKey tiebreak, maxItems:1000, truncated, NOT paginated) -->
- [X] CHK016 Are the RBAC `minRole` requirement and the CSRF requirement documented per operation, including that GET reads are exempt from CSRF? [Completeness, FR-016] <!-- Evaluator: Covered by per-op x-rbac.minRole + CsrfToken param (GET exempt, stated) -->


## Clarity

- [X] CHK017 Is the admin-only single-auth-plane boundary unambiguous — session cookie only, with an explicit statement that there is NO `X-API-Key` / runtime plane? [Clarity, FR-016] <!-- Evaluator: Covered by info.description §ADMIN-ONLY SURFACE + sessionCookie securityScheme ("NO machine X-API-Key plane") -->
- [X] CHK018 Is the internal issuance-path evaluation seam clearly described as NOT a wire operation (no URL, no request/response body)? [Clarity, FR-008] <!-- Evaluator: Covered by info.description §NO RUNTIME WIRE OPERATION (in-process module seam, no wire representation) -->
- [X] CHK019 Is it unambiguous that dry-run is the ONLY over-the-wire way to exercise evaluation, and that it is non-enforcing? [Clarity, FR-013] <!-- Evaluator: Covered by info.description ("dry-run is the ONLY way to exercise evaluation over the wire, non-enforcing") -->
- [X] CHK020 Is the PATCH-edit contract clear that a successful edit returns the NEW immutable version (not the prior one) and never mutates an existing version? [Clarity, FR-011] <!-- Evaluator: Covered by editPolicyRule description (INSERTs new immutable version, prior retained, never mutated) -->
- [X] CHK021 Is the distinction between a content edit (PATCH → new version) and a status transition (POST status → no new version) stated unambiguously? [Clarity, FR-011] <!-- Evaluator: Covered by editPolicyRule + transitionPolicyRuleStatus descriptions ("does NOT create a new content version") -->
- [X] CHK022 Are the three lifecycle states (`active` enforced / `preview` report-only / `disabled` never-evaluates) defined so a reviewer can distinguish their effects? [Clarity, FR-012] <!-- Evaluator: Covered by RuleStatus enum description -->
- [X] CHK023 Is the cross-tenant resolution rule stated precisely — an unknown OR cross-tenant `ruleKey`/`licenseId`/`entitlementId` resolves to `404 not_found`, never `403`? [Clarity, FR-015] <!-- Evaluator: Covered by info.description §TENANT SCOPING + RuleNotFound/EntitlementNotFound/RuleOrLicenseNotFound responses -->
- [X] CHK024 Is the authored-maximum semantic (a rule MAY raise above the base plan value up to the authored ceiling, never beyond) stated unambiguously in the contract? [Clarity, FR-007] <!-- Evaluator: Covered by info.description §AUTHORED MAXIMUM + AdjustLimitEffect.value description -->
- [X] CHK025 Is the timestamp format requirement explicit (RFC3339 / UTC with trailing `Z`) for all server-set and injected timestamps? [Clarity, FR-005] <!-- Evaluator: Covered by info.description §TIMESTAMPS (RFC3339/ISO8601 UTC trailing Z) -->
- [X] CHK026 Is the injected `decisionTimestamp` documented as the sole time source an evaluation sees (no wall-clock/random) unambiguously? [Clarity, FR-005] <!-- Evaluator: Covered by §TIMESTAMPS + DecisionContext.decisionTimestamp ("ONLY time source", "Never wall-clock") -->
- [X] CHK027 Is the error envelope shape (`{code, message, details?}`) and the per-code `details` shape clearly specified for each enumerated code? [Clarity, plan §Error Handling Strategy] <!-- Evaluator: Resolved — Error.details per-code shapes existed; added rule_set_limit_exceeded → {limit,actual,scope} and invalid_state_transition → {from,to} so every enumerated code has a details shape -->
- [X] CHK028 Is why dry-run requires CSRF (it writes a mode-marked audit row) explained clearly rather than left implicit? [Clarity, FR-016] <!-- Evaluator: Covered by dryRunPolicyRule description + CsrfToken param ("dry-run is a mutation-plane operation because it evaluates and writes a mode-marked audit row") -->


## Consistency

- [X] CHK029 Do the four validation `400` codes match across spec FR-002, the plan Error Handling Strategy, and the OpenAPI `Error.code` enum? [Consistency, FR-002] <!-- Evaluator: Covered — invalid_condition/unsafe_operator/effect_out_of_bounds/condition_too_large align across FR-002, plan §Error Handling Strategy, and Error.code enum -->
- [X] CHK030 Do the per-operation RBAC roles (admin authors/edits/status/dry-run; viewer reads) agree between the plan API Surface Summary and the OpenAPI `x-rbac`/security definitions? [Consistency, FR-016] <!-- Evaluator: Covered — plan API Surface (admin create/edit/status/dry-run; viewer list/get) matches each op's x-rbac.minRole -->
- [X] CHK031 Is the closed effect union (adjust_limit/toggle_boolean/select_tier) described identically across spec FR-003, the plan, and the OpenAPI discriminator mapping? [Consistency, FR-003] <!-- Evaluator: Covered — FR-003 + plan AD-002 + PolicyEffect discriminator mapping all name the same three kinds -->
- [X] CHK032 Is the "internal seam, not a public runtime endpoint" note consistent between the Implementation Signals (NEW-API), the plan API summary, and the OpenAPI description? [Consistency, FR-008] <!-- Evaluator: Covered — spec NEW-API + plan §API Surface Summary + contract §NO RUNTIME WIRE OPERATION agree -->
- [X] CHK033 Is the deferral of per-request online (E013 validate-time) evaluation stated consistently and not contradicted by any endpoint in the contract? [Consistency, FR-008] <!-- Evaluator: Covered — deferral in FR-008/Scope/plan/contract; no path exercises validate-time evaluation -->
- [X] CHK034 Is the no-secret/no-signing-key/no-PII (pseudonymous-reference-only) invariant applied consistently to every request, response, log, and audit field? [Consistency, FR-017] <!-- Evaluator: Covered — info.description §SECRECY & PII INVARIANTS + per-schema "no secret/PII" notes + author/customerReference pseudonymous fields -->
- [X] CHK035 Is the CSRF-on-all-mutations requirement consistent across create/edit/status/dry-run, with none of the four omitting it? [Consistency, FR-016] <!-- Evaluator: Covered — all four mutating ops declare the CsrfToken parameter -->
- [X] CHK036 Do the mode markers (`enforced` / `preview` / `dry_run`) used in the dry-run response and audit trail agree with the states named in FR-012/FR-014? [Consistency, FR-014] <!-- Evaluator: Covered — DryRunResult.mode=dry_run + audit mode markers align with FR-012/FR-014 and data-model mode CHECK -->
- [X] CHK037 Does the wire camelCase field naming map consistently to the data-model snake_case columns (e.g. `ruleKey`↔`rule_key`) without drift? [Consistency, plan §Data Model Summary] <!-- Evaluator: Covered — info.description §ERROR MODEL states the camelCase↔snake_case mapping; wire fields map cleanly to data-model columns -->

## Testability

- [X] CHK038 Can each of the four validation refusals be triggered and asserted independently via a distinct, observable `400` code and `details` shape? [Testability, FR-002] <!-- Evaluator: Covered — RuleValidationError examples give a distinct code + details per refusal (invalid_condition/unsafe_operator/effect_out_of_bounds/condition_too_large) -->
- [X] CHK039 Is the deterministic list ordering (`priority` DESC, tiebreak `ruleKey`) specified precisely enough to write a repeatable ordering assertion? [Testability, FR-006] <!-- Evaluator: Covered — PolicyRuleList orders by priority DESC then ruleKey -->
- [X] CHK040 Are the dry-run response fields (`mode`, `decision`, `firedRule`, `consideredNotApplied`) defined concretely enough to assert a would-be decision and a fail-closed outcome without ambiguity? [Testability, FR-013] <!-- Evaluator: Covered — DryRunResult fields + fired/failedClosed examples (source: base, reason: failed_closed) -->

