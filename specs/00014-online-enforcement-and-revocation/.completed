Completed: 2026-07-19T00:00:00Z
E013 Online Enforcement and Revocation — all 47 tasks implemented (Phases 1-9).
- Migration 0009_online_enforcement.sql: activation +2 anchor cols; new checkin (TTL-pruned anti-replay) + revocation_list (signed CRL) tables, forced RLS, append-only grants.
- New module src/server/modules/enforcement/: config, enforce (verdict logic, 200+verdict refusals), token (short-TTL LIC1 re-sign via E004 signer, current effective entitlements FR-017), checkin-repo (idempotent nonce + monotonic anchor), validate/heartbeat (shared runEnforcement core), crl + crl-worker + revocation-list, routes (POST /v1/validate + /v1/heartbeat + GET /v1/revocation-list, validate scope, rate-limited), README.
- Signer extended with domain-separated signDetached ("LICSRV-CRL-v1") for the CRL — enforcement stays off the keystore (Principle I).
- US1 validate + short-TTL renewal; US2 revocation/suspension propagation (bounded staleness ≤ TTL, reinstate resumes); US3 heartbeat renews-only-while-valid + entitlement refresh; US5 offline-first preserved (never-connected unaffected, machine_bound_token untouched); US4 signed versioned CRL (byte-stable, monotonic version FR-022, fetch fail-open/older-ignore/bad-sig-untrusted FR-011/022/023, json+?format=file, ETag/cache/304); US6 clock-tamper server-side monotonic anchor + signed time (client enforcement documented). CRL worker + prune fail-open in main.ts.
Validation: build/typecheck/lint clean; full Vitest suite 377 passed / 9 skipped (env-gated), no regression; coverage 93.14% lines / 81.4% branches (≥80% gate). Real autocannon perf p97.5 ~40-43ms << 120ms SLO. CI workflow enforcement.yml added (SHA-pinned). Integration tests use testcontainers Postgres + the real E004 signer + offline-verify via the E001 WASM core.
