# Checklist: API Quality — Online Enforcement and Revocation
**Created**: 2026-07-18 | **Feature**: [spec.md](../spec.md)

## Endpoint & Operation Contract Definition

- [X] CHK001 Are all three endpoints (POST /v1/validate, POST /v1/heartbeat, GET /v1/revocation-list) each given a complete operation contract — method, path, operationId, summary, request, and full response set? [Completeness, contract paths] <!-- Evaluator: Covered by contracts/online-enforcement-api.openapi.yaml paths — all three ops carry operationId/summary/request(body or params)/full response set -->
- [X] CHK002 Is the relationship between validate and heartbeat made explicit — that they share the EnforcementRequest/EnforcementResult schema but differ in role (session entry vs periodic renewal)? [Clarity, contract §EnforcementRequest/§EnforcementResult] <!-- Evaluator: Covered by contract §EnforcementRequest/§EnforcementResult ("SHARED" body/result) + validate desc ("ENTRY point") vs heartbeat desc ("periodic RENEWAL of a session that validate established") -->
- [X] CHK003 Does each operation carry a priority marker (x-priority) traceable to a user story, so the contract's coverage of US1–US6 is auditable? [Traceability, contract x-priority, US1–US6] <!-- Evaluator: Covered by contract x-priority (validate P1/heartbeat P1 → US1; getRevocationList P2 → US4) + per-op descriptions citing US1..US4 -->

## Refusal Verdict Semantics (200 + verdict)

- [X] CHK004 Is the response schema for a refusal verdict (revoked/suspended/expired/deactivated) fully specified — which fields are present/absent when no token is issued? [Completeness, contract §EnforcementResult] <!-- Evaluator: Covered by contract §EnforcementResult — shortLivedToken/renewAfter/expiresAt "PRESENT ONLY when verdict = valid ... OMITTED on every non-valid verdict"; revoked/deactivated examples show no token -->
- [X] CHK005 Is the Verdict enum exhaustive (valid/revoked/suspended/expired/deactivated) and each value mapped to a distinct, named enforcement condition? [Coverage, contract §Verdict, FR-004/FR-005] <!-- Evaluator: Covered by contract §Verdict enum + REVOCATION PROPAGATION block mapping each verdict to license.status(revoked/suspended)/expiresAt(expired)/activation.status(deactivated) -->
- [X] CHK006 Is it unambiguous which EnforcementResult fields are always present (verdict, serverTime, stalenessWindow) versus valid-only (shortLivedToken, renewAfter, expiresAt)? [Ambiguity, contract §EnforcementResult] <!-- Evaluator: Covered by contract §EnforcementResult required:[verdict,serverTime,stalenessWindow]; shortLivedToken/renewAfter/expiresAt each marked "PRESENT ONLY on valid" -->
- [X] CHK007 Is the rationale for signaling refusals as 200+verdict (a query, not a mutation) documented and reconciled against E009's 409 license_not_active, so the divergence reads as a deliberate, consistent decision? [Consistency, AD-001, contract "REFUSAL SEMANTICS"] <!-- Evaluator: Covered by contract "REFUSAL SEMANTICS" block (explicitly reconciles the divergence from E009's 409 license_not_active) + plan.md AD-001 -->

## Error Model Shape, Codes & Status Mapping

- [X] CHK008 Is the structured error shape `{ code, message, details? }` defined and stated to match the E008/E009 error model? [Consistency, contract §Error] <!-- Evaluator: Covered by contract §Error ("Project-standard structured JSON error { code, message, details? } (matches E008/E009)"; required:[code,message]) -->
- [X] CHK009 Is every error code enumerated and mapped to exactly one HTTP status (400/401/403/404/409/429/503), with no code reused across statuses? [Completeness, contract §Error.code] <!-- Evaluator: Covered by contract §Error.code enum with per-code status comments (validation_error→400, unauthorized→401, forbidden→403, activation_not_found/revocation_list_not_found→404, nonce_replayed→409, rate_limited→429, signer_unavailable→503); no code reused -->
- [X] CHK010 Is every HTTP status listed on an operation's responses backed by a defined response component, with no dangling status reference? [Coverage, contract paths/responses] <!-- Evaluator: Covered by contract components/responses — every $ref (BadRequest/Unauthorized/ForbiddenScope/ActivationNotFound/NonceReplayed/RateLimited/SignerUnavailable/RevocationListNotFound) is defined; 200/304 defined inline -->
- [X] CHK011 Is the per-code `details` shape documented so a client can parse the structured context deterministically? [Clarity, contract §Error.details] <!-- Evaluator: Covered by contract §Error.details — per-code shapes enumerated (validation_error→{field,value?}, forbidden→{requiredScope}, revocation_list_not_found→{productId,version?}, rate_limited/signer_unavailable→{retryAfterSeconds}, etc.) -->
- [X] CHK012 Is the boundary between a 404 activation_not_found and a 200 non-valid verdict for a KNOWN-but-refused binding drawn unambiguously? [Ambiguity, contract §ActivationNotFound] <!-- Evaluator: Covered by contract §ActivationNotFound ("Distinct from a non-valid VERDICT: a KNOWN activation whose license is revoked/suspended/expired or whose binding is deactivated returns 200 with that verdict, not 404") -->
- [X] CHK013 Is 503 signer_unavailable clearly distinguished from a non-valid verdict (valid-but-cannot-sign vs refused), so the two outcomes are never conflated? [Ambiguity, contract §SignerUnavailable] <!-- Evaluator: Covered by contract §SignerUnavailable ("The binding IS currently valid but ... could not be signed"; "A non-valid VERDICT ... is a 200, not a 503; 503 means 'valid but could not sign'") -->
- [X] CHK014 Are the CRL-absence outcomes (unknown/cross-tenant product vs no CRL yet published vs nonexistent version) mapped to a single 404 revocation_list_not_found, with the client fail-open consequence noted? [Clarity, contract §RevocationListNotFound, FR-011] <!-- Evaluator: Covered by contract §RevocationListNotFound (unknown/cross-tenant product OR no CRL published OR nonexistent version → one 404; "The CLIENT MUST fail OPEN ... FR-011") -->

## Request/Response Schema Completeness

- [X] CHK015 Is EnforcementRequest's "at least one of activationId or machineBoundKey" identity rule expressed as an enforceable schema constraint (anyOf), not only prose? [Measurability, contract §EnforcementRequest] <!-- Evaluator: Covered by contract §EnforcementRequest anyOf:[{required:[activationId]},{required:[machineBoundKey]}] -->
- [X] CHK016 Are required vs optional fields marked on every request/response schema (nonce required; fingerprint optional; token valid-only)? [Completeness, contract §EnforcementRequest/§EnforcementResult] <!-- Evaluator: Covered by contract required arrays across §EnforcementRequest(required:[nonce]+anyOf, fingerprint optional), §EnforcementResult, §RevocationList, §StalenessWindow, §RevokedIds, §Fingerprint -->
- [X] CHK017 Is request strictness specified (additionalProperties:false) so unknown fields are rejected rather than silently ignored? [Clarity, contract §EnforcementRequest] <!-- Evaluator: Covered by contract §EnforcementRequest additionalProperties:false (also on §Fingerprint) -->
- [X] CHK018 Is StalenessWindow's composition fully specified — the total `seconds` plus each contributing component (tokenTtlSeconds, crlNextUpdateSeconds, offlineToleranceSeconds)? [Completeness, contract §StalenessWindow, FR-013] <!-- Evaluator: Covered by contract §StalenessWindow — seconds (required) + tokenTtlSeconds/crlNextUpdateSeconds/offlineToleranceSeconds with the formula "seconds = max(tokenTtl, crlNextUpdate) + offline tolerance" -->
- [X] CHK019 Is the RevocationList schema complete, with all fields (version, productId, generatedAt, nextUpdate, keyId, signature, revokedIds) marked required? [Completeness, contract §RevocationList, FR-009] <!-- Evaluator: Covered by contract §RevocationList required:[version,productId,generatedAt,nextUpdate,keyId,signature,revokedIds] (all seven) -->
- [X] CHK020 Is the shortLivedToken modeled as an opaque signed string (internal claims not re-modeled) with only the sibling serverTime/expiresAt surfaced? [Clarity, contract §ShortLivedToken, AD-005] <!-- Evaluator: Covered by contract §ShortLivedToken ("an OPAQUE E001 LIC1 token ... Its internal claims are NOT modelled here beyond the sibling serverTime / expiresAt fields") -->
- [X] CHK021 Is the RevokedIds snapshot shape (`{ licenses[], activations[] }`) specified with element format (uuid) and both arrays required? [Completeness, contract §RevokedIds] <!-- Evaluator: Covered by contract §RevokedIds required:[licenses,activations]; both arrays items:{type:string,format:uuid} -->

## Idempotent-Replay Contract

- [X] CHK022 Is the idempotent-replay contract (same nonce + same activation → the ORIGINAL 200 result, same already-minted token) expressed at the API/contract level, not only in the data-model? [Coverage, contract "ANTI-REPLAY NONCE", FR-008/SC-010] <!-- Evaluator: Covered by contract "ANTI-REPLAY NONCE + IDEMPOTENT REPLAY" block + per-op descriptions (same nonce+same activation replays ORIGINAL result at 200) -->
- [X] CHK023 Is it explicit that a replay returns the SAME token (no re-mint, no second anchor advance), so the replayed response is reproducible field-for-field? [Clarity, contract §EnforcementResult, FR-008] <!-- Evaluator: Covered by contract §EnforcementResult ("returned verbatim on an idempotent nonce replay") + ANTI-REPLAY block ("no second token is minted and the monotonic anchor is not advanced twice") -->

## Conditional GET & Caching

- [X] CHK024 Are the CRL cache headers (Cache-Control, Expires, ETag) specified with an explicit alignment rule to nextUpdate? [Completeness, contract "CACHING", FR-010] <!-- Evaluator: Covered by contract 200 headers (Cache-Control max-age aligns to nextUpdate; Expires = nextUpdate; ETag carries version) + "CACHING" info-block -->
- [X] CHK025 Is the conditional-GET flow specified — If-None-Match against the current ETag returns 304 Not Modified? [Completeness, contract §IfNoneMatch, FR-010] <!-- Evaluator: Covered by contract §IfNoneMatch parameter + 304 response + CACHING block ("conditional If-None-Match matching the current version returns 304") -->
- [X] CHK026 Is the ETag→CRL-version binding stated so the validator is testable (the ETag carries the version)? [Measurability, contract §ETag/§RevocationList.version] <!-- Evaluator: Covered by contract ETag header ("Strong validator carrying the CRL version") + §RevocationList.version ("Also the ETag") -->
- [X] CHK027 Is the 304 response defined as bodyless, with the accompanying cache semantics (still valid to nextUpdate) made clear? [Clarity, contract 304] <!-- Evaluator: Covered by contract 304 response ("the cached copy is still valid to nextUpdate. No body") -->

## Content Negotiation (file vs json)

- [X] CHK028 Is the `?format=file` vs default-json content negotiation specified with distinct media types (application/octet-stream vs application/json)? [Completeness, contract §CrlFormatQuery] <!-- Evaluator: Covered by contract §CrlFormatQuery (enum:[json,file], default json) + 200 content with both application/json and application/octet-stream -->
- [X] CHK029 Is it required that both representations serve the SAME canonical signed bytes so the detached signature verifies identically? [Consistency, contract "REPRESENTATIONS", FR-010] <!-- Evaluator: Covered by contract "REPRESENTATIONS" block + octet-stream schema ("the SAME canonical signed CRL bytes as the JSON representation ... verifies identically") -->
- [X] CHK030 Is Content-Disposition: attachment specified as present only on the file form, and the default (json) defined when the param is omitted? [Clarity, contract §CrlFormatQuery] <!-- Evaluator: Covered by contract Content-Disposition header ("Present only on the FILE form (?format=file)") + §CrlFormatQuery default:json -->

## Rate-Limit & Retry Headers

- [X] CHK031 Is the Retry-After header specified on the 429 (and 503) responses, and is the retryAfterSeconds detail kept consistent with that header? [Consistency, contract §RateLimited/§SignerUnavailable, FR-021] <!-- Evaluator: Covered by contract §RateLimited (Retry-After header + details.retryAfterSeconds "also surfaced via the Retry-After header") and §SignerUnavailable (same pairing) -->

## CRL Growth, Versioning & Selection

- [X] CHK032 Is the CRL-growth concern addressed in the contract — a documented bound or a versioned/delta follow-up path as revokedIds accumulates, rather than an unbounded full list? [Coverage, spec Risks, research "Signed revocation list distribution"] <!-- Evaluator: Covered by spec Risks ("CRL growth ... mitigate via versioned/delta lists"), research §"Signed revocation list distribution" ("Avoid: unbounded full-list growth"; delta/filter-cascade follow-up), data-model §5 + plan Risk Mitigation -->
- [X] CHK033 Is the absence of pagination on revokedIds a deliberate, documented decision (full snapshot for MVP) rather than an unstated gap? [Ambiguity, data-model §5] <!-- Evaluator: Covered by data-model §5 (revoked_ids "a full snapshot for MVP"; Delta CRLs "a clean additive follow-up, out of scope for 0009") -->
- [X] CHK034 Is the version query-parameter semantics specified (omit = latest max(version); a specific version; a nonexistent version → 404)? [Clarity, contract §CrlVersionQuery, FR-009] <!-- Evaluator: Covered by contract §CrlVersionQuery ("Omit to receive the LATEST published version (max(version)) ... A version that does not exist → 404") -->

## API Versioning & Auth-Scope Expression

- [X] CHK035 Is the /v1 versioning convention (and breaking-change → /v2 with /v1 retained) stated and applied uniformly across all three endpoints? [Consistency, contract "VERSIONING"] <!-- Evaluator: Covered by contract "VERSIONING" block ("BREAKING change is introduced under a new prefix (/v2) with /v1 retained") + all three paths mounted under /v1 -->
- [X] CHK036 Is the required `validate` scope expressed per operation (x-rbac.requiredScope) with the note that OpenAPI cannot encode scope on an apiKey scheme, so it is enforced server-side? [Completeness, contract "SCOPE", FR-018] <!-- Evaluator: Covered by contract x-rbac:{requiredScope:validate} on all three ops + "SCOPE" block & securityScheme desc ("OpenAPI cannot express ... scope on an apiKey scheme ... enforced server-side") -->
- [X] CHK037 Is the request media-type expectation (application/json UTF-8; a non-JSON body → 400 validation_error) stated for validate/heartbeat? [Completeness, contract "MEDIA TYPE"] <!-- Evaluator: Covered by contract "MEDIA TYPE" block ("Request and response bodies are application/json (UTF-8) ... A non-JSON request body is rejected 400 validation_error") -->

## Contract ↔ Spec Coverage

- [X] CHK038 Does every validate/heartbeat functional requirement (FR-001..008, FR-014, FR-017, FR-018, FR-020, FR-021) have a representation in the OpenAPI contract? [Traceability, FR-001..021] <!-- Evaluator: Covered by contract info ("Covers FR-001..021") + per-op descriptions/blocks each citing the listed FRs (FR-001..008/014/017/018/020/021) -->
- [X] CHK039 Do the late-added client-side CRL requirements FR-022 (anti-downgrade) and FR-023 (untrusted-signature fallback) appear in the contract, or is their absence from the wire schema documented as client-only? [Coverage, FR-022/FR-023] <!-- Evaluator: Covered — both are client-only behaviors documented as such in spec.md FR-022/FR-023 (each "a client MUST…"; FR-023 explicitly "DISTINCT from ... FR-011") and plan.md Requirement Coverage Map (labeled "client, documented"); the wire substrate they act on (§RevocationList version/keyId/signature) is present in the contract -->
- [X] CHK040 Is the p95<120ms SLO and no-OCSP hot-path constraint (FR-020/SC-008) documented in the contract as a stated non-functional expectation for validate/heartbeat? [Traceability, FR-020, contract "PERFORMANCE SLO"] <!-- Evaluator: Covered by contract "PERFORMANCE SLO" block ("validate/heartbeat MUST meet p95 < 120 ms ... NO third-party round-trip (OCSP was rejected precisely to protect this budget)") -->
