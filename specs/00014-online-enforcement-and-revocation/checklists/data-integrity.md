# Checklist: Data Integrity — Online Enforcement and Revocation
**Created**: 2026-07-18 | **Feature**: [spec.md](../spec.md)

## Expand-Only Migration Discipline

- [X] CHK001 Is the migration required to be additive/expand-only — with an explicit, verifiable prohibition on changing ANY existing column of activation/license/plan — rather than stated only as an aspiration? [Clarity, data-model §2/§4, Implementation Signals MIGRATION] <!-- Evaluator: Covered by data-model header + §2 (activation) + §4 (license "read, never modified") + §7 (plan "kept unchanged") + §11 DDL ("NO changes to any EXISTING column") -->>
- [X] CHK002 Is the migration ordering requirement (`0009` sequential after `0008`, applied under the expand/contract advisory-locked harness) specified so a new object can never precede its referenced parent? [Completeness, data-model §11, plan Project Structure] <!-- Evaluator: Covered by data-model header + §11 ("expand-only, sequential after 0008") + plan Project Structure ("expand/contract advisory-locked migration harness"); DDL orders ALTER activation → CREATE checkin → revocation_list so each FK parent precedes its child -->>
- [X] CHK003 Is the full set of objects `0009` adds enumerated (two `activation` columns + `checkin` + `revocation_list` + their indexes + RLS/policies/grants) so no schema object is left undefined? [Coverage, data-model §11/§13] <!-- Evaluator: Covered by data-model §11 DDL sketch + §13 Migration summary (2 activation cols; checkin + checkin_activation/checkin_prune indexes + FK/nonce_uniq/outcome_shape; revocation_list + product_fk/version_uniq/window; ENABLE/FORCE RLS + 2 policies + grant) -->>
- [X] CHK004 Are the two new `activation` columns required to be nullable/additive with a defined meaning for NULL (never checked in online, not revoked-by-default), so adding them cannot break existing rows or the E009 lifecycle? [Completeness, data-model §2, FR-012] <!-- Evaluator: Covered by data-model §2 (both cols nullable/additive; NULL last_checkin_at = "never checked in online", NOT revoked-by-default) + §6 (never-connected safety, US5/FR-012) -->>
- [X] CHK005 Is a no-breaking-change guarantee for the E008 `license` and E009 `activation` contracts stated (columns read-only, lifecycle unchanged), so downstream readers can rely on stability? [Consistency, data-model §4, AD-0010] <!-- Evaluator: Covered by data-model §4 (license "read, never modified"; activation "extended additively, lifecycle unchanged", machine_bound_token untouched) + ADR-0010 §Decision-Outcome pt 4 (E001 verifier + E009 credential UNCHANGED) -->>

## Referential Integrity & Composite Tenant-Scoped FKs

- [X] CHK006 Is every FK from a new table to a tenant-owned parent required to be a COMPOSITE FK including `tenant_id`, so a child row can never bind to another tenant's parent? [Completeness, data-model Conventions/§8] <!-- Evaluator: Covered by data-model Conventions ("every FK to a tenant-owned parent is a composite FK including tenant_id") + §8 (checkin→activation and revocation_list→product both composite (tenant_id, ...)) -->>
- [X] CHK007 Is the `checkin`→`activation` FK's `ON DELETE NO ACTION` behaviour specified TOGETHER with the invariant that guarantees no orphan/block (check-in TTL always shorter than the E009 90-day activation purge)? [Clarity, data-model §3/§8] <!-- Evaluator: Covered by data-model §3 + §8 (ON DELETE NO ACTION stated with the invariant "check-in TTL (short) is always shorter than the E009 activation purge (90d) → no block/orphan") -->>
- [X] CHK008 Is the `revocation_list`→`product` FK's `ON DELETE NO ACTION` reconciled with the E007 archive-not-delete catalog policy, so a product with published CRLs is never hard-deleted out from under them? [Consistency, data-model §8] <!-- Evaluator: Covered by data-model §8 (ON DELETE NO ACTION; "a product with published CRLs is not hard-deleted (catalog is archive-not-delete, E007)") -->>
- [X] CHK009 Is the composite primary key `(tenant_id, id)` required on both new tables — matching `license`/`activation`/`customer`/`plan` — so referential identity stays tenant-local? [Consistency, data-model Conventions/§8] <!-- Evaluator: Covered by data-model Conventions ("composite (tenant_id, id) — matching license/activation/customer/plan") + §8 ("PK (both new tables) PRIMARY KEY (tenant_id, id)") -->>

## RLS Policy & Append-Only Grant Integrity

- [X] CHK010 Is the `tenant_isolation` policy required on BOTH `USING` (read) and `WITH CHECK` (write) for each new table, so a row can never be INSERTed under a mismatched or unset tenant scope? [Coverage, data-model §10] <!-- Evaluator: Covered by data-model §10 ("on both USING (read) and WITH CHECK (write)"; unset GUC → NULL → zero rows) + §11 DDL policies -->>
- [X] CHK011 Is the immutability of a `checkin` row and a signed `revocation_list` version stated as a data invariant, backed by the deliberate absence of an `UPDATE` grant to the app role (a replay reads, never mutates)? [Consistency, data-model §8/§10] <!-- Evaluator: Covered by data-model §10 ("GRANT SELECT, INSERT only — no UPDATE; check-in record and signed CRL version are immutable; a replay reads, never mutates") + §1/§5 ("one immutable row"/"Immutable once signed") -->>
- [X] CHK012 Is it specified that the two additive `activation` columns are writable only via E009's existing table-level `UPDATE` grant, and that the monotonic anchor advance is the ONLY mutation the online path performs on the new data surface? [Completeness, data-model §10] <!-- Evaluator: Covered by data-model §10 ("activation columns covered by E009's existing table-level UPDATE grant"; new tables SELECT/INSERT-only, no UPDATE) + §6 (anchor advance is the guarded UPDATE) — new tables append-only/immutable, so the anchor advance is the sole mutation -->>

## Anti-Replay Constraint & Row-Shape Integrity

- [X] CHK013 Is the anti-replay integrity constraint defined as `UNIQUE (tenant_id, nonce)` (tenant-scoped, not global), with the tenant-scoping rationale stated so the constraint's boundary is unambiguous? [Clarity, data-model §3/§8, FR-008] <!-- Evaluator: Covered by data-model §3/§8 (checkin_nonce_uniq UNIQUE (tenant_id, nonce)) + §4a (store-and-replay mechanism) + §8 tenant-leading-index rationale ("matching the RLS predicate and tenant-first access pattern") -->>
- [X] CHK014 Is the `checkin_outcome_shape` CHECK fully specified so every `renewed` row carries a token and no reason, and every `refused` row carries a reason and no token — leaving no half-populated row valid? [Completeness, data-model §3/§8, FR-004] <!-- Evaluator: Covered by data-model §3 + §8 (checkin_outcome_shape: (renewed AND token NOT NULL AND reason NULL) OR (refused AND token NULL AND reason NOT NULL)) -->>
- [X] CHK015 Is the `outcome` domain constrained by a CHECK (`IN ('renewed','refused')`) so no undefined outcome value can be persisted? [Measurability, data-model §8] <!-- Evaluator: Covered by data-model §8 + §3 + §11 DDL (CHECK (outcome IN ('renewed','refused'))) -->>
- [X] CHK016 Is the `checkin` per-request nonce required to be distinct in role from the E009 permanent per-activation `activation.nonce`, so the two anti-replay stores are not conflated in the data model? [Ambiguity, data-model §4a] <!-- Evaluator: Covered by data-model §4a ("why not the E009 activation.nonce pattern": E009 = one PERMANENT nonce per activation; checkin = per-request, frequent, TTL-pruned) + §3 ("Distinct from E009 activation.nonce") -->>

## TTL-Pruning / Retention Correctness

- [X] CHK017 Is a retention/pruning requirement defined for the check-in nonce store that guarantees idempotent replay still works for the FULL renewal window before a nonce may be pruned? [Completeness, data-model §4a/§9, FR-008] <!-- Evaluator: Covered by data-model §4a/§9 (nonce remembered ≤ renewal window; purge deletes rows older than max(renewal-window TTL)+skew, so a nonce is retained through the token's full validity — replay returns the original until after expiry) -->>
- [X] CHK018 Is the pruning horizon quantified (rows older than `max(renewal-window TTL) + skew`) so retention correctness is measurable rather than "eventually"? [Measurability, data-model §9] <!-- Evaluator: Covered by data-model §9 + §4a ("TTL-pruned to max(renewal-window TTL) + skew") -->>
- [X] CHK019 Is the pruning-safety argument stated as a requirement (a pruned nonce could at most reproduce an already-expired token, fail-closed on expiry), so pruning provably cannot re-open a replay window? [Clarity, data-model §4a, FR-011] <!-- Evaluator: Covered by data-model §4a ("Beyond that a replay of the nonce could at most reproduce an already-expired token (fail-closed on expiry, FR-011), so forgetting it is safe") + §9 -->>
- [X] CHK020 Is the boundedness invariant of the check-in table captured (live rows per activation ≈ `TTL / heartbeat-cadence`), so the store is provably bounded rather than growing unbounded like the rejected permanent-nonce design? [Completeness, data-model §4a, AD-002] <!-- Evaluator: Covered by data-model §4a ("live rows per activation ≈ TTL / heartbeat-cadence (a handful)"; contrasted with the unbounded permanent-nonce design) + plan AD-002 -->>

## Monotonic Anchor Integrity

- [X] CHK021 Is `last_anchor_at`'s non-decreasing invariant defined via a guarded `UPDATE` (`WHERE last_anchor_at IS NULL OR last_anchor_at <= $t`) and explicitly declared a repo invariant, NOT a DB trigger, so the enforcement mechanism is unambiguous? [Clarity, data-model §2/§6, FR-014] <!-- Evaluator: Covered by data-model §2 + §6 + §8 (guarded UPDATE ... WHERE last_anchor_at IS NULL OR last_anchor_at <= $t; "a repo invariant, not a DB trigger") -->>
- [X] CHK022 Is the separation between `last_checkin_at` (descriptive "last contact", prunable) and `last_anchor_at` (enforcement floor, monotone) specified with distinct invariants, so retention cannot accidentally clear the anti-rollback floor? [Consistency, data-model §2/§6] <!-- Evaluator: Covered by data-model §2 ("distinct invariants: last_checkin_at descriptive/prunable vs last_anchor_at enforcement floor with monotonicity guarantee; kept separate so the anti-rollback floor is explicit") + §6/§9 -->>
- [X] CHK023 Is it specified that an idempotent replay must NOT advance `last_checkin_at` or the anchor (only a fresh `renewed` beat does), so a replay cannot bump enforcement state? [Completeness, data-model §2/§4a] <!-- Evaluator: Covered by data-model §2 (last_checkin_at "not advanced on a refused beat or an idempotent replay") + §4a ("no replay ever mints an extra token or advances the anchor") -->>

## CRL Byte-Stability, Version Monotonicity & next_update

- [X] CHK024 Is the CRL required to be signed over a canonical, byte-stable encoding of `{version, generated_at, next_update, revoked_ids}`, with "identical bytes for the JSON and `?format=file` forms" stated as a data invariant? [Consistency, data-model §5, contract §RevocationList] <!-- Evaluator: Covered by data-model §5 ("signed over exact bytes ... canonical encoding of {version, generated_at, next_update, revoked_ids} ... re-servable byte-stable for CDN caching and air-gap file export") + contract §RevocationList / file form ("SAME canonical signed bytes ... signature verifies identically") -->>
- [X] CHK025 Is CRL `version` monotonicity per `(tenant, product)` defined as an app-layer invariant (`max(version)+1` in the generation transaction) with a `UNIQUE (tenant_id, product_id, version)` backing it, and are gaps explicitly tolerated while monotone increase is guaranteed? [Clarity, data-model §5/§8, FR-009] <!-- Evaluator: Covered by data-model §5 ("worker assigns max(version)+1 per product inside the generation transaction ... gaps are tolerated, monotone increase is guaranteed") + §8 (UNIQUE (tenant_id, product_id, version)) -->>
- [X] CHK026 Is `next_update > generated_at` specified as a CHECK constraint so a CRL can never be published already-expired? [Measurability, data-model §8, FR-010] <!-- Evaluator: Covered by data-model §8 (revocation_list_window CHECK (next_update > generated_at)) + §1 + §11 DDL -->>
- [X] CHK027 Is a `revocation_list` row required to be immutable once signed (no `UPDATE`), so a published version's signed bytes and `next_update` can never drift after distribution/caching? [Completeness, data-model §5/§10] <!-- Evaluator: Covered by data-model §5 ("one immutable revocation_list row per published version") + §10 ("no UPDATE ... a signed CRL version is immutable once written") -->>

## CRL Projection Consistency (No Drift)

- [X] CHK028 Is the revoked-id set required to be PROJECTED on demand from `license.status='revoked'` (+ deactivated activations) rather than materialized in a separate table, so the CRL content cannot drift from authoritative status? [Completeness, data-model §5, AD-003] <!-- Evaluator: Covered by data-model §5 ("Projected on demand, never materialized ... No new materialized revocation table") + plan AD-003 -->>
- [X] CHK029 Is `license.status` (E008) named as the SINGLE authoritative source of revocation, with an explicit prohibition on a duplicate `revoked_license` table that could diverge? [Consistency, data-model §5, AD-003] <!-- Evaluator: Covered by data-model §5 ("license.status (E008) is the single authoritative source; a separate revoked_license table would duplicate it and risk drift. No new materialized revocation table") -->>
- [X] CHK030 Is `revoked_ids` specified as a point-in-time SNAPSHOT captured at generation (not a live join), and is its relationship to the signed bytes and `generated_at` stated so a reader knows the CRL reflects status as-of generation? [Clarity, data-model §5, contract §RevokedIds] <!-- Evaluator: Covered by data-model §5/§1/§11 ("snapshot content projected from status at generation") + signature covers {version, generated_at, next_update, revoked_ids}, binding the snapshot to generated_at + contract §RevokedIds ("point-in-time snapshot ... projected at generation") -->>

## Idempotent-Replay Data Consistency

- [X] CHK031 Is storing the original `renewed_token` on the `checkin` row (for its TTL window) specified as the mechanism that lets an idempotent replay return the ORIGINAL token — never a re-minted one with a fresh `exp`? [Completeness, data-model §3/§4a, FR-008, SC-010] <!-- Evaluator: Covered by data-model §3 (renewed_token "stored only so an idempotent replay returns the original token (never a re-minted one with a fresh exp)") + §4a -->>
- [X] CHK032 Is the rejection of a monotonic-counter / windowed-bloom store documented against FR-008's second half (they cannot replay the original result), so the chosen store's necessity is traceable? [Traceability, data-model §4a, FR-008] <!-- Evaluator: Covered by data-model §4a ("Rejected alternatives": a monotonic per-activation counter and a windowed bloom filter both cannot satisfy FR-008's second half — return the original result) -->>

## Audit-on-Mutation Integrity

- [X] CHK033 Is a requirement defined that every check-in outcome and every CRL publication appends exactly one `audit_log` row in the SAME transaction as the data write, so the audit trail can never diverge from the persisted data? [Completeness, data-model Audit/§4/§5, FR-019] <!-- Evaluator: Covered by data-model Conventions/Audit ("every check-in outcome and CRL publication appends one row to the existing audit_log ... in the same transaction (FR-019)") + §4/§5 + spec FR-019 -->>
- [X] CHK034 Is reuse of the existing append-only `audit_log` (no new audit table, no grant change) stated as a consistency constraint with the E002 substrate? [Consistency, data-model §4/§10] <!-- Evaluator: Covered by data-model §4 ("E002 audit_log — reused, append-only ... No new audit table, no grant change") + §10 ("No changes to audit_log grants — it stays INSERT/SELECT-only") -->>

## GDPR Retention / Erasability of Check-in & Last-Seen Data

- [X] CHK035 Is the erasability obligation for check-in/last-seen data satisfied by design (no raw identifier stored → pseudonymized retention + TTL pruning) and stated as a requirement, rather than deferred to a hard-delete path? [Completeness, data-model §9, Key Entities] <!-- Evaluator: Covered by data-model §9 ("no row carries a raw identifier or secret, ordinary pseudonymized retention + TTL pruning satisfies the erasure/retention obligation without a hard-delete grant") + spec Key Entities (inherits E009 GDPR retention/erasability) -->>
- [X] CHK036 Is the aging-out of a stale activation's anchor timestamps tied to the E009 activation retention (90 days after deactivation), so last-seen data is bounded and deletable? [Coverage, data-model §9] <!-- Evaluator: Covered by data-model §9 ("A stale activation's anchor timestamps age out with the E009 activation retention (90d after deactivation)") -->>
- [X] CHK037 Is it specified that no `DELETE` grant exists on the app role, with pruning routed through the platform owner path (using the `checkin_prune` BRIN index), so retention is enforced consistently and least-privilege is preserved? [Consistency, data-model §9/§10] <!-- Evaluator: Covered by data-model §9 ("Bounded deletion is the platform retention path (owner role, using the checkin_prune BRIN index) — no DELETE grant to the app role") + §10 (no DELETE) -->>
