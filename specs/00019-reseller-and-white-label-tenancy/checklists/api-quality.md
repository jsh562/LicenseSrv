# API Quality Checklist: Reseller and White-label Tenancy (E018)
**Created**: 2026-08-14 | **Feature**: [spec.md](../spec.md) | **Checklist**: CHL003 API Quality

## Error-Model Completeness

- [X] CHK001 Are all nine 409 refusal codes (onboarding_conflict, quota_exceeded, field_locked, not_verified, binding_conflict, sub_tenants_unresolved, reseller_suspended, last_owner, invalid_state_transition) each defined as a distinct, testable code with a documented trigger? [Completeness, schemas.Error enum] <!-- Evaluator: Covered by contracts/reseller-api.openapi.yaml schemas.Error.code enum — all nine 409 codes enumerated with inline trigger comments -->
- [X] CHK002 Is every operation that can return `last_owner` identified, given no operation in this contract lists it among its responses (plan attributes it to the reused E005/E006 user-admin surface)? [Completeness, FR-016 / plan Error codes] <!-- Evaluator: Covered by plan.md §API Surface Summary Error codes — "`last_owner` (FR-016) is enforced on the reused E005/E006 user-admin surface, not a new endpoint here"; enumerated in Error.code for completeness -->
- [X] CHK003 Is it unambiguous that out-of-scope / cross-tenant id references resolve to 404 not_found (never 403) with no existence disclosure, for every path-id operation? [Consistency, FR-004/005, SC-002] <!-- Evaluator: Covered by info.description §Downward-only visibility + parameters SubTenantId/BindingId + responses SubTenantNotFound/BindingNotFound (out-of-subtree resolves 404, never 403, security-event audited) -->
- [X] CHK004 Does the spec require that RBAC/plane denials and CSRF failures both surface as 403 forbidden and are recorded as security events? [Completeness, responses.Forbidden, SC-005/007] <!-- Evaluator: Covered by responses.Forbidden (both role/plane deny AND csrf failure → 403, recorded as security event) + parameters.CsrfToken description -->
- [X] CHK005 Is each spec Edge Case mapped to a specific documented response code (over-quota→quota_exceeded, offboard-with-sub-tenants→sub_tenants_unresolved, duplicate host→binding_conflict, locked-field override→field_locked, suspended-cascade mutation→reseller_suspended)? [Completeness, spec Edge Cases] <!-- Evaluator: Covered by responses ProvisionConflict/SubTenantsUnresolved/BindingConflict/SubTenantBrandingConflict and plan §Error Handling Strategy table — each edge case maps to a distinct 409 -->
- [X] CHK006 Is the "refuse to white-label a trust signal" edge case represented via exclusion from the BrandingFieldName set rather than an unspecified refusal path, and is that mechanism unambiguous? [Clarity, FR-008, SC-006] <!-- Evaluator: Covered by schemas.BrandingFieldName (8-field enum, description states trust signals are NEVER in the set) + info.description white-label section (FR-008) — exclusion, not a refusal path -->
- [X] CHK007 Is the upward/lateral escalation edge case (reaching a parent/platform/sibling) mapped to a defined response — 404 for an id reference vs 403 for a plane breach — with the distinction specified? [Consistency, FR-005, SC-007] <!-- Evaluator: Covered by info.description §Downward-only (out-of-scope id → 404) vs responses.Forbidden wrongPlane example (plane breach → 403); the two mechanisms are distinctly specified -->
- [X] CHK008 Are the two onboarding_conflict reasons (already_reseller, already_sub_tenant) enumerated and distinguishable in the error `details`? [Clarity, onboardReseller, FR-001] <!-- Evaluator: Covered by schemas.Error.details (`onboarding_conflict → { tenantId, reason: "already_reseller" | "already_sub_tenant" }`) + responses.OnboardingConflict both examples -->
- [X] CHK009 Does every mutating operation enumerate its full applicable 4xx set (400/401/403/404/409) rather than an implicit subset? [Completeness, contracts operations] <!-- Evaluator: Resolved — the verify op (checkDomainVerification) was missing its applicable `409 binding_conflict` (a losing concurrent verify hits the global unique index per data-model INV-5); added it to the op Refusals + VerifyConflict response. Other ops enumerate their applicable set (no-body POSTs correctly omit 400; id-less POSTs correctly omit 404). -->

## Auth & Plane Consistency

- [X] CHK010 Is an `x-rbac` plane (operator/reseller/sub_tenant) and `minRole` specified for every one of the 20 operations? [Completeness, x-rbac] <!-- Evaluator: Covered — all 20 operations carry `x-rbac: { plane, minRole }` (operator/reseller/sub_tenant planes; viewer/admin roles) -->
- [X] CHK011 Is the read-vs-mutation RBAC rule (viewer for GET, admin for POST/PUT/PATCH) applied consistently across all operations? [Consistency, info.description RBAC] <!-- Evaluator: Covered — every GET is minRole viewer; every POST/PUT/PATCH is minRole admin, per info.description RBAC rule and each op's x-rbac -->
- [X] CHK012 Is `X-CSRF-Token` required on every mutation and explicitly not required on GET, for all operations? [Consistency, parameters.CsrfToken] <!-- Evaluator: Covered — parameters.CsrfToken (required:true) is referenced by every POST/PUT/PATCH; its description states GET operations do not require it -->
- [X] CHK013 Is it explicit that plane and subtree membership are derived server-side from the session and never from a client-supplied tenant/reseller id? [Clarity, securitySchemes.sessionCookie, FR-004/005] <!-- Evaluator: Covered by securitySchemes.sessionCookie description ("plane and subtree membership are derived SERVER-SIDE from the session — never from a client-supplied tenant/reseller id") + info.description -->
- [X] CHK014 Is the auth credential specified as the E005 `admin_session` cookie only, with no `X-API-Key` accepted on any operation? [Consistency, securitySchemes.sessionCookie] <!-- Evaluator: Covered — the only securityScheme is sessionCookie (admin_session); info.description + scheme description explicitly state there is NO X-API-Key plane -->
- [X] CHK015 Is it specified that a reseller-admin attempting an operator-plane operation is denied (403) and audited as a security event? [Testability, US1-AS4, responses.Forbidden] <!-- Evaluator: Covered by responses.Forbidden (wrongPlane example: reseller-admin attempting an OPERATOR-plane action → 403, recorded as security event) + info.description operator-plane note (US1-AS4) -->

## Contract–Requirement Coverage

- [X] CHK016 Does every FR that implies an operation (FR-001/003/010/011/012/013/015) have a corresponding endpoint, and does every endpoint trace back to at least one FR/SC? [Completeness, plan Requirement Coverage Map] <!-- Evaluator: Covered by plan.md §API Surface Summary + §Requirement Coverage Map — each FR maps to endpoint(s) and each endpoint is FR-tagged (onboard→FR-001/010, provision/quota→FR-003, suspend/reinstate→FR-011, offboard→FR-012, domains→FR-013, move→FR-015) -->
- [X] CHK017 Is the absence of any audit-retrieval endpoint intentional and clearly attributed to the reused E006 audit surface rather than an omission of FR-009? [Completeness, FR-009] <!-- Evaluator: Covered — FR-009 records into "the existing append-only, tamper-evident audit trail" (spec FR-009, info.description); plan §Integration Points maps FR-009 to the reused E002/E005/E006 `audit_log`; spec Implementation Signals NEW-API lists no audit-read endpoint. Recording is in-scope; retrieval reuses E006, not omitted. -->
- [X] CHK018 Is there a specified way for a reseller to view its own quota position (subTenantQuota / subTenantCount), given quota is exposed only on the operator plane? [Completeness, FR-003, SC-008] <!-- Evaluator: Resolved — added read-only `subTenantQuota`/`subTenantCount` to the reseller-plane SubTenantList (listSubTenants) so a reseller can see its own standing against the hard cap before a `409 quota_exceeded`. Reseller reading its own position, not cross-tenant data (FR-003, SC-008). -->
- [X] CHK019 Are all 20 operations accounted for — no spec-implied operation left unspecified and no extra operation lacking a requirement basis? [Consistency, plan API Surface Summary] <!-- Evaluator: Covered — plan §API Surface Summary lists exactly 20 operations, each FR-traced; the contract implements the same 20 (7 operator + 3 reseller sub-tenant + 2 reseller branding + 5 reseller domains ... verified against paths) with no orphan operation -->
- [X] CHK020 Is the metadata-only constraint reflected in the sub-tenant response schemas so no license/usage/activation field is present? [Completeness, FR-017, SC-001] <!-- Evaluator: Covered — schemas.SubTenant carries only identity/status/readOnly/createdAt/resellerId (additionalProperties:false), explicitly no license/usage/activation field (FR-017, SC-001) -->

## State Transitions & Idempotency

- [X] CHK021 Are the reseller lifecycle transitions (active⇄suspended, active→offboarding) fully specified with valid source states and an `invalid_state_transition` trigger for each disallowed transition? [Completeness, ResellerStatus, FR-011/012] <!-- Evaluator: Covered — suspend (from active only), reinstate (from suspended only), offboard (active→offboarding); each disallowed source → 409 invalid_state_transition (responses.InvalidStateTransition + per-op Refusals + schemas.ResellerStatus) -->
- [X] CHK022 Is the behavior of re-invoking offboard on an already-`offboarding` reseller specified (idempotent progress vs invalid_state_transition)? [Testability, offboardReseller, FR-012] <!-- Evaluator: Resolved — offboardReseller description now specifies IDEMPOTENT PROGRESS: re-invoking on an already-offboarding reseller is NOT invalid_state_transition; it re-reports progress (refreshes unresolvedSubTenantCount + graceEndsAt) and completes when count==0 -->
- [X] CHK023 Is it specified whether offboarding is reversible, given `reinstate` accepts only a `suspended` reseller? [Clarity, reinstateReseller / offboardReseller] <!-- Evaluator: Resolved — offboardReseller now states offboarding is a ONE-WAY, TERMINAL path (cannot return to active); reinstateReseller now states it applies SOLELY to a suspended reseller and an offboarding reseller cannot be reinstated (consistent with data-model reseller transitions) -->
- [X] CHK024 Are the binding transitions pending→verified→active specified with pre/post-conditions and the re-invocation behavior of verify/activate on an already-verified or already-active binding? [Testability, BindingStatus, FR-013] <!-- Evaluator: Resolved — verify/activate descriptions now specify IDEMPOTENT re-invocation (re-verify on verified/active or re-activate on active → 200 no-op) and verify's losing-race pre-condition (`409 binding_conflict` per data-model INV-5); pre/post transitions pending→verified→active retained -->
- [X] CHK025 Are the move pre-conditions (destination quota, source/destination not suspended, destination not offboarding) each mapped to a distinct 409 code? [Completeness, moveSubTenant, FR-015] <!-- Evaluator: Covered by moveSubTenant Refusals + responses.MoveConflict — destination at cap → quota_exceeded; source/destination suspended → reseller_suspended; destination offboarding → invalid_state_transition -->
- [X] CHK026 Is the suspend read-only cascade specified as derived at request time (the `readOnly` flag) rather than stored per sub-tenant, so its post-condition is testable? [Testability, AD-007, FR-011] <!-- Evaluator: Covered — schemas.SubTenant.readOnly is described as DERIVED at request time from the reseller's suspended status (AD-007), not stored per sub-tenant; SubTenantStatus notes it is independent of the derived cascade -->

## Request/Response Shape Clarity

- [X] CHK027 Is the OnboardResellerRequest `oneOf` unambiguously discriminated by `mode`, with required fields per branch (displayName for create_new, tenantId for promote_existing)? [Clarity, OnboardResellerRequest] <!-- Evaluator: Covered — OnboardResellerRequest oneOf with discriminator `mode`; OnboardCreateNew requires [mode, displayName, firstAdminUserReference], OnboardPromoteExisting requires [mode, tenantId, firstAdminUserReference] -->
- [X] CHK028 Is the MoveSubTenantRequest `destination` `oneOf` unambiguously discriminated by `type` (to_reseller requires destinationResellerId; to_direct_platform requires none)? [Clarity, MoveSubTenantRequest] <!-- Evaluator: Covered — MoveSubTenantRequest.destination oneOf with discriminator `type`; MoveToReseller requires [type, destinationResellerId], MoveToDirectPlatform requires [type] only -->
- [X] CHK029 Is the PUT branding replace-vs-merge semantics unambiguous — does omitting a field clear it, and does omitting `locked` clear all locks? [Clarity, SetResellerBrandingRequest] <!-- Evaluator: Covered — SetResellerBrandingRequest "Replaces the profile" (PUT replace: an omitted field clears it) and `locked: Omit/empty for none` (omitting locked clears all locks); reinforced by the CHK030 replace-by-omission clarification -->
- [X] CHK030 For sub-tenant override removal, is the distinction between omitting a field and sending `null` specified, given BrandingFields does not type its fields as nullable? [Clarity, SetSubTenantBrandingRequest, FR-007] <!-- Evaluator: Resolved — SetSubTenantBrandingRequest + setOwnBranding now specify REPLACE-by-OMISSION: a present field sets/updates the override, an omitted field removes it (falls back per precedence); fields are non-nullable so `null` is not a distinct signal and is rejected `400 validation_error` (aligns prose to the non-nullable BrandingFields schema) -->
- [X] CHK031 Is the ResolvedField shape (field, nullable value, source, locked) sufficient to test the per-field precedence and locked outcomes required by SC-004? [Testability, ResolvedField, SC-004] <!-- Evaluator: Covered — schemas.ResolvedField carries {field, value:[string,null], source:(sub_tenant|reseller|platform), locked:boolean}, sufficient to assert each field's effective value, precedence source, and lock outcome (SC-004) -->
- [X] CHK032 Is the `not_verified` trigger for setting a customDomain / emailSenderAddress to an unverified host clearly tied to requiring a verified+active binding first? [Clarity, setResellerBranding / setOwnBranding, FR-013] <!-- Evaluator: Covered — setResellerBranding + setOwnBranding refuse `409 not_verified` for a customDomain/emailSenderAddress whose host is not a verified+active binding; BrandingFields field descriptions state a VERIFIED+active binding is required (FR-013) -->
- [X] CHK033 Is the verification response (DomainBinding.challenge DNS records, and the `unmet` records detail on failure) specified precisely enough to be actionable? [Clarity, initiateDomainVerification / checkDomainVerification, FR-013] <!-- Evaluator: Covered — DomainBinding.challenge is an array of DnsRecord {purpose, recordType, name, value}; on failure VerifyConflict `details.unmet` lists [{purpose, recordType}] — actionable DNS records to publish (FR-013) -->

## Pagination, Determinism & Bounds

- [X] CHK034 Are the reseller and sub-tenant list endpoints specified as deterministically ordered (displayName then id) and bounded (1000 cap + `truncated` signal)? [Testability, listResellers / listSubTenants] <!-- Evaluator: Covered — ResellerList/SubTenantList: maxItems 1000 + `truncated`; listResellers ordered displayName then resellerId, listSubTenants ordered displayName then subTenantId -->
- [X] CHK035 Is the behavior when a list exceeds the 1000 hard cap specified (truncated=true, no pagination), and is the inability to retrieve the remainder an acceptable, stated limit? [Clarity, ResellerList / SubTenantList] <!-- Evaluator: Covered — ResellerList description "bounded to 1000 with a truncated signal; NOT paginated"; `truncated` = true when more than 1000 exist and the result was capped (the non-paginated cap is the stated limit) -->
- [X] CHK036 Is the domain-binding list (DomainBindingList) specified with a deterministic order and a bound, or is its unbounded/unordered shape an intentional, documented gap? [Completeness, listDomainBindings] <!-- Evaluator: Resolved — aligned DomainBindingList with the other lists: ordered by `host` then `bindingId`, maxItems 1000, `truncated` signal (required); listDomainBindings description updated to match -->

## Runtime/Crypto Plane Exclusion

- [X] CHK037 Is it explicit that this contract is admin/console-only with no runtime plane, no API-key surface, and no signing/crypto/token operation? [Consistency, info.description] <!-- Evaluator: Covered by info.description (ADMIN-PLANE, CONSOLE-ONLY; NO runtime/API-key plane; NO signing/crypto/token surface) + white-label section (presentation-only, no crypto) -->
- [X] CHK038 Is it specified that the DNS challenge token is a public proof (never a secret) and that no request/response/audit field carries a secret, key, or PII beyond a pseudonymous reference? [Consistency, info.description Secrecy invariants, FR-017] <!-- Evaluator: Covered by info.description §Secrecy & PII invariants + DnsRecord/DomainBinding descriptions (challenge token is a PUBLIC proof); users referenced only by pseudonymous userReference; no secret/key/PII/PAN in any field -->
- [X] CHK039 Is the unversioned `/admin` path convention (no `/v1`) explicitly justified by the absence of any runtime wire operation? [Consistency, info.description Versioning] <!-- Evaluator: Covered by info.description §Versioning ("admin/console plane is deliberately UNVERSIONED (/admin/...) ... no /v1 runtime plane because there is no runtime wire operation") -->
