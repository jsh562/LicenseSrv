# CHL002 Data Integrity: Reseller and White-label Tenancy
**Created**: 2026-08-14 | **Feature**: [spec.md](../spec.md)

## Forced RLS / Isolation Invariants

- [X] CHK001 Is the requirement that the per-tenant `tenant_isolation` predicate remains UNCHANGED stated precisely enough to be checked against migration `0014`? [Clarity, INV-2/AD-002] <!-- Evaluator: Covered by data-model.md INV-2 + §isolation-crux and migration 0014 (new tables get an identically-formed policy; no ALTER of the existing predicate) -->
- [X] CHK002 Are the requirements complete that unset/empty `app.current_tenant` GUC → NULL → zero rows holds on ALL three new tables (`reseller`, `branding_profile`, `domain_binding`)? [Completeness, INV-1] <!-- Evaluator: Covered by data-model.md INV-1 and migration 0014 §6 — tenant_isolation on NULLIF(current_setting('app.current_tenant',true),'')::uuid created for all three tables -->
- [X] CHK003 Is `ENABLE`+`FORCE ROW LEVEL SECURITY` (owner also subject to RLS) required for every new tenant-owned table, not just enabled? [Completeness, INV-1/data-model §6] <!-- Evaluator: Covered by data-model.md INV-1 ("the owner is subject to RLS too (FORCE)") + §DDL step 6 and migration 0014 lines 166-168 (ENABLE + FORCE on all three tables) -->
- [X] CHK004 Is the reseller subtree READ defined unambiguously as a privileged, ownership-asserted seam rather than a widened predicate, so an implementation that broadens RLS would be rejected? [Clarity, INV-2/AD-002] <!-- Evaluator: Covered by data-model.md §isolation-crux (row 1) + INV-2 and plan AD-002/HINT-001; migration 0014 lines 10-14 (privileged seam WHERE parent_reseller_id=:reseller after ownership assertion, never a widened predicate) -->
- [X] CHK005 Is the requirement that a sub-tenant CANNOT read its reseller's `branding_profile` row under RLS (reseller-default read is privileged-seam only) stated and consistent with the resolver? [Consistency, data-model Branding precedence] <!-- Evaluator: Covered by data-model.md §Branding precedence ("The reseller-default layer is read cross-tenant via the privileged seam ...; a sub-tenant cannot read its reseller's row under RLS") -->
- [X] CHK006 Is the reseller-ACTION model (scoped descent under the sub-tenant's own `app.current_tenant`) separated from the subtree-membership gate clearly enough to keep the write checked against the sub-tenant's rows? [Clarity, AD-001/INV-2] <!-- Evaluator: Covered by data-model.md §isolation-crux (row 2) + INV-2; plan AD-001/HINT-001; migration 0014 lines 15-17 (gate asserts ownership, mutation runs under the sub-tenant's own withTenant scope) -->
- [X] CHK007 Is the out-of-subtree reference outcome (zero rows → 404, no existence disclosure, + `security_event`) traceable to a measurable success criterion? [Testability, SC-002/SC-007] <!-- Evaluator: Covered by spec.md SC-002 (404, no disclosure, 100% of attempts) + SC-007 (escalation denied + security event, zero cross-subtree rows); data-model.md §isolation-crux row 3 + INV-2 -->

## Referential Integrity

- [X] CHK008 Is the self-ref FK `tenant.parent_reseller_id` fully specified (target `tenant(id)`, `ON DELETE NO ACTION`, `CHECK(<> id)`, partial index `WHERE NOT NULL`)? [Completeness, data-model tenant] <!-- Evaluator: Covered by data-model.md tenant entity row + §DDL step 1; migration 0014 lines 33-49 (FK REFERENCES tenant(id) ON DELETE NO ACTION, CHECK <> id, partial index WHERE parent_reseller_id IS NOT NULL) -->
- [X] CHK009 Is the one-level rule complete — is a requirement stated that a tenant carrying a `reseller` row must NOT itself have a `parent_reseller_id`, given a single-column CHECK cannot assert "not referenced as a parent"? [Completeness, INV-3] <!-- Evaluator: Covered by data-model.md INV-3 ("a reseller ... must not itself carry a parent_reseller_id — nesting refused at the service layer (a single-column CHECK cannot assert 'not referenced as a parent')"); migration 0014 lines 30-32 -->
- [X] CHK010 Is the enforcement layer for the no-nesting rule unambiguously assigned to the service layer (with a stated rationale for why the DB CHECK is insufficient)? [Clarity, INV-3] <!-- Evaluator: Covered by data-model.md INV-3 (nesting "refused at the service layer" with rationale: a single-column CHECK cannot assert not-referenced-as-a-parent); migration 0014 lines 30-32 restate this -->
- [X] CHK011 Is the `ON DELETE NO ACTION` implication (a referenced reseller cannot be deleted while sub-tenants point to it) reconciled with the offboard transfer-or-reassign gate so no requirement contradicts the other? [Consistency, data-model tenant/FR-012] <!-- Evaluator: Resolved — added a "ON DELETE NO ACTION vs the offboard gate (RESOLVED — reconciled)" bullet to data-model.md §Resolved modeling decisions: dependents block hard-delete; offboard resolves refs (transfer/reassign) then tombstones (no hard delete) — FK guard and offboard gate are complementary. Consistent with migration 0014 lines 38-39/66-68 -->
- [X] CHK012 Are the composite PK requirements for `domain_binding` (`PK=(tenant_id,id)`) and `audit_log` unambiguous about which column carries the tenant scope? [Clarity, data-model domain_binding/audit_log] <!-- Evaluator: Covered by data-model.md domain_binding row (PK=(tenant_id,id)) + ER diagram (audit_log tenant_id PK = "target sub-tenant scope"); migration 0014 line 124 (PRIMARY KEY (tenant_id, id)); tenant_id is the RLS scope column in both -->
- [X] CHK013 Is the deliberate ABSENCE of an FK on `audit_log.actor_reseller_id` stated with rationale (attribution survives transfer), so a reviewer can confirm it is intentional not an omission? [Clarity, INV-8] <!-- Evaluator: Covered by data-model.md INV-8 + §Resolved modeling decisions (dual-identity column); migration 0014 lines 190-195 ("No FK (parity with the existing un-FK'd tenant_id ...)", attribution survives a later transfer) -->
- [X] CHK014 Are the 1:1 relationships (`reseller`↔tenant, `branding_profile`↔tenant, both `PK=tenant_id`) complete about which tenants MAY versus MUST have a row? [Completeness, data-model reseller/branding_profile] <!-- Evaluator: Covered by data-model.md — reseller "1:1 with a tenant that IS a reseller" (presence of the row = reseller designation; others none), branding_profile "1:1 with a tenant" and optional (resolver falls back to platform default when absent) -->
- [X] CHK015 Is the reassign-to-platform semantics (`parent_reseller_id` set NULL) defined so NULL unambiguously means direct-platform OR reseller-itself without collision? [Clarity, data-model tenant] <!-- Evaluator: Covered by data-model.md tenant row ("NULL = direct-platform tenant OR a reseller tenant itself") — the two are disambiguated by presence/absence of a reseller row, so no harmful collision; migration 0014 lines 29-32 -->

## One-Binding-Per-Host

- [X] CHK016 Is the uniqueness requirement scoped precisely — `(binding_type, host)` unique across `status IN ('verified','active')`, globally across all tenants? [Clarity, INV-5/FR-013] <!-- Evaluator: Covered by data-model.md INV-5 + domain_binding row + §one-binding-per-host; migration 0014 lines 158-159 (CREATE UNIQUE INDEX domain_binding_host_bound_uniq ON domain_binding (binding_type, host) WHERE status IN ('verified','active')) -->
- [X] CHK017 Is the requirement that this unique index holds INDEPENDENT of RLS/GUC (a physical constraint that does not read `app.current_tenant`) stated explicitly? [Completeness, INV-5] <!-- Evaluator: Covered by data-model.md INV-5 + §one-binding-per-host ("a UNIQUE INDEX is a physical constraint ... does not read app.current_tenant ... holds even under forced RLS and even with an unset GUC"); migration 0014 lines 147-157 -->
- [X] CHK018 Is the rationale for covering BOTH `verified` and `active` (not `verified`-only) captured, so the predicate cannot be narrowed and let an active host be re-claimed? [Completeness, data-model one-binding-per-host] <!-- Evaluator: Covered by data-model.md §one-binding-per-host + INV-5 + §Resolved decisions ("Covering both bound states is critical: an active host cannot be re-verified/claimed ... a verified-only predicate would let an active row escape"); migration 0014 lines 152-154 -->
- [X] CHK019 Are the multiple-`pending`-claims-per-host allowance and the anti-squatting rule (pending never locks out the true owner) complete and consistent with single verified/active? [Consistency, INV-5/data-model resolved decisions] <!-- Evaluator: Covered by data-model.md §one-binding-per-host + §Resolved decisions ("multiple tenants MAY hold a pending claim ... only one may reach verified/active ... first-to-bind wins; anti-squatting preserved"); migration 0014 lines 151-155 -->
- [X] CHK020 Is "normalized host" defined (what normalization is applied) so uniqueness comparison is deterministic and testable? [Testability, data-model domain_binding] <!-- Evaluator: Resolved — added a "domain_binding.host normalization (RESOLVED — standard default)" bullet to data-model.md §Resolved modeling decisions: trim, lower-case, strip trailing dot, IDNA/ToASCII punycode; applied identically at write and at Host→tenant lookup. Standard safe default, consistent with migration 0014 line 114 (lower-cased) -->
- [X] CHK021 Is the losing verify/activate outcome (`409 binding_conflict`, no cross-tenant disclosure) specified for BOTH the verify and the activate steps? [Completeness, INV-5/SC-011] <!-- Evaluator: Covered by data-model.md INV-5 + §one-binding-per-host + §Resolved decisions ("the losing verify/activate attempt hits the index -> unique_violation -> 409 binding_conflict with no cross-tenant disclosure") and migration 0014 lines 154-155; SC-011. NOTE (API-quality, not data-integrity): contract VerifyConflict omits binding_conflict on /verify though INV-5 permits it -->

## Branding Resolution Correctness

- [X] CHK022 Is per-field INDEPENDENT fallback (each field resolves separately, not whole-profile) stated unambiguously enough to reject a whole-profile design? [Clarity, FR-007/INV-4] <!-- Evaluator: Covered by spec.md FR-007 ("each field falls back independently") + data-model.md §Branding precedence ("For each of the 8 ... fields independently") + INV-4; plan AD-004 -->
- [X] CHK023 Is the locked-field rule complete — a reseller-locked field ignores any sub-tenant override and is presented as non-editable WITHOUT revealing the hierarchy? [Completeness, FR-006/INV-4/STF-004] <!-- Evaluator: Covered by spec.md FR-006 + STF-004 + data-model.md §Branding precedence/INV-4 ("a field in the reseller's locked_fields ... override is ignored and it is presented ... non-editable without revealing the reseller hierarchy") -->
- [X] CHK024 Is "computed at read, never stored resolved (no drift)" stated so a stored-resolution (drifting) implementation would fail the requirement? [Testability, AD-004/INV-4] <!-- Evaluator: Covered by data-model.md INV-4 ("computed at read, never stored resolved (no drift)") + branding_profile row + plan AD-004; migration 0014 lines 74-76 -->
- [X] CHK025 Is the exact 8-field `BrandingFieldName` set defined and mapped 1:1 to DB columns, with the dropped `support_email` and added `help_url` reconciled? [Consistency, data-model resolved decisions] <!-- Evaluator: Covered by data-model.md §Resolved decisions (branding_profile field set: 8 columns mapped 1:1 to contract BrandingFieldName, support_email dropped, help_url added) + §Branding precedence mapping; contract BrandingFieldName enum (8) -->
- [X] CHK026 Is the `locked_fields` validation authority (service-layer allow-list vs DB CHECK) specified together with the valid member set (the 8 field names)? [Clarity, AD-005/data-model] <!-- Evaluator: Covered by data-model.md §Resolved decisions (locked_fields: DB CHECK asserts array only; member allow-list = the 8 BrandingFieldNames validated service-layer, per AD-005); migration 0014 lines 91-98 -->
- [X] CHK027 Is the dependency that `emailSenderAddress`/`customDomain` take effect only when a matching binding is `status='active'` stated as a resolution-time requirement? [Completeness, INV-6/FR-013] <!-- Evaluator: Covered by data-model.md §Branding precedence ("emailSenderAddress/customDomain only take effect once a matching domain_binding reaches status='active'") + INV-6; migration 0014 lines 89-90 -->
- [X] CHK028 Are trust signals (revocation/tamper/security notices, signing identity, audit, legal text) explicitly and enumerably excluded from `branding_profile` sourcing? [Completeness, FR-008/INV-4] <!-- Evaluator: Covered by spec.md FR-008 + data-model.md §Branding precedence ("Trust signals (revocation/tamper/security notices, signing identity, audit, legal text) are never sourced from branding_profile") + INV-4; migration 0014 lines 79-80 -->

## Lifecycle State Integrity

- [X] CHK029 Is the `reseller.status` enum (`active|suspended|offboarding`) and its full allowed/refused transition set specified (including which source states reject suspend/reinstate/offboard)? [Completeness, data-model reseller/plan error map] <!-- Evaluator: Covered by data-model.md reseller row transitions (active⇄suspended; active→offboarding) + plan Error Handling row (suspend non-active / reinstate non-suspended / move into offboarding → 409 invalid_state_transition) + contract suspend/reinstate/offboard responses -->
- [X] CHK030 Is the suspend read-only cascade defined as DERIVED at request time (no fan-out write) unambiguously enough to reject a per-sub-tenant stored flag? [Clarity, AD-007/INV-9] <!-- Evaluator: Covered by data-model.md §isolation-crux row 4 + INV-9 ("derived at request time ... no fan-out write") + plan AD-007; contract SubTenant.readOnly "DERIVED read-only-cascade flag" -->
- [X] CHK031 Is the quota specified as a HARD cap with a complete rule for lowering below current `subTenantCount` (no deletion, blocks further provisioning)? [Completeness, FR-003/SC-008] <!-- Evaluator: Covered by spec.md FR-003/SC-008 (hard cap) + contract updateResellerQuota ("Lowering a quota below the current subTenantCount does NOT delete existing sub-tenants but blocks further provisioning until the count falls below the new cap") -->
- [X] CHK032 Is the offboard no-orphan gate complete — every sub-tenant transferred or reassigned before completion, with a defined notice/grace window? [Completeness, FR-012/SC-010] <!-- Evaluator: Covered by spec.md FR-012/SC-010 + contract offboardReseller (409 sub_tenants_unresolved until every sub-tenant transferred/reassigned; graceEndsAt notice/grace window) + OffboardResult schema -->
- [X] CHK033 Is the transfer requirement complete — keep sub-tenant overrides, re-resolve reseller-default + per-field locks to the destination, dual audit on source AND destination? [Completeness, FR-015/STF-006] <!-- Evaluator: Covered by spec.md FR-015 + STF-006 + contract moveSubTenant ("preserves the sub-tenant's own overrides while the reseller-default layer and per-field locks re-resolve to the destination ... audited on both source and destination") -->
- [X] CHK034 Is the sub-tenant's own `status` kept distinct from the derived `readOnly` cascade, with both independently defined so they cannot be conflated? [Consistency, contract SubTenantStatus] <!-- Evaluator: Covered by contract SubTenantStatus ("Independent of the DERIVED read-only cascade, which is reported separately by readOnly") + SubTenant.status/SubTenant.readOnly fields -->
- [X] CHK035 Are the move-blocking conditions (source/destination suspended, destination offboarding, destination quota) completely enumerated as distinct refusal reasons? [Completeness, plan error map/FR-015] <!-- Evaluator: Covered by contract moveSubTenant refusals + MoveConflict (quota_exceeded destination full, reseller_suspended source-or-destination, invalid_state_transition destination offboarding) + plan Error Handling row -->

## Audit Append-Only

- [X] CHK036 Are the append-only + tamper-evident requirements (`audit_log` keeps `SELECT,INSERT` only; no role may edit/delete) stated as verifiable grant-level constraints? [Testability, INV-8/FR-009] <!-- Evaluator: Covered by data-model.md INV-8 ("audit_log keeps SELECT,INSERT only (no UPDATE/DELETE grant) — tamper-evident") + spec.md FR-009; migration 0014 lines 195/207-208 (no new grant; append-only preserved) -->
- [X] CHK037 Is dual-identity complete — `tenant_id`=target sub-tenant, `actor`=reseller-admin user, `actor_reseller_id`=acting reseller home tenant, NULL for non-delegated actions? [Completeness, INV-8/FR-009] <!-- Evaluator: Covered by data-model.md INV-8 + audit_log row + §Resolved decisions (tenant_id=target, actor=reseller-admin user, actor_reseller_id=acting reseller home tenant, NULL for ordinary non-delegated); migration 0014 lines 183-195 -->
- [X] CHK038 Is the requirement that `actor_reseller_id` is stored INDEPENDENTLY of the mutable `parent_reseller_id` (attribution survives a later transfer) explicitly captured? [Clarity, AD-008/INV-8] <!-- Evaluator: Covered by data-model.md INV-8 + §Resolved decisions ("stored independently of the mutable tenant.parent_reseller_id so the attribution survives a later sub-tenant transfer") + plan AD-008; migration 0014 lines 190-192 -->

## Migration Safety

- [X] CHK039 Is the expand-only guarantee complete — no existing column or semantics altered, forced RLS preserved, only additive columns/tables? [Completeness, INV-10] <!-- Evaluator: Covered by data-model.md INV-10 + §DDL; migration 0014 header lines 4-7 ("Expand-only: ONE additive self-ref column ... ONE additive nullable column ... three NEW tables; NO existing column is altered; the per-tenant forced-RLS predicate is UNCHANGED") -->
- [X] CHK040 Is the sequential ordering (after `0013`) and the least-privilege grant posture (config tables full DML on `licensesrv_app`; `audit_log` append-only unchanged) specified without ambiguity? [Clarity, INV-10/INV-11] <!-- Evaluator: Covered by data-model.md INV-10/INV-11 + §DDL step 7; migration 0014 lines 1-3 (sequential after 0013) and 199-208 (SELECT,INSERT,UPDATE,DELETE on the three config tables; audit_log stays SELECT,INSERT) -->
