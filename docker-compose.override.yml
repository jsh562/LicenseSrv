# Local dev override (auto-merged by `docker compose`). Exposes Postgres on the host so the
# dev seed script (scripts/seed-dev.ts) can connect at localhost:5432. NOT for production.
services:
  db:
    ports:
      - "15432:5432"
